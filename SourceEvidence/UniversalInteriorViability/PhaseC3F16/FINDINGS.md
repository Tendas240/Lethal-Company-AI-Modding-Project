# C3F16 — Black Mesa x Greenhouse repository-held runtime-evidence sufficiency review

**Status:** TRUSTED RUNTIME EVIDENCE REVIEW COMPLETE / EXACT BLACK MESA x GREENHOUSE RUNTIME PAIR ABSENT / SMALLEST FUTURE RUNTIME CONTRACT DEFINED / NO MATRIX, BUILD OR GAMEPLAY CHANGE  
**Date:** 2026-09-23  
**Accepted baseline:** S1.42AK  
**Working PR:** #138 / `scope/universal-interior-phase-c3a-dawn-tags`  
**Parent head:** `9d965e0c5aa8dddf30d9445558936e0625f7cb77`

## Bounded objective

C3F16 follows C3F15's first strong Count-3 result and asks one repository-held question:

> Does trusted existing runtime evidence already prove the exact pairing Black Mesa moon x `GreenhouseFlow` through successful generation, practical entrance/traversal use and sufficient routing/NavMesh behavior?

The answer is **no**.

This checkpoint does not build a diagnostic candidate, start gameplay, change the B3 matrix or modify any runtime/build controller.

## Evidence reviewed

### Authoritative C1 trusted-runtime triage

`Current/168_S1.42AK_UNIVERSAL_INTERIOR_PHASE_C1_EXISTING_RUNTIME_COMPATIBILITY_TRIAGE.md` is the completed conservative scan of trusted runtime-pass / accepted / accepted-focused evidence plus the explicitly authorized LC Office DIAG2 A/B.

C1 establishes that **all observed generation in its trusted evidence set is on Offense**.

For `GreenhouseFlow`, C1 records only:

- `RuntimeEvidence/S1.42AH/20260909T162513Z/raw/LogOutput.log`;
- result class `GENERATED_ONLY`;
- Greenhouse generated on **Offense**;
- four entrance connections were formed;
- no DunGen generation-failure marker was observed;
- no player entry was recorded before the next day.

The corresponding runtime session index binds the raw log at SHA-256 `ae57fb71a38952936e9056150240e2eaa70be9d89253b15644b3f5d35dd09729`.

That evidence is useful interior-side generation evidence. It is not Black Mesa moon evidence and does not provide Greenhouse player traversal even on Offense.

### Historical Black-Mesa-looking entrance signals

C3F1 already rechecked the historical runtime material that could otherwise be mistaken for Black Mesa moon topology evidence:

- S1.42AA explicitly ran on Offense;
- S1.42AC raw evidence explicitly reports `current planet: 21 Offense`.

Therefore historical `EntranceTeleportA/B(Clone)` observations from those runs cannot establish Black Mesa moon x Greenhouse or any other Black Mesa-moon pairing.

### No later runtime evidence on PR #138

The complete current PR #138 changed-file inventory contains no path under `RuntimeEvidence/`.

Therefore the C3 source/static work after the authoritative C1 scan has not added a later gameplay session that could supply the missing exact Black Mesa x Greenhouse pair.

The current lifecycle also remains `active_candidate=null` and `runtime_test_outstanding=false`.

## C3F16 sufficiency result

No trusted repository-held runtime evidence establishes the exact pair:

- moon: Black Mesa;
- interior: `GreenhouseFlow`.

Accordingly, the static clearance reached by C3F15 cannot yet be promoted to full Phase-C compatibility clearance or to a B3 matrix change.

The result is an **evidence gap**, not a compatibility failure.

## What C3F15 already removes from the future test burden

The future runtime test does not need to rediscover static Count-3 mechanics:

- Black Mesa selection-layer support for Greenhouse is already established;
- exact Black Mesa moon alternate-entrance count/topology is established;
- exact V81 entrance numbering and opposite-side pairing semantics are established by C3F5;
- Greenhouse standard inside fire-exit template semantics are established by C3F7/C3F8;
- DunGen GlobalProp selection/removal and later `SpawnSyncedProps` consumption are established by C3F11/C3F12;
- C3F15 proves a Greenhouse static generated eligible positive ID-1231 lower bound of at least 8, comfortably above Black Mesa's required count of 3.

The remaining obligation is therefore practical runtime qualification of this exact moon/interior pair.

## Smallest future runtime proof contract

A future diagnostic candidate may be justified only after its exact implementation is repository-native reviewed. It must:

1. derive from exact accepted S1.42AK unless a later baseline has become accepted by then;
2. preserve `S1.42ABInteriorWeightNormalization` unchanged;
3. preserve Black Mesa's Dawn/native ownership and avoid duplicate LLL registration;
4. make no unrelated gameplay/config/package changes;
5. deterministically exercise **Black Mesa moon + `GreenhouseFlow`** through an owner-supported selection path rather than relying on random natural selection.

One successful targeted session can satisfy the pair-specific runtime obligation only if its evidence establishes all of the following:

1. **Identity:** the active moon is Black Mesa and the selected/generated dungeon flow is exactly `GreenhouseFlow`.
2. **Generation:** DunGen reaches successful floor generation without exhausted retries, fatal generation errors or an equivalent generation abort.
3. **Entrance cardinality/materialization:** the generated runtime exposes the expected main entrance plus three alternate entrance connections, consistent with Black Mesa's 0..3 topology and the already-proven static pairing model.
4. **Player traversal:** the player successfully enters and leaves the generated Greenhouse interior through the normal gameplay entrance path.
5. **Alternate-exit practicality:** all three Black Mesa alternate entrance pairs are exercised sufficiently to show that none is a stranded/mismatched teleport target. Prefer direct player use of each alternate pair in the same targeted session when practical rather than inferring runtime usability from static numbering alone.
6. **Geometry/traversal:** no severe clipping, inaccessible required doorway geometry or other target-pair traversal blocker is observed at the exercised entrance endpoints.
7. **Routing/NavMesh qualification:** the run must not expose a new severe or persistent route/NavMesh failure attributable to this pairing; available PathfindingLib/navigation signals should show the expected entrance connections. The separate deferred Black Mesa/Pikmin routing-recovery scope is **not** reopened or made a prerequisite here.

If existing runtime instrumentation cannot unambiguously establish items 1-7, the later diagnostic candidate must add only the minimum bounded observation/logging necessary for those facts. Do not broaden it into general Black Mesa/Pikmin repair.

## Decision consequence

- `Black Mesa x GreenhouseFlow` remains `NOT_YET_PROVEN` in the authoritative B3 matrix.
- C3F15's static Count-3/entrance-topology clearance remains valid and is not rolled back.
- No `KNOWN_TECHNICAL_RESTRICTION` is inferred.
- No gameplay candidate exists yet.
- No runtime test is armed yet.
- BuildSpecs/controller state remains unchanged.
- Black Mesa native ownership, S1.42AB normalization and Shatteredrooms exclusions remain unchanged.

## Next bounded checkpoint

Perform **C3F17 — targeted Black Mesa x Greenhouse diagnostic-candidate design**.

First determine the smallest repository-native mechanism that can force exactly this moon/interior pairing while preserving Black Mesa native ownership and the accepted normalizer. Then author and statically validate the candidate contract if the mechanism is safe.

Do not start a gameplay run in the same segment merely because a candidate can be designed.
