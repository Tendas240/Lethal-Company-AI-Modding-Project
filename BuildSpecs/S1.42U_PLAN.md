# S1.42U Plan — BCMER 1.71.0 Reactivation Gate

**Status:** built and repository-verified; runtime validation pending  
**Base:** S1.42T — Normal Enemy Restore (runtime PASS)

Base profile:

`Profiles/LC V1 S1.42T Normal Enemy Restore.r2z`

Base SHA-256:

`a2714d04777edc95490398367c9dad2e320b44b664e20e9fe0b0f85d6a5fea10`

Built output:

`Profiles/LC V1 S1.42U BCMER 1.71.0 Reactivation Gate.r2z`

Output SHA-256:

`ff5fdebf22fefdd5515b95677174290f9666e491447138f074e5b65673173969`

Build verification:

`Current/77_S1.42U_BUILD_VERIFICATION_BCMER_REACTIVATION.md`

GitHub Actions run:

`33818241873` = **success**

## Goal

Restore the final major package-state variable that was intentionally held off during the isolated enemy-regression chain: exact BCMER 1.71.0.

This remains a one-variable attribution gate until runtime acceptance closes it.

## Exact intended change — built as specified

Package:

`SoftDiamond-BrutalCompanyMinusExtraReborn`

Version:

`1.71.0`

State:

`disabled -> enabled`

Do **not** upgrade to 2.0.0 or another version.

Repository build result:

- 331 archive members;
- changed existing member only `export.r2x`;
- no added/removed members;
- no config patch;
- no compatibility-code/plugin build change.

## Preserve exactly

Verified preserved in the built S1.42U candidate:

- S1.42T `Isolated Enemy Regression = false`;
- compatibility plugin v1.3.14;
- compatibility DLL SHA-256 `3fd38c0e8ff76b55c5c335cd9eb867e254a422caea2287fb95d46447e2167960`;
- `Thumper Bite Limit = 3`;
- Crawler absent from LethalMin Attack Blacklist;
- all S1.42T normal spawn-owner configs;
- all accepted S1.42S Baboon-Hawk/Pikmin lifecycle fixes;
- Puffer -> Pikmin protection;
- Pikmin -> Hawk and Pikmin -> Thumper/Crawler intended behavior.

The accepted S1.41 BCMER configuration was carried forward unchanged; no unrelated BCMER config value was rewritten.

## Custom patch delta

None.

## Patch Safety Review

No custom patch is introduced or changed.

The risk is package-state attribution only. Therefore do not combine the runtime gate with any other package/config/gameplay change.

## Forbidden mixed changes

Do not include:

- BCMER EventType probability rebalance;
- interior probability changes;
- CullFactory exceptions;
- Mausoleum fog changes;
- CodeRebirth microwave rarity tuning;
- compatibility code changes;
- repository structural migration.

## Runtime acceptance — still required

1. main menu/startup succeeds;
2. exact BCMER 1.71.0 is loaded/active;
3. normal non-isolated enemies remain available/spawn normally;
4. no crash/freeze;
5. no Work/no-task loop;
6. no Leader-null loop;
7. no new S1.39 Compatibility Fixes error/exception flood;
8. no new BCMER-driven catastrophic system regression;
9. accepted Pikmin interaction behavior remains intact;
10. ingest a fresh full runtime log and compare it against S1.42T.

S1.42T runtime evidence is the immediate BCMER-off comparison baseline:

`RuntimeEvidence/S1.42T/20260903T222109Z/`

A heavy Baboon-Hawk stress retest is not required merely because BCMER was re-enabled, unless runtime evidence gives a reason to reopen it.

## Controllers

`BuildSpecs/current.json` is disarmed after the successful build:

- `enabled = false`
- `build_id = IDLE_AFTER_S1.42U_BUILD_AWAITING_RUNTIME_VALIDATION`

`RuntimeInbox/ACTIVE_BUILD.txt = S1.42U`
