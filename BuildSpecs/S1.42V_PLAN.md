# S1.42V Plan — Post-BCMER Balance Tuning

**Status:** plan only / not armed  
**Base:** S1.42U — BCMER 1.71.0 Reactivation Gate (runtime PASS)

Base profile:

`Profiles/LC V1 S1.42U BCMER 1.71.0 Reactivation Gate.r2z`

Base SHA-256:

`ff5fdebf22fefdd5515b95677174290f9666e491447138f074e5b65673173969`

Runtime acceptance:

`Current/78_S1.42U_RUNTIME_ACCEPTANCE_BCMER_REACTIVATION.md`

Final handover:

`Current/79_FINAL_HANDOVER_S1.42U_PASS_S1.42V_NEXT.md`

## Goal

Apply requested post-restoration balance tuning without regressing the newly accepted full normal stack.

No S1.42V profile exists yet. This plan is **not armed** because the requested Jetpack acceleration change still needs an exact technically correct implementation before the build scope can be frozen.

## Confirmed config change — Immortal Snail

Owner/config:

`BepInEx/config/dev.idjut.SnailFork.cfg`

Current:

`Rarity = 80`

Target:

`Rarity = 40`

Preserve:

`Max Snails = 2`

This halves the configured weighted spawn value. It does not imply that every moon's final absolute probability becomes mathematically exactly 50% after the complete weighted pool is evaluated.

## Proposed config change — CodeRebirth Functional Microwave volume

Owner/config:

`BepInEx/config/CodeRebirth.cfg`

Current:

`Functional Microwave | Volume = 0.7`

The previous reduction is confirmed present in S1.42U, but the user still perceived the Microwave as too loud.

Proposed target:

`Functional Microwave | Volume = 0.5`

Microwave **spawn rarity** is a separate later backlog item. Do not silently mix it into this exact volume change unless the scope is deliberately expanded before arming.

## Jetpack acceleration — implementation gate

User requirement at handover:

**Increase the base Jetpack acceleration modestly as an always-on behavior.**

Do not silently reinterpret this as a purchase-gated ship-upgrade buff.

### ButteRyBalance

Config:

`BepInEx/config/butterystancakes.lethalcompany.butterybalance.cfg`

Current:

`Control Scheme = V49`

This is a broad speed/inertia/handling mode, not a small numeric base-acceleration knob. Do **not** switch V49 -> Vanilla/Dynamic merely to approximate the requested acceleration buff.

### More Ship Upgrades

Config:

`BepInEx/config/com.malco.lethalcompany.moreshipupgrades.cfg`

The purchasable `Jet Fuel` upgrade directly affects Jetpack acceleration:

- `Initial Acceleration Increase = 20`;
- `Incremental Acceleration Increase = 20`.

However these values only apply when Jet Fuel has been purchased. They are not an unconditional base-Jetpack acceleration setting.

### Required technical resolution before build

Before arming S1.42V:

1. identify the exact runtime owner/method/field that controls unconditional base Jetpack acceleration;
2. inspect the relevant code path and interaction with ButteRyBalance `V49` and JetpackFixes;
3. prefer a real existing narrow config/owner if one exists;
4. do not invent a config key;
5. do not use `Jet Fuel 20 -> 25` as a substitute unless the user explicitly changes the requirement to a purchase-gated buff;
6. if project-local code is required, perform a dedicated Patch Safety Review;
7. if practical, isolate the custom Jetpack code change from unrelated Snail/Microwave config tuning so failures remain attributable;
8. freeze the exact final file/value/code delta before `BuildSpecs/current.json` is armed.

## Verified state to preserve

### Moon power/spawn configuration

`ProfileSources/S1.42U/BepInEx/config/LethalLevelLoader.cfg` is byte-identical to the canonical S1.42C restore baseline.

Git blob SHA:

`14dcd076692cbc54e073ad281a63d046b0976e00`

Preserve every per-moon:

- Maximum Inside Enemy Power Count;
- Maximum Outside Daytime Enemy Power Count;
- Maximum Outside Nighttime Enemy Power Count;
- inside/daytime/nighttime enemy spawning list.

Do not wholesale regenerate or normalize the file.

### Spawn-cycle timing

`BepInEx/config/butterystancakes.lethalcompany.spawncyclefixes.cfg`

Preserve:

`Consistent Spawn Times = true`

The S1.42U file is byte-identical to S1.42C. This keeps the standardized first spawn wave at approximately 07:39 and avoids vanilla's possible outside/daytime delay while vents remain occupied.

### Compatibility/full-stack invariants

Preserve exactly:

- exact BCMER 1.71.0 enabled;
- EnemyIsolation off;
- compatibility plugin v1.3.14;
- compatibility DLL SHA-256 `3fd38c0e8ff76b55c5c335cd9eb867e254a422caea2287fb95d46447e2167960`;
- `Thumper Bite Limit = 3`;
- Crawler absent from LethalMin Attack Blacklist;
- Puffer -> Pikmin protection;
- narrow Baboon Hawk -> Pikmin protection with native LethalMin lifecycle preserved;
- normal enemy population restored.

## Patch Safety Review

No custom patch is currently specified because the exact Jetpack owner/implementation is not yet resolved.

If an always-on Jetpack acceleration change requires project-local code, the build plan is incomplete until a dedicated Patch Safety Review identifies:

- exact owner/method/field;
- inheritance/base lifecycle if relevant;
- secondary movement/state responsibilities;
- interaction with ButteRyBalance V49;
- interaction with JetpackFixes;
- max-speed implications;
- explosion/death implications;
- network/client-host implications if any;
- rollback path;
- exact runtime acceptance criteria.

Follow:

`Current/68_PROJECT_LOCAL_PATCH_SAFETY_AND_REGRESSION_POLICY.md`

## Runtime acceptance after S1.42V

At minimum:

1. startup/main menu succeeds;
2. exact BCMER 1.71.0 remains active and completes patching;
3. Compatibility Fixes 1.3.14 loads;
4. normal enemies still spawn;
5. deterministic Snail config is `Rarity = 40` and `Max Snails = 2`;
6. Immortal Snail remains functional;
7. Functional Microwave is audibly lower without functional regression;
8. Jetpack base acceleration is modestly higher as intended;
9. no unintended max-speed change unless explicitly part of the frozen implementation;
10. V49 inertia/handling remains as intended;
11. no new Jetpack explosion/death/JetpackFixes regression;
12. Work/no-task = 0;
13. Leader-null = 0;
14. no new project compatibility error/exception flood;
15. fresh complete runtime log is ingested.

A heavy Baboon-Hawk stress retest is not required unless the implementation touches that compatibility path or new evidence reopens it.

## Later stages — explicitly not part of S1.42V unless re-scoped

Later build IDs are intentionally not assigned yet.

### Environment / Interior tuning

- equal probability for all installed interiors and the same rule for newly added interiors;
- CullFactory exceptions for `junkrooms` and `shatteredrooms`;
- Mausoleum fog reduction;
- CodeRebirth Microwave rarity reduction.

### BCMER EventType balancing

- fixed equal distribution: **8 EventTypes x 12.5%**.

### Final S1.42 full-stack acceptance

- longer normal run after individual tuning stages pass;
- varied enemies, Pikmin lifecycle, BCMER events, interiors, Jetpack and CodeRebirth coverage;
- complete runtime-log ingest and permanent invariant checks;
- promote the final tuned baseline only if clean.

Do not silently mix these later scopes into S1.42V. Each later stage must freeze exact file/value deltas before its own build is armed.
