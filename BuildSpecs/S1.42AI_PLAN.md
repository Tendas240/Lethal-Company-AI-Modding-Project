# S1.42AI — ShyGuy Correction and Diagnostic Isolation Plan

**Current planning status:** SHYGUY_ONLY_DIAGNOSTIC_REVISION_PLANNED / NOT BUILT
**User-directed amendment:** 2026-09-10
**Canonical scope:** `Knowledge/BCMER.md`; lifecycle: `Current/CURRENT_STATE.json`
**Planned diagnostic identity:** `S1.42AI-DIAG1`
**Existing built artifact:** S1.42AI remains unchanged, not accepted; its original build specification and results are retained below.

## Current execution order and identity

Prepare a temporary diagnostic revision of S1.42AI that allows only Shy Guy enemy spawns and only BCMER event `[ShyGuy]`. This is the same BCMER event implicated by `RuntimeEvidence/S1.42AH/20260908T202138Z/`, not `NoShyGuy` or another SCP event.

The diagnostic is derived from the exact existing profile `Profiles/LC V1 S1.42AI ShyGuy Interior Only.r2z`, SHA-256 `d993bc0fca265fe7a2b069bd654b5e2c1f590623eaf7f4fabb325f8b4d863cb2`. Planned output: `Profiles/LC V1 S1.42AI-DIAG1 ShyGuy Isolation.r2z`; Gale name: `LC V1 S1.42AI-DIAG1 ShyGuy Isolation`; readable snapshot: `ProfileSources/S1.42AI-DIAG1/`. These are future output identities, not claims that artifacts exist.

Preserve the original S1.42AI bytes, ProfileSources, SHA, build result and static evidence. Never overwrite them with diagnostic bytes or reuse their uploader attribution. S1.42AH remains accepted. Existing S1.42AI remains the latest built/active unaccepted candidate with its full-normal runtime decision outstanding; preparation of the diagnostic now takes precedence over requesting that run. An already completed S1.42AI test can still be ingested without repetition.

This change authorizes a plan, not a build trigger. Keep `BuildSpecs/current.json` disabled and `RuntimeInbox/ACTIVE_BUILD.txt = S1.42AI` during preparation. Only after a diagnostic build and static verification may a coordinated lifecycle transition update candidate, build result, runtime attribution, readable navigation and the diagnostic-specific upload record.

## Enemy isolation contract

- Only the exact resolved Shy Guy enemy identity may pass enemy-spawn paths. Cover indoor, outdoor and daytime lists, late list repopulation, BCMER direct spawns, native mod spawns and networked spawn paths.
- Do not inherit the old Thumper/Puffer/Baboon Hawk allowlists or the blanket Pikmin/Puffmin/Bulbmin exemption. Inventory EnemyAI-derived entities and distinguish player/ship/item infrastructure without guessing exemptions. Preserve the installed packages and their non-spawn responsibilities.
- Use a temporary diagnostic mode with an exact Shy Guy allowlist, default off outside this profile. Resolve and record the actual EnemyType/prefab/type identity from the current dependency/runtime evidence before coding; do not equate the config key `ShyGuyDef` with an unverified runtime identifier.
- Prevent non-allowlisted spawns at reviewed spawn entry points. A later despawn does not prove that spawning was prevented. Any bypass is a failed isolation gate, with its owner/path recorded; it must not be hidden by cleanup.
- Preserve the Shy Guy event's own interior spawning. Do not fabricate a generic Shy Guy encounter and treat it as proof that BCMER executed the event.
- Crucially, filter by enemy identity, not by location for Shy Guy: if the corrected event unexpectedly creates an exterior Shy Guy, leave that Shy Guy observable and fail the correction gate. Do not erase an exterior Shy Guy, blanket-disable its exterior execution path, or scrub its log marker to make the test pass.
- Do not carry forward the legacy diagnostic's forced outside minimum of one or its injected target pools. Keep normal power/curve settings and all unrelated gameplay packages unchanged unless a separately evidenced diagnostic need is documented.

The S1.42AI correction itself stays intact: all three ShyGuy exterior scales remain `0, 0, 0, 0`; interior scales stay `20, 0.8, 20, 100`, `2, 0.04, 2, 6`, `2, 0.04, 2, 6`; ordinary Scopophobia `SpawnOutside = false` remains unchanged. This is a correction-validation test, not a reintroduction of the old exterior-spawn settings.

## BCMER event isolation contract

Retain exact BCMER 1.71.0. Enumerate every registered built-in, modded and custom event from the candidate; the sole enabled and executable event must be `ShyGuy`.

1. In the diagnostic overlay, set every non-ShyGuy `Event Enabled?` entry in `VanillaEvents.cfg`, `ModdedEvents.cfg` and any loaded custom-event config to false; keep `[ShyGuy] Event Enabled? = true`, `Event Type = VeryBad`, and empty `Events To Spawn With`.
2. In `CoreProperties.cfg`, preserve `Disable all events? = false`, `Chance of Events Occurring = 100, 0.0, 100, 100`, special/beta events off and the randomizer off. Set `Enable Custom Events? = false` for this diagnostic and verify that no already-registered custom event remains executable.
3. In `Difficulty_Settings.cfg`, use `[_Event Settings] Event scale amount = 1, 0, 1, 1` and `Weights for bonus events = 1, 0, 0, 0, 0`: exactly one normal draw and no bonus draws. Preserve `Scale by Heat? = false`, `Force event at max heat? = false` and an empty heat force list.
4. Preserve the eight accepted constant EventType scales and `Use custom weights? = false`. One enabled event changes the effective event distribution intentionally for diagnosis; this is not a replacement for the accepted normal eight-type distribution.
5. Validate normal selection, empty-pool `Nothing` fallback, forced events, forced side events, additional events and runtime/custom registrations. Disabled config entries alone are not sufficient proof: the source has a separate forced execution route. Reject or prevent every non-ShyGuy execution through exact reviewed entry points, without replacing BCMER's entire selection/execution lifecycle.
6. Use an eligible moon/round and positively prove `ShyGuy` selection and execution. Missing Shy Guy, fallback events, rejected eligibility or repeated selection retries invalidate the test; do not report success from an empty round.

## Focused source findings and Patch Safety Review

Reviewed project source at main `5878733dcbb7bd9eb46d77f6a1002de9662b9d42`:
`Patches/S139CompatibilityFixes/Plugin.cs`, plus the S1.42AI profile configs.
The existing `Diagnostics / Isolated Enemy Regression` is false. Its code uses Thumper/Puffer indoor and Baboon Hawk outdoor targets, adds target entries/minimum counts, skips Gordion pool work, and permits Pikmin-family entities in its live allowlist. The defined `RemoveEscapedLiveEnemies` cleanup helper is not evidence of pre-spawn prevention or complete active route coverage. Its three installed hooks reapply pools at `RoundManager.FinishGeneratingNewLevelClientRpc`, `PredictAllOutsideEnemies` and `BeginEnemySpawning`; `Update` does not drive isolation.

Exact BCMER source is the version-pinned authority referenced by `Current/109_BCMER_1_71_0_EVENTTYPE_WEIGHT_PATH_ANALYSIS.md`:
`TheSoftDiamond/BrutalCompanyMinusExtraReborn@e2ca64b9954b7076e75e9a9ff97d76232c4086b0`.
Reviewed `BrutalCompanyMinus/Minus/EventManager.cs` and `MEvent.cs`:
- `ChooseEvents(out List<MEvent>)` removes selected events; `RandomWeightedEvent` returns `Nothing` for an empty pool. The old two-or-more draw settings are unsuitable for a one-event diagnostic.
- Normal additional events are filtered against `disabledEvents`, but the separate `forcedEvents` loop directly calls `Execute()` and also executes forced side events.
- `MEvent.MonsterEvent.Execute()` changes both enemy lists and calls both inside/outside spawn helpers. Therefore list-only filtering does not establish complete spawn isolation.

**Review status: PARTIAL / IMPLEMENTATION GATE OPEN.** These findings establish why simply enabling the old toggle and disabling event configs is insufficient. Before compiling a diagnostic DLL, finish the exact declared method/signature/call-site and inheritance/network lifecycle review under `Current/68_PROJECT_LOCAL_PATCH_SAFETY_AND_REGRESSION_POLICY.md`. Record coverage for vanilla/native/BCMER spawning, event forcing, repeated rounds and host/client behavior; resolve the Shy Guy identity; verify the existing source-to-DLL provenance. Choose the smallest prevention-only targets from that evidence. No guessed broad fallback, global per-frame scan, blanket component/AI lifecycle disable, shared NetworkObject suppression or post-spawn deletion is approved by this plan. Existing native death/despawn/cleanup and accepted compatibility hooks must remain functional.

## Captured identity and spawn review — 2026-09-11

Read `SourceEvidence/ShyGuyIsolation/20260910T205433Z/REVIEW.md` and its hashed capture evidence before further implementation. Exact-package Actions inspection succeeded: the Shy Guy AI type is `ShyGuy.AI.ShyGuyAI`, Scopophobia loads `ShyGuyDef.asset`, BCMER uses the asset-name registry, and direct BCMER/forced-event plus painting routes are confirmed. The old embedded diagnostic still uses the legacy allowlists.

The exact V81 NuGet reference has only `throw null` spawn-method bodies. Native interception review therefore remains blocked on focused installed-game source evidence; do not claim this inspection completed the patch safety gate. Next prepare that capture through the repository's existing provenance-verified helper pattern and inventory remaining native-mod spawn owners. No diagnostic build or controller transition is authorized by this evidence checkpoint.

## Build, evidence and restoration gates

- Build repository-natively from the exact S1.42AI base with `overwrite = false`. Use the separately named diagnostic output.
- Expected change categories: Gale identity; the diagnostic config and narrowly reviewed local plugin DLL if needed; BCMER event-enable entries; the explicit custom-event and event-count diagnostic settings above. Record the final exact file/config delta and source/DLL hashes before declaring build-safe. No package additions/removals/disables or unrelated config edits.
- Static gates must prove a single-event inventory, corrected Shy Guy interior/exterior values, exact package versions, declared patch-target coverage, default-off restoration behavior, unchanged baseline files and no hidden legacy allowlist exceptions.
- Runtime gates must positively prove the Shy Guy BCMER event executed and produced its intended interior encounter, zero non-ShyGuy spawn/execution bypasses, no other BCMER event execution, no exterior Shy Guy from the event, and no new errors/retry floods. Repeat on the same moon and after lobby restart; include host/client verification for any network interception.
- Preserve full fresh diagnostic `LogOutput.log` with build-specific attribution. Do not issue the S1.42AI uploader for S1.42AI-DIAG1. When the diagnostic is actually ready, provide both the canonical Gale replacement command and its exact self-contained diagnostic uploader in the same response.
- After diagnosis, remove only the diagnostic overlay and restore normal enemy/event selection from the exact full-normal S1.42AI parent, retaining its correction and inherited S1.42AH changes. The old S1.42C restore authority describes the normal baseline contract; it is not permission to roll back later accepted changes.
- Diagnostic success is diagnostic evidence only. The normal-stack acceptance gate in Current/143 remains required before promoting the correction. Preserve failure evidence and never accept from build/startup success.

---

## Original S1.42AI build specification and result — retained provenance

The following records the already-built full-normal artifact. Its old trigger instructions are historical and must not be re-run or used as the diagnostic build request.


**Status:** BUILT / STATIC-VERIFIED / ACTIVE RUNTIME CANDIDATE / NOT ACCEPTED
**Parent:** accepted S1.42AH — Mouth Dog Pikmin Dual Prevention  
**Parent profile:** `Profiles/LC V1 S1.42AH Mouth Dog Fix.r2z`  
**Parent SHA-256:** `06e07fe6805e5e41786c16b5c1ea2132c4f65b385517c902f8aa566ccf49cd4e`  
**Scope authority:** `Knowledge/BCMER.md`  
**Deferred contract:** `BuildSpecs/DEFERRED_BCMER_SHYGUY_INTERIOR_ONLY_PLAN.md`

## Controller invariant

`BuildSpecs/current.json` must remain disabled between atomic build operations. Therefore this pre-build PR records and validates the exact successor plan while leaving the live controller in its current idle S1.42AH-guard state. The actual `enabled = true` transition is an atomic build trigger and must occur only after this preparation has passed repository validation.

## Scope boundary

This successor is a config-only BCMER correction derived directly from the accepted S1.42AH profile. It must not include LC Office, CullFactory exceptions, MelanieMausoleum fog, Black Mesa/Pikmin routing, LethalEscape, or any other independent deferred scope.

The only intended gameplay/config delta is within:

`BepInEx/config/BrutalCompanyMinusExtraReborn/ModdedEvents.cfg`

section `[ShyGuy]`:

```text
ShyGuyDef OutsideEnemyRarity = 0, 0, 0, 0
ShyGuyDef MinOutsideEnemy = 0, 0, 0, 0
ShyGuyDef MaxOutsideEnemy = 0, 0, 0, 0
```

## Required preserved values

The build must preserve:

```text
[ShyGuy]
Event Type = VeryBad
Event Enabled? = true
ShyGuyDef InsideEnemyRarity = 20, 0.8, 20, 100
ShyGuyDef MinInsideEnemy = 2, 0.04, 2, 6
ShyGuyDef MaxInsideEnemy = 2, 0.04, 2, 6
```

Scopophobia v1.3.4 must remain unchanged and retain ordinary `SpawnOutside = false`; the build must not introduce any `SpawnOutside = true` value in `BepInEx/config/Scopophobia.cfg`.

## Prepared atomic build request

When the atomic build operation is executed, `BuildSpecs/current.json` must use:

- `enabled = true`;
- `build_id = S1.42AI`;
- base profile `Profiles/LC V1 S1.42AH Mouth Dog Fix.r2z`;
- base SHA-256 `06e07fe6805e5e41786c16b5c1ea2132c4f65b385517c902f8aa566ccf49cd4e`;
- output `Profiles/LC V1 S1.42AI ShyGuy Interior Only.r2z`;
- Gale profile name `LC V1 S1.42AI ShyGuy Interior Only`;
- `overwrite = false`;
- no mod state changes, additions, removals, file injections or local plugin builds;
- exactly three config patches, all targeting `[ShyGuy]` in `BepInEx/config/BrutalCompanyMinusExtraReborn/ModdedEvents.cfg`, setting `ShyGuyDef OutsideEnemyRarity`, `ShyGuyDef MinOutsideEnemy`, and `ShyGuyDef MaxOutsideEnemy` to `0, 0, 0, 0`.

The atomic request must include build assertions proving the complete `[ShyGuy]` contract above after patching, confirming Scopophobia v1.3.4, confirming at least one `SpawnOutside = false`, and forbidding any `SpawnOutside = true` in `BepInEx/config/Scopophobia.cfg`.

## Expected archive delta

Because the builder updates the Gale profile name, the expected changed existing archive members versus S1.42AH are exactly:

1. `export.r2x` — profile-name metadata only;
2. `BepInEx/config/BrutalCompanyMinusExtraReborn/ModdedEvents.cfg` — the three `[ShyGuy]` exterior values above only.

Expected added archive members: none.  
Expected removed archive members: none.  
Expected package enable/disable/add/remove changes: none.  
Expected local plugin/DLL changes: none.

After the build, `ProfileSources/S1.42AI/` and the generated archive must be compared against S1.42AH. The candidate must not be exposed for runtime testing until that static delta is verified and lifecycle state is explicitly prepared.

## Static build result

- Built profile: `Profiles/LC V1 S1.42AI ShyGuy Interior Only.r2z`
- SHA-256: `d993bc0fca265fe7a2b069bd654b5e2c1f590623eaf7f4fabb325f8b4d863cb2`
- Build checkpoint commit: `2dea753ec48ea2a8f417491ae9a13cf7a6d7b8b9`
- Build workflow run: `34496960816`
- Static evidence: `BuildSpecs/S1.42AI_BUILD_EVIDENCE/STATIC_VERIFICATION.md`
- Changed existing archive members: exactly `export.r2x` and `BepInEx/config/BrutalCompanyMinusExtraReborn/ModdedEvents.cfg`.
- Added/removed members: none.
- Runtime status: active candidate; explicit gameplay validation is still required before acceptance.
