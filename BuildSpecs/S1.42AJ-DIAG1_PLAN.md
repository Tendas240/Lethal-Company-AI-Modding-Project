# S1.42AJ-DIAG1 — LC Office diagnostic selection

**Status:** IMPLEMENTATION PROPOSAL / NOT ARMED / NOT ACCEPTED
**Parent:** exact balanced S1.42AJ, SHA-256 `7c1441aeb0732208bb8e910d89348c2e0129ce202422103a025e8f8aea707dba`
**Lifecycle authority:** `Current/CURRENT_STATE.json`
**Scope authority:** `Knowledge/INTERIORS_AND_LLL.md`
**Patch policy:** `Current/68_PROJECT_LOCAL_PATCH_SAFETY_AND_REGRESSION_POLICY.md`

## Purpose and immutable boundary

Obtain only the missing LC Office generation/traversal/elevator/power/enemy-navigation/scrap coverage. S1.42AI remains accepted; balanced S1.42AJ remains unchanged and not accepted. DIAG1 is never an accepted baseline or a parent for a balanced gameplay build.

The normal S1.42AJ evidence remains `RuntimeEvidence/S1.42AJ/20260917T171109Z/`: OfficeDungeonFlow registration, Offense author rarity 65, normalized rarity 100. Diagnostic singleton selection is not proof of default viability or balanced probability.

## Exact source verification

GitHub Actions runs 35261013411 and 35261242818 materialized IAmBatby-LethalLevelLoader 1.7.12 from the hash-guarded AJ export. Persisted excerpts and package/DLL/source hashes: `SourceEvidence/S1.42AJ-DIAG1/`. Full decompile remains in the corresponding workflow artifacts.

Verified source contracts:

- `DungeonManager.GetValidExtendedDungeonFlows(ExtendedLevel, bool)` returns a new `List<ExtendedDungeonFlowWithRarity>`; every call creates fresh weighted wrappers. Native matching runs first and returns nonzero viable entries.
- `ExtendedDungeonFlowWithRarity.extendedDungeonFlow` and `rarity` are declared instance fields. `ExtendedDungeonFlow.DungeonName` and `DungeonFlow` are declared readable properties; the flow asset name is validated as `OfficeDungeonFlow`, as observed in AJ runtime.
- `LethalLevelLoaderNetworkManager.GetRandomExtendedDungeonFlowServerRpc()` calls with true, converts returned flow assets and rarities into arrays, and calls the native client RPC. The existing RPC/selection mechanism remains owner.
- `TerminalManager.GetSimulationResultsText(ExtendedLevel)` also calls with true. Therefore true alone is NOT a sufficient selection guard.
- `LevelManager` day-history creation calls with false and must receive the complete normal pool.

Captured package hash is a first observation of the pinned package, not independent proof of the user's installed DLL. Runtime arming checks the actual loaded LLL DLL against that hash and refuses any mismatch.

## Patch Safety Review

Exact and sole patch target: static declared `LethalLevelLoader.DungeonManager.GetValidExtendedDungeonFlows(ExtendedLevel, bool)`, one Harmony postfix. No prefix, transpiler, RPC patch or fallback target.

The target is a static query, with no inherited MonoBehaviour/network lifecycle to disable. Its responsibilities are fresh viable-pool construction, native matching, optional logging/sorting and stopwatch bookkeeping. All original responsibilities complete before the postfix. The downstream network owner retains synchronization and selection; the diagnostic changes only this invocation's returned list.

Arming validates loaded dependency versions and DLL hashes, exact declared target signature/return/body, weighted fields, readable name/asset/moon properties, the exact declared selection and simulation caller signatures/bodies, and the installed accepted normalizer owner. Order is declared with HarmonyAfter normalizer plus Priority.Last. The installed postfix owners are logged.

For Offense and true only, the managed call stack must identify the exact unmodified selection MethodInfo. Terminal simulation is explicitly excluded. No guessed method-name/dynamic-method fallback is allowed. If runtime rewriting/inlining prevents exact caller identification, the diagnostic refuses; this uncertainty must be closed by runtime evidence, not by broadening the patch.

All pool/identity validation precedes mutation. Exactly one entry named LC Office must already exist; its asset must be OfficeDungeonFlow and its already-normalized rarity must be 100. Only then clear the freshly allocated result list and re-add that same existing wrapper. No flow/wrapper is created, no global registration/list/config is changed. Unknown caller, missing/duplicate flow, mismatched asset or unnormalized rarity refuses without deliberate pool mutation and records one blocking error.

Adjacent behavior: terminal simulation remains normal, other moons remain normal, false/day-history calls remain normal, repeated invocations must rebuild the full upstream pool, native RPC ownership and downstream generation remain intact.

Forbidden alternatives: global LLL state, new registration, moon availability overrides, RNG manipulation, whole-component/lifecycle suppression, normalizer modification, package/config drift, deprecated LLL fork, deferred Interior changes.

## Buildspec and static gates

`BuildSpecs/S1.42AJ-DIAG1.json` is a separately named request, not the active controller. The proposal leaves `BuildSpecs/current.json`, `RuntimeInbox/ACTIVE_BUILD.txt`, `Current/AUTO_BUILD_RESULT.*` and machine lifecycle unchanged.

The PR workflow calls the existing repository profile builder with the separate request and build-specific result paths. It produces a review artifact only; it never publishes/arms/promotes a runtime.

Required before runtime:
1. Compile source using the repository's pinned BepInEx/Harmony/game references.
2. Execute selection-boundary negative tests (other moon, false, simulation/nonselection caller, missing/duplicate/null entries, wrong asset, 65/0 rarity, repeat/input preservation).
3. Verify exact AJ profile hash.
4. Verify archive delta is exactly export profile identity plus one new DIAG1 DLL; all other members and package blocks unchanged.
5. Verify accepted normalizer DLL byte/hash identity and compiled/injected DIAG1 byte identity.
6. Record output and DIAG1 DLL hashes in build-specific static evidence.
7. Review source/provenance and permanent repository CI.
8. Publish the validated diagnostic and perform a separate atomic lifecycle/controller preparation before requesting runtime.

Static success is build evidence only. Actual loaded startup/order/caller markers cannot be proven by compilation.

## Runtime gate after separate arming

The same ready-to-test response must include canonical Gale replacement/import and exact build-specific log uploader. No runtime is requested by this implementation proposal.

Require `[AJDIAG1] ARMED`, no REFUSED marker, LLL normal viable Office(65), accepted normalizer Office(100), then `[AJDIAG1] SELECTED` with pool 41->1 (or the actual normal viable count). Verify only Office is selected, generation and traversal, elevator, breaker/power where available, normal scrap and enemy navigation, inherited accepted contracts and no new critical flood. Verify later false/history queries still contain the full normal pool, terminal simulation remains normal where exercised, and repeat selection across another landing/lobby where practical.

A refused caller/identity/weight contract invalidates diagnostic coverage. Do not relabel an ordinary Facility roll as a successful DIAG1 run.

Do not accept or merge diagnostic force-selection into balanced S1.42AJ. Roll back by using the exact unchanged S1.42AJ profile through the canonical Gale workflow.
