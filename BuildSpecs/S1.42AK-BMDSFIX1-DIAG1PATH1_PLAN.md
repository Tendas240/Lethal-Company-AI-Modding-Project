# S1.42AK-BMDSFIX1-DIAG1PATH1 — Short Local Profile Identity Repair

**Stage:** INACTIVE REVIEW BUILD  
**Status:** REVIEW BUILD PASS / ACTIONS ARTIFACT ONLY / NOT PUBLISHED / NOT ARMED  
**Classification:** DIAGNOSTIC SUPPORT ONLY / NEVER ACCEPT  
**Parent diagnostic bytes:** exact published `S1.42AK-BMDSFIX1-DIAG1`

## Problem

The published DIAG1 archive itself materializes correctly, but its local Gale profile identity is long enough that two runtime-consumed nested DLL paths reach 260 and 262 characters on the observed Windows installation root. BepInEx/Mono then fails in the preloader before DIAG1 can arm.

This is a local path-length launch blocker, not diagnostic gameplay evidence and not a BMDSFIX1 rejection.

## Exact repair surface

DIAG1PATH1 is an identity-only rewrap of the exact published DIAG1 profile.

- Base profile: `Profiles/LC V1 S1.42AK-BMDSFIX1-DIAG1 Deterministic Deep Sewers Selector.r2z`
- Base SHA-256: `31c24a3752aefe040b74c5dc2c3b7f677c17068a91f8c8e2893ace615050b78e`
- New local/profile identity: `LC V1 S1.42AK-D1P1`
- Review output: `Profiles/LC V1 S1.42AK-D1P1.r2z`

The build recipe performs no plugin build, no file injection, no package change, no config change and no mod-state change. `BuildSystem/profile_builder.py` changes only the single `profileName:` field in `export.r2x`.

## Byte-preservation contract

The inactive review build proves:

1. the exact DIAG1 base profile SHA matches;
2. the archive member set and count remain unchanged at `338`;
3. `export.r2x` is the only changed archive member;
4. `S142AKBMDSFix1Diag1.dll` remains byte-identical with SHA-256 `3b9954b21fc2f1214e73b4021c8ab278f71420583e0e2e9f478f981fc64b20e1`;
5. `S142AKBMDSFix1.dll` remains byte-identical with SHA-256 `f337da49f4a0e75bf2753e17e3abc52cdbea56ba37eddec5f1065b17f4d75a92`;
6. `S142ABInteriorWeightNormalization.dll` remains byte-identical with SHA-256 `901c02a8e85d33af24d0aa906faa6052a7de33faa7dfbeeca590bbd8a8f59a06`;
7. package/config/gameplay deltas remain zero;
8. no local plugin build occurs.

Exact reviewed output identity is recorded in the separately versioned inactive review-build checkpoint and its Actions artifact. The review `.r2z` remains ephemeral and is not committed by this gate.

## Path budget

For the observed Gale profile root on the test machine, the two startup paths that failed under the original long profile identity are 260 and 262 characters. With `LC V1 S1.42AK-D1P1`, the same paths are 215 and 217 characters, leaving at least 43 characters of margin below the classic 260-character boundary.

The short identity is intentionally terse. Descriptive provenance belongs in repository records, not in a runtime directory name that is concatenated with deeply nested package paths.

## Controller boundary

The review-build stage does not change live controllers. The repository still routes runtime attribution to the blocked long-name DIAG1 until a separately published/indexed DIAG1PATH1 successor is explicitly lifecycle-activated. The blocked long-name DIAG1 profile must not be run again.

Accepted baseline remains S1.42AK. Active gameplay candidate remains exact S1.42AK-BMDSFIX1 / not accepted. The regular Black Mesa x `DeepSewersFlow` BMDSFIX1 qualification gate remains outstanding and unwaived.

No acceptance inference is permitted from the preloader failures or from this review build.

## Next gate

Only after the final review-build head is CI-green may a separate exact-byte publication checkpoint materialize the exact reviewed `LC V1 S1.42AK-D1P1` artifact bytes and readable snapshot into the repository.

Publication/profile-index reconciliation and later runtime activation remain separate gates. No Gale import or runtime execution is authorized by this plan stage.
