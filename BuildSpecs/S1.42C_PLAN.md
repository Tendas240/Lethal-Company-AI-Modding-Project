# S1.42C Build Plan — Pikmin Enemy Interaction Guard

Status: **isolated build requested**

Base:
`Profiles/LC V1 S1.42B LMDL NRE Guard.r2z`

Base SHA-256:
`8523754926e3f67c0ccef5aee976cbe72ab976f997876c59b51fedcfb293befe`

Target:
`Profiles/LC V1 S1.42C Pikmin Enemy Guard.r2z`

## Purpose

Keep the confirmed S1.42B LethalModDataLib fix and isolate two newly observed Pikmin interaction problems.

### Thumper / Crawler

Runtime evidence shows a Crawler spawn followed by a Pikmin entering LethalMin's enemy-grab state and then producing repeated `Leader is null when following` errors.

Required behavior: Thumper and Pikmin must not interact in either direction.

Config changes:
- `[Enemy Behavior] Thumper Bite Limit = 0`
- add `Crawler` to `[Pikmin Behavior] Attack Blacklist`

### Puffer

Current config already has:
`Puffer Can Poison Pikmin = false`

But LethalMin still logs:
`Injected effect trigger to Puffer's Smoke prefab`

and Pikmin are visibly affected by Puffer attacks/smoke.

Compatibility plugin v1.2.0 adds a targeted Puffer smoke guard:
- patches `PufferAI.Start` after other patches;
- removes LethalMin-owned effect/trigger/hazard components from Puffer smoke objects only;
- does not alter vanilla Puffer/player behavior.

Puffer remains attackable by Pikmin because the user did not request bidirectional Puffer isolation.

## Isolation

Do not yet apply:
- BCMER equal 12.5% EventType scales;
- global interior Weight 100 normalization;
- CullFactory junkrooms/shatteredrooms exceptions;
- Mausoleum fog reduction.

## Expected archive delta

Versus S1.42B only:
- `export.r2x`
- `BepInEx/config/NoteBoxz.LethalMin.cfg`
- `BepInEx/plugins/Tendas-S139CompatibilityFixes/S139CompatibilityFixes.dll`

No added/removed members.

## Runtime validation

1. Import with Gale **Advanced options -> Import all files**.
2. Host/load and generate a dungeon.
3. Deliberately bring Pikmin near a Thumper:
   - no Thumper grab/bite on Pikmin;
   - Pikmin do not attack/latch Thumper;
   - no Thumper-related leader-null spam.
4. Bring Pikmin into/near Puffer smoke:
   - smoke/attack does not affect Pikmin;
   - players/Puffer otherwise behave normally.
5. Confirm LMDL remains healthy.

Expected log:
- `[PufferPikminGuard] Removed`
- `[LMDLGuard] Safe ModDataAttribute scan completed:`
- `ModDataHandler initialised!`

Return full `LogOutput.log`.
