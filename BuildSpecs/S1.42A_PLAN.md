# S1.42A Build Plan — Interior Config Seed

Status: **fresh-audited 2026-09-03; build requested through repository-first automation.**

Base profile:
`Profiles/LC V1 S1.41 BCMER Reactivation.r2z`

Base SHA-256:
`d69d0b59144002c24cfedf041ca5cbb70086e9218692aa3ac9359170f338cb2b`

Target:
`LC V1 S1.42A Interior Config Seed`

## Purpose

This is a config/registration seed build, not the final tuned interior build.

Add the binding interior packages and only the hard dependency that is not already present. Do not pre-write guessed interior IDs, CullFactory IDs, weight normalization, or deep balancing.

## Fresh-audited package additions

1. `Beaniebe-Liminal_House 1.1.6`
2. `MelanieMelicious-Melanie_Interiors_MelanieMelicious 1.2.1`
3. `Beaniebe-Deepcore_Mines 1.0.9`
4. `MrKixcat-Junkrooms 4.0.2`
5. `Beaniebe-Super_Market 1.0.3`
6. `MrKixcat-Shatteredrooms 2.1.6`
7. `BLB_Thunderstore_Mods_LOL-Lead_Interiors 0.0.7`
8. `DemonMae-Dungeons_Ultimately_Lacking_Liveliness 1.8.8`
9. `MaxWasUnavailable-LethalModDataLib 1.2.2` — hard dependency of DULL 1.8.8; isolated regression surface

Do not add `Beaniebe-Beanies_Interiors 1.0.6`; the accepted S1.41 base already contains standalone `Beaniebe-Storage_Complex 1.2.5`.

## Dependency audit result

The accepted S1.41 base already contains dependency infrastructure at versions satisfying the selected packages, including:
- BepInExPack 5.4.2305
- LethalLevelLoader 1.7.12
- JLL 1.10.1
- DungeonGenerationPlus 1.5.0
- Beanie Lib 1.0.9
- itolib 0.9.3
- WaterAssetRestorer 1.0.1
- LethalLib 1.2.0
- HookGenPatcher 0.0.5
- AutoHookGenPatcher 1.1.1
- CullFactory 2.0.7
- LethalSponge 1.4.3

DULL 1.8.8 explicitly requires LethalModDataLib 1.2.2, which is absent from S1.41 and therefore must be added in S1.42A.

Lead Interiors 0.0.7 does **not** list Boom_Scraps among its seven hard dependencies. Its README says some functionality is unavailable without Boom_Scraps. Therefore Boom_Scraps is intentionally **not** added to this seed build; actual runtime behavior is to be evaluated before any deliberate optional integration.

## Risk / no-speculation rules

- Do not add guessed CullFactory identifiers in S1.42A.
- Junkrooms has a known CullFactory exception requirement; determine/confirm the runtime identifier before S1.42 tuning.
- Shatteredrooms has a known CullFactory concern and author moon restrictions; preserve those restrictions and do not force it onto Experimentation/Embrion.
- LethalModDataLib is reintroduced only because DULL requires it. Treat save/mod-data/netcode behavior as a regression surface.
- Do not add Boom_Scraps merely to silence the Lead Interiors README note.
- Do not normalize weights until the generated runtime configs and registered IDs are collected.
- Do not duplicate-register Black Mesa or existing standalone interiors.

## Build workflow

1. Build from exact accepted S1.41 through `.github/workflows/profile-build.yml`.
2. Builder self-test must pass.
3. Verify `Current/AUTO_BUILD_RESULT.json` and `Current/AUTO_BUILD_RESULT.md`.
4. Verify generated `ProfileSources/S1.42A/`, exact profile name, archive delta, package count and output SHA-256.
5. Only after the candidate exists, change `RuntimeInbox/ACTIVE_BUILD.txt` to `S1.42A`.

## Runtime seed procedure

Import using Gale:

**Advanced options -> Import all files**

Then:
1. reach Main Menu;
2. host/load a save;
3. land on at least one normal moon;
4. allow a dungeon to actually generate;
5. exit game.

Upload to `RuntimeInbox/Current/`:
- complete `BepInEx/config/` directory as ZIP;
- full `LogOutput.log`.

Only after that evidence exists should S1.42 deep tuning begin.

## Builder readiness

S1.42A uses `mod_additions`.

`BuildSystem/test_profile_builder.py` verifies package-addition serialization against the accepted S1.41 Gale export. The profile-build workflow runs this self-test before every build. Do not bypass it.
