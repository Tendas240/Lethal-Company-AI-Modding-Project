# S1.42R Plan — LethalMin Latched Dead-Target Completion

Base:

`Profiles/LC V1 S1.42Q LethalMin Native Minimal Rollback.r2z`

Base SHA-256:

`50a8488a7d5f5c0a318db2557895d7029de3cfa1c0d704498bb9d90eaa481cb1`

Runtime evidence:

`RuntimeEvidence/S1.42Q/20260903T195158Z/`

Log SHA-256:

`e8949f87c0df2e3f5a8e7b985bf698aab9de68bba08ae45e3fe5b89e89f27aa5`

Root-cause analysis:

`Current/62_S1.42Q_RUNTIME_LATCHED_COATTACKER_ROOT_CAUSE.md`

Exact upstream decompile:

`Current/61_LETHALMIN_1.1.108_ATTACK_TASK_DECOMPILE.txt`

## Scope

Compatibility plugin:
**v1.3.13**

Add one exact generic LethalMin patch:

`LethalMin.Pikmin.AttackEnemyTask.IntervaledUpdate()`

When:
- `IsPikminOnEnemy == true`;
- this task's own `enemy` exists;
- `enemy.enemyScript.isEnemyDead == true`;

then:
- invoke the exact native `PikminAI.FinishTaskServerRpc()`;
- skip the broken upstream interval for that tick.

## Explicitly forbidden

Do not:
- hook BaboonBirdAI.KillEnemy;
- scan RoundManager.SpawnedEnemies;
- use a proximity radius;
- guess target identity;
- call RemoveCurrentTask directly;
- manually unlatch;
- manually restore leader/follow state;
- add corpse carry logic.

## Kept unchanged

- EnemyIsolation = enabled
- BCMER 1.71.0 = disabled
- Thumper Bite Limit = 3
- one-way Enemy -> Pikmin protections
- Puffer protection
- CodeRebirth utility-kill protection
- Dead Baboon Hawk corpse guard
- all package versions/states

## Runtime acceptance

1. Use multiple Pikmin on one Baboon Hawk.
2. Kill the Hawk.
3. Every attacker must stop hitting the dead Hawk.
4. Each latched co-attacker should emit `[LethalMinLatchedDeathGuard]`.
5. Each affected Pikmin should then emit native `Task finished`.
6. No attacker remains stale.
7. Exact follower count is fully recoverable.
8. Repeat on second Hawk.
9. Dead Hawk corpse remains naturally carryable to Onion.
10. Living Hawk ignores corpse.
11. Enemy -> Pikmin protections remain intact.
12. No `Work state with no task assigned!`.
13. No `Leader is null when following`.
