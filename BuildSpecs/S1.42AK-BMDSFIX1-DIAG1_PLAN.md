# S1.42AK-BMDSFIX1-DIAG1 — Deterministic Deep Sewers Selector

**Stage:** INACTIVE REVIEW BUILD  
**Status:** REVIEW BUILD PASS / NOT PUBLISHED / NOT ARMED  
**Diagnostic classification:** DIAGNOSTIC ONLY / NEVER ACCEPT  
**Parent gameplay candidate:** S1.42AK-BMDSFIX1 — ACTIVE_RUNTIME_CANDIDATE_NOT_ACCEPTED

## Fixed parent bytes

- Parent profile: `Profiles/LC V1 S1.42AK-BMDSFIX1 Black Mesa Deep Sewers Size Fix.r2z`
- Parent profile SHA-256: `3f9c7fd5c21c532528db1ddae36764ada73236b7527c6ab2ae1b982c3976b7b0`
- Existing BMDSFIX1 DLL SHA-256: `f337da49f4a0e75bf2753e17e3abc52cdbea56ba37eddec5f1065b17f4d75a92`
- Accepted baseline remains: `S1.42AK` / `b39aa550a517ec727de6eb1ae825383933047d3c556cb6e8d4aa7611c9f89dee`

The parent profile and existing BMDSFIX1 DLL are immutable inputs. DIAG1 must not alter either.

## Purpose

Black Mesa has repeatedly exposed Deep Sewers as a viable normalized-rarity-100 entry without necessarily selecting it. DIAG1 is a deterministic selector used only to answer whether the unchanged BMDSFIX1 clamp can carry Black Mesa x `DeepSewersFlow` through generation and normal landed gameplay.

DIAG1 does not replace the regular exact-byte BMDSFIX1 qualification contract in `Current/175_S1.42AK_BMDSFIX1_PARTIAL_RUNTIME_EVIDENCE_NON_TARGET_CONTROL.md`.

## Exact source contract

The diagnostic contains exactly one Harmony postfix on:

`LethalLevelLoader.DungeonManager.GetValidExtendedDungeonFlows(ExtendedLevel, bool)`

It is ordered after `tendas.lethalcompany.s142abinteriorweightnormalization` with `Priority.Last`. It may mutate only the fresh returned viable/normalized list, and only by retaining the same already-existing Deep Sewers wrapper as a singleton.

Required selector guards:

1. LethalLevelLoader version exactly `1.7.12`.
2. LethalLevelLoader DLL SHA-256 exactly `b95aad3813dd7dc1d50aa29c9606660022b149790905a1589180e19d7c157c8c`.
3. Accepted S1.42AB normalizer version `1.0.0` and SHA-256 `901c02a8e85d33af24d0aa906faa6052a7de33faa7dfbeeca590bbd8a8f59a06` exactly present.
4. Exactly one normalizer postfix already installed on the exact selection target.
5. Managed call stack contains `LethalLevelLoaderNetworkManager.GetRandomExtendedDungeonFlowServerRpc`.
6. Managed call stack containing `TerminalManager.GetSimulationResultsText` is excluded.
7. `NumberlessPlanetName == "Black Mesa"`.
8. Return object has exact `List<ExtendedDungeonFlowWithRarity>` runtime type.
9. No returned wrapper is null.
10. Exactly one returned `ExtendedDungeonFlow` has canonical `DungeonName == "Deep Sewers"`.
11. That wrapper's `extendedDungeonFlow` is non-null and is the same object retained by the diagnostic.
12. Its `DungeonFlow` asset name is exactly `DeepSewersFlow`.
13. Its final normalized rarity is exactly `100`.

Only after all checks pass may the local list change from `N -> 1`.

## Forbidden surfaces

No EntranceTeleport/teleport-ID patching, NavMesh mutation, global RNG hook, RPC replacement, flow registration, ownership change, rarity recalculation, global LLL-list mutation, BMDSFIX1 size-logic duplication, S1.42AB changes, or unrelated package/config/profile changes.

## Source/static deliverables

- `Patches/S142AKBMDSFix1Diag1/Plugin.cs`
- `Patches/S142AKBMDSFix1Diag1/SelectionPolicy.cs`
- pure fail-closed policy tests
- source project and deterministic dependency feeds
- `Patches/S142AKBMDSFix1Diag1/PATCH_SAFETY_REVIEW.md`
- `AnalysisTools/validate_s142ak_bmdsfix1_diag1_source.py`
- `.github/workflows/s142ak-bmdsfix1-diag1-source-static.yml`

Source/static completion authority: `Current/176_S1.42AK_BMDSFIX1_DIAG1_SOURCE_PURE_STATIC_IMPLEMENTATION.md`.

## Inactive review-build contract

The separately versioned review recipe is:

`BuildSpecs/S1.42AK-BMDSFIX1-DIAG1.json`

It uses the exact BMDSFIX1 parent profile and performs exactly one local plugin build/injection:

`BepInEx/plugins/S142AKBMDSFix1Diag1/S142AKBMDSFix1Diag1.dll`

The expected review output identity is:

`Profiles/LC V1 S1.42AK-BMDSFIX1-DIAG1 Deterministic Deep Sewers Selector.r2z`

The inactive review gate is:

- `.github/workflows/s142ak-bmdsfix1-diag1-build-static.yml`
- `AnalysisTools/validate_s142ak_bmdsfix1_diag1_build.py`

The gate must prove:

- exact parent profile SHA remains the BMDSFIX1 candidate SHA;
- exactly one archive member is added: the DIAG1 DLL;
- the only changed existing member is `export.r2x`, limited to profile identity metadata;
- the existing BMDSFIX1 DLL is byte-identical and keeps SHA-256 `f337da49f4a0e75bf2753e17e3abc52cdbea56ba37eddec5f1065b17f4d75a92`;
- the accepted S1.42AB normalizer is byte-identical and keeps SHA-256 `901c02a8e85d33af24d0aa906faa6052a7de33faa7dfbeeca590bbd8a8f59a06`;
- package changes = 0 and config changes = 0;
- the generated `.r2z` remains an ephemeral GitHub Actions review artifact and is not committed/published by this gate.

Exact reviewed hashes/run metadata are recorded separately in `BuildSpecs/S1.42AK-BMDSFIX1-DIAG1_BUILD_EVIDENCE/` and `Current/177_S1.42AK_BMDSFIX1_DIAG1_REVIEW_BUILD_CHECKPOINT.md`.

## Controller boundary

The review-build stage leaves all live/build controllers exactly unchanged:

- `BuildSpecs/current.json`: disabled, `IDLE_UNIVERSAL_INTERIOR_VIABILITY_ANALYSIS`, base profile still exact S1.42AK-BMDSFIX1.
- `RuntimeInbox/ACTIVE_BUILD.txt`: `S1.42AK-BMDSFIX1`.
- `Current/CURRENT_STATE.json`: S1.42AK remains accepted; S1.42AK-BMDSFIX1 remains active/not accepted; `runtime_test_outstanding=true`.
- no DIAG1 profile is repository-published;
- no Gale activation or runtime execution is authorized.

## Acceptance boundary

DIAG1 remains **DIAGNOSTIC ONLY / NEVER ACCEPT**. A later successful DIAG1 runtime may be supporting deterministic target evidence only; it does not become regular exact-byte BMDSFIX1 qualification under `Current/175`.

## Next gate, only after review-build completion

A later, separately segmented step may publish the **exact reviewed DIAG1 bytes** and reconcile the diagnostic lifecycle for runtime preparation. Publication, Gale activation and runtime execution are not part of this review-build stage.
