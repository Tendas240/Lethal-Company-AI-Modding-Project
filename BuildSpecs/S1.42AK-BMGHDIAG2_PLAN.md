# S1.42AK-BMGHDIAG2 — Black Mesa x Greenhouse Diagnostic Startup-Provenance Repair Plan

**Status:** SOURCE IMPLEMENTED / SOURCE STATIC CI PENDING / NOT BUILT / NOT PUBLISHED / NOT ARMED / NOT ACCEPTED  
**Date:** 2026-09-23  
**Parent:** exact accepted S1.42AK — `Profiles/LC V1 S1.42AK LC Office Camera Enemy Balance.r2z`  
**Parent SHA-256:** `b39aa550a517ec727de6eb1ae825383933047d3c556cb6e8d4aa7611c9f89dee`  
**Root-cause authority:** `SourceEvidence/UniversalInteriorViability/PhaseC3F18/FINDINGS.md`  
**Implementation evidence:** `SourceEvidence/UniversalInteriorViability/PhaseC3F18/IMPLEMENTATION_FINDINGS.md`  
**Failed predecessor:** `S1.42AK-BMGHDIAG1` — startup refusal evidence only, never a parent  
**Lifecycle authority:** `Current/CURRENT_STATE.json`  
**Patch policy:** `Current/68_PROJECT_LOCAL_PATCH_SAFETY_AND_REGRESSION_POLICY.md`

## Purpose

Repair only the BMGHDIAG1 startup-provenance defect that assumed loaded `Assembly-CSharp` must expose a hashable `Assembly.Location`, while preserving the exact Black Mesa x Greenhouse deterministic-selection and read-only entrance-observation contract.

This is a diagnostic-only successor. It must derive directly from exact accepted S1.42AK, never from BMGHDIAG1 profile bytes.

## Successor identity

- build ID: `S1.42AK-BMGHDIAG2`;
- project/assembly: `S142AKBMGHDiag2`;
- plugin GUID: `tendas.lethalcompany.s142akbmghdiag2`;
- version: `1.0.0`;
- marker prefix: `[BMGHDIAG2]`.

The predecessor marker prefix `[BMGHDIAG1]` must not be emitted by the successor DLL.

## Immutable gameplay boundary

Allowed Harmony surfaces remain exactly two:

1. one postfix on `LethalLevelLoader.DungeonManager.GetValidExtendedDungeonFlows(ExtendedLevel, bool)` that reduces only the already-viable Black Mesa result to the existing Greenhouse wrapper after accepted normalization;
2. one read-only postfix on exact `EntranceTeleport.TeleportPlayer()` for traversal/topology evidence after targeted selection succeeds.

No prefix, transpiler, RNG hook, RPC replacement, network-manager replacement, registration hook, config rewrite, global-list mutation, entrance-ID write, manual teleport or availability rewrite is allowed.

## Installed-V81 provenance repair

Expected installed source binary:

- logical path: `Lethal Company_Data/Managed/Assembly-CSharp.dll`;
- SHA-256: `5f7db5538b78dc408845a3002907619785ac9f9c6b6059d13dc9a602d9b65731`;
- Steam build ID bound by existing evidence: `22825947`.

The successor hashes the physical source file resolved only from BepInEx's canonical managed directory:

`Path.Combine(Path.GetFullPath(Paths.ManagedPath), "Assembly-CSharp.dll")`.

Fail closed unless the managed directory exists, the exact file exists and its SHA-256 matches the expected value. Do not use `Assembly.Location`, `Assembly.CodeBase`, current-directory probing, recursive searching or guessed Steam paths to identify the game source binary.

This game-binary validator is separate from the file-backed dependency validator used for ordinary plugin assemblies.

## Loaded runtime observation contract

Resolve the actual runtime type with `AccessTools.TypeByName("EntranceTeleport")` and fail closed unless:

- type full name is exactly `EntranceTeleport`;
- declaring assembly simple name is exactly `Assembly-CSharp`;
- assembly is non-dynamic;
- manifest module name is exactly `Assembly-CSharp.dll`;
- exact declared public instance `TeleportPlayer()` has zero parameters, return type `void`, and a method body;
- exact declared fields remain `entranceId : int`, `isEntranceToBuilding : bool`, `exitScript : EntranceTeleport`.

The runtime identity check is intentionally structural. It does not introduce an unproven MVID/token/process-module substitute merely to recreate a loaded-byte hash.

## Dependency contract retained

### LethalLevelLoader

- GUID: `imabatby.lethallevelloader`;
- version: `1.7.12`;
- DLL SHA-256: `b95aad3813dd7dc1d50aa29c9606660022b149790905a1589180e19d7c157c8c`.

### Accepted normalizer

- GUID: `tendas.lethalcompany.s142abinteriorweightnormalization`;
- version: `1.0.0`;
- DLL SHA-256: `901c02a8e85d33af24d0aa906faa6052a7de33faa7dfbeeca590bbd8a8f59a06`.

Require exactly one accepted normalizer postfix on the exact LLL selection target before installing the successor selection postfix. Install successor selection with `HarmonyAfter` equivalent ownership and `Priority.Last`; verify installed owner/order after patching.

## Selection contract retained

Fail closed unless all predecessor selection contracts hold:

- exact static `GetValidExtendedDungeonFlows(ExtendedLevel, bool)` target and return type;
- exact LLL wrapper/member contracts;
- exact server-selection caller in managed stack;
- terminal simulation caller absent;
- exact moon name `Black Mesa`;
- result non-null and exact expected list type;
- no null wrapper;
- exactly one `DungeonName == "Greenhouse"`;
- same entry asset name exactly `GreenhouseFlow`;
- final rarity exactly `100`.

Only after every validation succeeds may the fresh returned list be reduced to the same existing wrapper and the in-process target-selection flag be set.

## Observation contract retained

The TeleportPlayer postfix remains observation-only and inert until targeted selection succeeds.

A successful traversal observation still requires a non-null native `exitScript` with the same `entranceId` and opposite `isEntranceToBuilding` side. Track IDs 0..3 in both directions and emit one-time topology status requiring exactly one active object per side for each ID.

No observation result may mutate gameplay.

## Required successor markers

- `[BMGHDIAG2] ARMED`;
- `[BMGHDIAG2] SELECTED Black Mesa Greenhouse / GreenhouseFlow; normalized rarity=100; pool=<N>->1`;
- `[BMGHDIAG2] TRAVERSED ...`;
- `[BMGHDIAG2] TOPOLOGY_OK ids=0,1,2,3 uniqueOppositePairs=4`;
- `[BMGHDIAG2] TOPOLOGY_INCONCLUSIVE ...`;
- `[BMGHDIAG2] REFUSED TO ARM`;
- `[BMGHDIAG2] REFUSED selection`.

The startup success marker additionally states that installed V81 source-hash provenance and loaded TeleportPlayer structural identity both passed, without printing an absolute local path.

## Pure/static test minimum

### Provenance/path tests

- valid exact managed source path + expected hash -> pass;
- blank managed path -> fail;
- missing directory -> fail;
- missing exact file -> fail;
- unreadable file -> fail;
- wrong SHA -> fail;
- verify no alternate search/fallback behavior;
- verify game-binary check does not consume loaded `Assembly.Location`/`CodeBase`.

### Runtime identity tests

Pure helpers cover accepted/rejected assembly/type/module/method identity inputs. Reflection integration checks statically require exact name/module/non-dynamic/method/field contracts.

### Selection/observation tests

The complete predecessor policy suite is ported unchanged in behavior, including caller exclusion, exact Greenhouse identity/rarity, independent result lists, traversal pairing and topology checks.

## Source/static implementation gate

Implemented:

- `Patches/S142AKBMGHDiag2/` project and tests;
- `AnalysisTools/validate_s142ak_bmghdiag2_source.py`;
- `.github/workflows/s142ak-bmghdiag2-source-static.yml`;
- `SourceEvidence/UniversalInteriorViability/PhaseC3F18/IMPLEMENTATION_FINDINGS.md`.

The source gate proves:

- exactly two Harmony patch calls;
- one gameplay-mutating selection surface and one read-only observer;
- no prefix/transpiler/PatchAll;
- no game/mod field writes;
- exact dependency hashes/constants;
- exact installed V81 hash constant;
- canonical BepInEx managed-path resolution only for the game source binary;
- absence of loaded-game `Assembly.Location`/`CodeBase` provenance logic;
- full successor markers;
- live build/runtime controllers remain idle at S1.42AK.

CI success is not claimed until the exact implementation head completes both successor source/static and permanent repository gates.

## Review-build boundary

No BMGHDIAG2 build request exists in this source checkpoint.

Only after the successor source/static gate passes may a separate later checkpoint authorize an **inactive review build** from exact S1.42AK. Its eventual archive delta must be exactly one new BMGHDIAG2 diagnostic DLL plus permitted profile identity metadata, with zero package/config changes and byte-identical accepted normalizer.

## Runtime boundary

Static/source/build success never authorizes gameplay. A runtime test may occur only after a later separately reviewed publication and atomic lifecycle/controller arming step sets `runtime_test_outstanding=true` for exact published BMGHDIAG2 bytes.

When that later arming occurs, the same response must provide the repository-derived exact Gale replacement/import instructions and exact build-specific one-line PowerShell runtime-log uploader.

## Patch Safety Review

**Exact smallest surfaces:** the LLL returned-list postfix and post-native `EntranceTeleport.TeleportPlayer()` observer are unchanged from BMGHDIAG1. The repair happens before patch installation in startup provenance validation and adds no gameplay interception.

**Lifecycle ownership:** LLL remains sole selection/registration owner before the final diagnostic result-list reduction; native `EntranceTeleport.TeleportPlayer()` remains sole teleport owner. The successor never disables components or replaces lifecycle/network behavior.

**Secondary responsibilities:** none are suppressed. Startup provenance reads one exact installed file and inspects reflection metadata only.

**Forbidden broader alternatives:** removing the V81 hash guard; trusting only assembly simple name; filesystem-wide searching; hashing an arbitrary same-named DLL; changing Greenhouse rarity/availability; duplicate-registering Black Mesa; rewriting teleport IDs; manual teleport/RPC replacement; altering S1.42AB normalization.

**Runtime acceptance boundary:** unchanged from the predecessor proof contract — exact Black Mesa + Greenhouse generation, topology IDs 0..3, main entrance round trip, alternate exit traversal, no severe target-attributable geometry/routing failure, and no diagnostic refusal/inconclusive markers.

## Preserved state

This plan changes no live lifecycle/controller state. S1.42AK remains accepted/latest, no active candidate exists, no runtime test is outstanding, `BuildSpecs/current.json` remains disabled, and `RuntimeInbox/ACTIVE_BUILD.txt` remains `S1.42AK`.
