# S1.42AK-SCRAPDIAG1 — LC Office Scrap Placement Diagnostic

**Status:** DESIGN COMPLETE / IMPLEMENTATION PENDING / NOT BUILT / NOT ARMED  
**Date:** 2026-09-18  
**Parent:** exact accepted S1.42AK  
**Base profile:** `Profiles/LC V1 S1.42AK LC Office Camera Enemy Balance.r2z`  
**Base SHA-256:** `b39aa550a517ec727de6eb1ae825383933047d3c556cb6e8d4aa7611c9f89dee`  
**Design authority:** `Current/160_LC_OFFICE_SCRAP_PLACEMENT_DIAGNOSTIC_DESIGN.md`  
**Patch safety:** `Current/68_PROJECT_LOCAL_PATCH_SAFETY_AND_REGRESSION_POLICY.md`

## Objective

Produce a diagnostic-only profile that deterministically selects LC Office on Offense and logs the stable spawned scrap set with world/floor/room-placement evidence. Do not change any gameplay balance.

## Planned diagnostic delta

From exact S1.42AK only:

1. build and add existing reviewed `S142AJDiag1OfficeSelection.dll` from its current repository source;
2. build and add new `S142AKDiagScrapPlacement.dll` from `Patches/S142AKDiagScrapPlacement/`.

No package changes. No config changes. No mod removals. No gameplay tuning.

The diagnostic output must use a new profile/build identity and is never promotable directly to balanced gameplay state.

## New logger contract

The logger installs one read-only postfix on exact declared `RoundManager.SpawnScrapInLevel()`. It schedules two bounded delayed snapshots, validates a stable scrap instance-ID set, then logs item positions, nearest EntranceTeleport floor anchors and downward support-collider hierarchy paths.

Full safety/marker contract: `Current/160_LC_OFFICE_SCRAP_PLACEMENT_DIAGNOSTIC_DESIGN.md`.

## Planned non-active build request

Create `BuildSpecs/S1.42AK-SCRAPDIAG1.json` only after the logger source exists and static source review passes.

That request should contain two `local_plugin_builds`:

- `Patches/S142AJDiag1OfficeSelection/S142AJDiag1OfficeSelection.csproj` -> `BepInEx/plugins/S142AJDiag1OfficeSelection/S142AJDiag1OfficeSelection.dll`;
- `Patches/S142AKDiagScrapPlacement/S142AKDiagScrapPlacement.csproj` -> `BepInEx/plugins/S142AKDiagScrapPlacement/S142AKDiagScrapPlacement.dll`.

It must remain separate from `BuildSpecs/current.json` until publication/static review is complete.

## Static gate

The produced diagnostic must prove:

- exact S1.42AK base SHA;
- exactly two added archive members, both diagnostic DLLs;
- no changed or removed existing member;
- accepted S139 Compatibility Fixes and Interior Weight Normalization byte-identical to S1.42AK;
- AJDIAG1 source/tests still satisfy its existing exact-selection contract;
- new logger exact-target validation and bounded read-only behavior;
- compiled DLL SHA-256 values recorded;
- no runtime controller changes.

## Runtime boundary

This plan does not arm a runtime test. After implementation/build/static review, a later explicit lifecycle step may publish/arm the diagnostic. That ready-to-test response must include the canonical Gale replacement/import command and exact build-specific log uploader in the same response.
