# S1.42AD — Functional Microwave Spawn Rarity Reduction — Pre-Build Plan

**Status:** PREBUILD REVIEW BLOCKED / NOT ARMED / NOT BUILT  
**Accepted base:** S1.42AC — BCMER EventType Equal Distribution  
**Base profile:** `Profiles/LC V1 S1.42AC BCMER EventType Equal Distribution.r2z`  
**Base SHA-256:** `0ce58ab1fa0f0d76d6fbe1a4bff1dce9defc92e3d4b70cfb3056306e617e47d9`  
**Recovery authority:** `Current/119_S1.42AD_INTERRUPTED_IMPLEMENTATION_RECOVERY.md`  
**Patch safety policy:** `Current/68_PROJECT_LOCAL_PATCH_SAFETY_AND_REGRESSION_POLICY.md`

## Scope

The user explicitly selected the next build after S1.42AC acceptance. The selected isolated scope is:

**Functional Microwave spawn-rarity reduction.**

Preserve every accepted S1.42AC package/config/profile behavior outside this scope.

Accepted Functional Microwave audio setting remains:

`Volume = 0.15`

Do not confuse the spawn-rarity scope with historical volume proposals.

## Existing implementation draft

Source-only draft:

`Patches/S142ADCodeRebirthMicrowaveSpawnTuning/`

The interrupted session added the draft in four commits after accepted S1.42AC:

- `8f09cb5e70e569ef418dba950bac1d38d5d9583c` — NuGet config;
- `8c06d88b0fbf940d4c83fea11ef3910a927e2098` — project file;
- `61d4e48c77c58b09bbb02786251fce53d5d68ea8` — plugin implementation draft;
- `f8b547b06786371b875c6fa0b21ac2ded4f7498e` — draft README/contract.

This source has never been built into a profile. It is not an active candidate.

## BLOCKER 1 — exact balance target not yet authorized

The source draft hard-codes:

`SpawnScale = 0.5f`

Current canonical project requirements only establish that the Functional Microwave should become **rarer**. No exact percentage/scale is currently recorded as user-authorized project state.

Therefore `0.5` is a **proposal**, not the final build target.

Before arming the build, resolve the intended magnitude with the user or locate a genuinely authoritative prior instruction that defines it. If the approved target differs, update the source and this plan before building.

## BLOCKER 2 — ownership/provenance review not yet closed

The draft assumes the validated owner path is:

- CodeRebirth `1.6.9`;
- DawnLib `0.9.25`;
- DawnLib.Dusk `0.9.25`;
- exact map-object key `code_rebirth:functional_microwave`;
- `InsideInfo.SpawnWeights`;
- one `Dusk.MapObjectSpawnMechanics` provider in the DawnLib ProviderTable;
- exactly 19 moon/tag curves.

The structure mirrors the accepted S1.42Z DawnLib provider-scaling implementation, but the Microwave-specific owner/key/provider/curve contract must still be independently verified before build authorization.

Expected draft curve keys are documented in `Patches/S142ADCodeRebirthMicrowaveSpawnTuning/README.md`.

## Patch Safety Review — current status

### Exact owner / mutation surface

**Draft hypothesis, requires independent confirmation.**

Mutation surface is intended to be only the Functional Microwave's DawnLib/Dusk inside-spawn `AnimationCurve` provider set after registry freeze.

### Why this is intended to be the smallest safe surface

The project requirement is only to reduce one CodeRebirth inside hazard's natural occurrence. The draft does not suppress CodeRebirth globally, disable the object, patch broad game lifecycle methods, or alter unrelated map-object providers.

### Lifecycle / secondary responsibilities

The draft subscribes once to `LethalContent.Moons.OnFreeze`, validates frozen registry state, applies once, and otherwise leaves native CodeRebirth/DawnLib spawning/evaluation intact. This must be checked against exact owner source/API semantics before the build gate closes.

### State ownership

DawnLib/Dusk remains owner of map-object spawn evaluation. The project code proposes only deterministic amplitude scaling of the target provider curves.

### Forbidden broader alternatives

Do not:

- disable Functional Microwave components wholesale;
- globally patch DawnLib map-object selection;
- mutate all inside hazards;
- guess/fallback to another provider/key if the exact contract is absent;
- reinstall historical `CodeRebirthLib`.

### Adjacent behavior to preserve

- Functional Microwave remains functional when it spawns;
- accepted Volume `0.15` remains unchanged;
- all other CodeRebirth inside/outside hazards remain unchanged;
- accepted S1.42Z ACU/G.R.E.G. tuning remains unchanged;
- S1.42AC BCMER/interior/enemy/Pikmin/Jetpack invariants remain unchanged.

### Review status

**INCOMPLETE until the exact owner/curve contract and final balance target are confirmed.**

## Build gate after blockers are resolved

Only after both blockers above are closed:

1. update this plan with the approved exact scale and provenance;
2. compile `Patches/S142ADCodeRebirthMicrowaveSpawnTuning/S142ADCodeRebirthMicrowaveSpawnTuning.csproj` repository-native;
3. record the resulting DLL SHA-256;
4. configure `BuildSpecs/current.json` on exact S1.42AC base/SHA;
5. add only the intended local plugin DLL to the successor profile;
6. require archive/profile assertions that unrelated files/packages/configs are unchanged;
7. run the repository-native profile build and automated QC;
8. only then declare S1.42AD an active candidate and set `RuntimeInbox/ACTIVE_BUILD.txt = S1.42AD`.

Do not arm `BuildSpecs/current.json` while this plan status is BLOCKED.

## Required runtime acceptance after a candidate exists

The candidate runtime log must show:

- exact validated CodeRebirth/DawnLib/Dusk versions;
- S1.42AD tuner armed on the intended registry lifecycle;
- exact target/provider contract validated;
- final marker proving the approved curve scale was applied to all expected Microwave curves;
- no S1.42AD contract-refusal/error marker;
- no new fatal/project-critical regression marker;
- ordinary round generation/gameplay remains healthy.

The user should additionally confirm no problematic gameplay behavior was observed.

A short runtime sample cannot statistically prove an exact occurrence percentage. Acceptance should rely primarily on deterministic provider mutation plus clean adjacent runtime behavior.

## Runtime-test UX requirement

When and only when S1.42AD becomes a ready-to-test candidate, the same response that gives test instructions must also include:

1. the repository-driven Gale replacement/import PowerShell one-liner;
2. the exact S1.42AD self-contained PowerShell runtime-log uploader.

Do not provide an uploader now because no S1.42AD runtime test is currently outstanding.
