# S1.42AD — Functional Microwave Spawn Rarity Reduction — Build Contract

**Status:** PREBUILD REVIEW COMPLETE / READY TO ARM  
**Accepted base:** S1.42AC — BCMER EventType Equal Distribution  
**Base profile:** `Profiles/LC V1 S1.42AC BCMER EventType Equal Distribution.r2z`  
**Base SHA-256:** `0ce58ab1fa0f0d76d6fbe1a4bff1dce9defc92e3d4b70cfb3056306e617e47d9`  
**Recovery authority:** `Current/119_S1.42AD_INTERRUPTED_IMPLEMENTATION_RECOVERY.md`  
**Patch safety policy:** `Current/68_PROJECT_LOCAL_PATCH_SAFETY_AND_REGRESSION_POLICY.md`

## Scope

The isolated S1.42AD scope is:

**Functional Microwave spawn-rarity reduction to 50% of its current effective spawn-weight curve amplitude.**

Preserve every accepted S1.42AC package/config/profile behavior outside this scope.

Accepted Functional Microwave audio setting remains:

`Volume = 0.15`

S1.42AD changes spawn rarity only.

## Balance target — BLOCKER 1 CLOSED

During the segmented S1.42AD pre-build review on 2026-09-05, the user explicitly specified that the Functional Microwave should be encountered **half as often**.

The build target is therefore:

`SpawnScale = 0.5f`

Implementation semantics are proportional amplitude scaling of the currently effective Microwave spawn curves. It does not replace the curves with absolute fixed weights.

## Ownership / provenance — BLOCKER 2 CLOSED

Independent source/provenance review established the expected shipped runtime path:

- CodeRebirth `1.6.9`;
- DawnLib `0.9.25`;
- DawnLib.Dusk `0.9.25`;
- exact map-object key `code_rebirth:functional_microwave`;
- target owner surface `DawnMapObjectInfo.InsideInfo.SpawnWeights`;
- exactly one `Dusk.MapObjectSpawnMechanics` provider;
- `PrioritiseMoons == true`;
- exactly 18 Moon/tag curves;
- exactly zero Interior/tag curves.

### Bundle/source provenance correction

The later Unity-source state must not be mistaken for the shipped CodeRebirth 1.6.9 asset contract:

- relevant `coderebirthasset` rebuild commit: `c4020f20f5a05f1aee6519a2eb1554903890e08f` — 2026-04-06;
- later source edit commit: `eb4d5148047c625076b4735784a7ca2477ef17b6` — 2026-04-09;
- that later edit added Interior curves after the relevant bundle rebuild;
- therefore the later 19-key/source-only state is not accepted as the shipped runtime contract.

At the bundle-time source state, Functional Microwave has:

- Moon priority enabled;
- 18 Moon/tag curves;
- no Interior curves.

The expected 18 Moon/tag keys are documented in `Patches/S142ADCodeRebirthMicrowaveSpawnTuning/README.md`.

`code_rebirth:functional_microwave_ultra_high` is not part of the verified shipped Moon/tag contract.

## Patch Safety Review — COMPLETE

### Exact owner / mutation surface

Only the exact Functional Microwave DawnLib/Dusk inside-spawn provider is eligible for mutation.

The patch does not modify:

- other CodeRebirth map objects;
- other Inside Hazards;
- global DawnLib map-object selection;
- CodeRebirth networking/save/object functionality;
- package state or the accepted Microwave volume.

### Lifecycle / secondary responsibilities

The plugin:

1. validates exact CodeRebirth/DawnLib/Dusk versions;
2. waits for the DawnLib Moon-registry freeze unless already frozen;
3. requires the MapObjects registry to be frozen;
4. resolves exactly `code_rebirth:functional_microwave`;
5. validates the exact one-provider / Moon-priority / 18-Moon / 0-Interior contract;
6. validates every expected curve as non-null/non-empty;
7. only then scales all expected curve values and tangents by `0.5` once.

### State ownership

DawnLib/Dusk remains owner of map-object spawn evaluation. S1.42AD only scales the exact target provider's curves deterministically.

### Fail-closed rule

Any dependency/key/provider/priority/curve-count/key-set/curve-validity drift produces an error and **no mutation**.

No guessed fallback is permitted.

### Forbidden broader alternatives

Do not:

- disable Functional Microwave components wholesale;
- globally patch DawnLib map-object selection;
- mutate all inside hazards;
- mutate Interior curves if the expected shipped contract is not present;
- guess/fallback to another provider/key;
- reinstall historical `CodeRebirthLib`.

### Adjacent behavior to preserve

- Functional Microwave remains functional when it spawns;
- accepted Volume `0.15` remains unchanged;
- all other CodeRebirth inside/outside hazards remain unchanged;
- accepted S1.42Z ACU/G.R.E.G. tuning remains unchanged;
- S1.42AC BCMER/interior/enemy/Pikmin/Jetpack invariants remain unchanged.

## Build gate

The two pre-build blockers are closed. The repository-native build may now:

1. compile `Patches/S142ADCodeRebirthMicrowaveSpawnTuning/S142ADCodeRebirthMicrowaveSpawnTuning.csproj` in Release mode;
2. record the resulting DLL SHA-256;
3. build directly from the guarded S1.42AC profile/SHA;
4. add only `BepInEx/plugins/S142ADCodeRebirthMicrowaveSpawnTuning/S142ADCodeRebirthMicrowaveSpawnTuning.dll`;
5. make no package state/config changes except the Gale internal profile name generated by the builder;
6. require assertions that accepted CodeRebirth Microwave `Volume = 0.15` and `Allow Editing Config = true` remain unchanged;
7. require automated archive/profile diff QC;
8. only after a successful build declare S1.42AD an active runtime candidate and set `RuntimeInbox/ACTIVE_BUILD.txt = S1.42AD`;
9. return `BuildSpecs/current.json` to a disabled candidate guard after successful generation.

## Required runtime acceptance after candidate build

The runtime log must show:

- exact CodeRebirth `1.6.9`, DawnLib `0.9.25`, and Dusk `0.9.25` validation;
- S1.42AD tuner armed on the intended registry lifecycle;
- provider marker proving `PrioritiseMoons=true, MoonCurves=18, InteriorCurves=0`;
- final marker proving all 18 Functional Microwave Moon/tag curves were scaled by `0.5`;
- no S1.42AD contract-refusal/error marker;
- no new fatal/project-critical regression marker;
- ordinary startup, round generation and gameplay remain healthy.

The user should additionally confirm no problematic gameplay behavior was observed.

A short runtime sample cannot statistically prove an exact 50% observed occurrence ratio. Acceptance relies primarily on deterministic provider mutation plus clean adjacent runtime behavior.

## Runtime-test UX requirement

When S1.42AD is a ready-to-test candidate, the same response that gives test instructions must also include:

1. the canonical repository-driven Gale replacement/import PowerShell one-liner;
2. the exact S1.42AD self-contained PowerShell runtime-log uploader.

Do not ask the user to build the profile locally.
