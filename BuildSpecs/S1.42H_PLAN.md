# S1.42H Plan — Thumper Grab Guard, Late Enemy Isolation, BCMER Off

Status: **AUTHORIZED DIAGNOSTIC BUILD**

Base:
`Profiles/LC V1 S1.42G Routed Moon Performance Fix.r2z`

Base SHA-256:
`09364c11f8032645205b869ad760471259520cd57758e4d2d09a35665cf0d35a`

## Purpose

Finish the focused enemy regression stage with deterministic target spawns while BCMER is temporarily disabled, and fix the reproduced Thumper/Crawler -> Pikmin invalid grab state.

## Compatibility plugin v1.3.5

### Exact Pikmin grab hook
- patch exactly the declared `LethalMin.PikminAI.GrabPikmin(Transform,float,int)` implementation once;
- do not scan or patch inherited implementations through derived Pikmin types;
- this replaces the incomplete adapter-only state-repair coverage.

### Thumper / Crawler zero interaction
Binding rule:
**Thumper/Crawler and Pikmin must not interact in either direction.**

- detect a Crawler/Thumper-owned GrabPikmin snap position;
- block the GrabPikmin call before LethalMin removes the Pikmin leader or starts its grabbed death timer;
- retain `Crawler` in LethalMin's Pikmin Attack Blacklist for the reverse Pikmin -> Thumper direction;
- expected marker:
  `[ThumperPikminGuard] Blocked Crawler/Thumper -> Pikmin GrabPikmin before leader/grab/death-timer state mutation.`

### Generic invincible-Pikmin recovery
For non-Thumper enemy grabs:
- capture valid state before `PikminAI.GrabPikmin`;
- allow normal enemy interaction;
- after the death-delay window, restore/release a surviving invincible Pikmin only if it lost its leader/follow state;
- no broad Harmony reflection scan.

Expected startup marker:
`[LethalMinStateGuard] Directly patched declared LethalMin.PikminAI.GrabPikmin(Transform,float,int) exactly once.`

### EnemyIsolation
Carry forward the unbuilt v1.3.4 late-lifecycle improvement:
- FinishGeneratingNewLevelClientRpc postfix;
- PredictAllOutsideEnemies prefix;
- BeginEnemySpawning prefix;
- no Update-driven or continuous global EnemyAI scan;
- target pools: indoor Crawler/Puffer, outdoor Baboon Hawk, daytime none;
- minimum diagnostic spawn attempts: inside >= 2, outside >= 1.

### Ship door
Carry forward the throttled DoorAudit / BCMER DoorFailure compatibility recognition:
- no per-call stacktrace flood;
- retain state-change audit and anti-lockout;
- recognize forced-open zero-power BCMER DoorFailure only on transitions.

## Temporary package state

Disable only:
`SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0`

BCMER must be restored after this isolated regression stage.

Expected manifest:
- 188 packages total;
- 182 enabled;
- 6 disabled.

## Deliberately unchanged

- Functional Microwave volume remains 0.7.
- Functional Microwave rarity is **not changed in S1.42H**; the requested rarity reduction remains deferred until after the isolated enemy regression.
- ButteRyBalance `Reduce Battery = false`.
- Jetpack target remains 140 seconds.
- JetpackFixes `MidAirExplosions = Off`.
- normal enemy configuration is not rewritten; canonical restore point remains `Current/ENEMY_SPAWN_BASELINE_S1.42C.json`.

## Runtime acceptance

Import with:
**Gale -> Advanced options -> Import all files**

Primary checks:
1. Main Menu/startup succeeds.
2. No periodic routed-moon freezes.
3. Thumper can approach through Pikmin without grabbing/removing leaders/disappearing into a broken interaction state.
4. No new `Leader is null when following` loop from Thumper contact.
5. Baboon Hawk grab on an invincible Pikmin recovers to a valid leader/follow state.
6. Puffer smoke remains unable to affect Pikmin.
7. Enemy targets continue to spawn and appear in the terminal `Enemies` output.
8. No Coroner per-frame Jetpack null-player flood.

After this stage passes:
- disable the temporary EnemyIsolation diagnostic;
- restore the full normal enemy state from `Current/ENEMY_SPAWN_BASELINE_S1.42C.json`;
- re-enable exact BCMER 1.71.0.
