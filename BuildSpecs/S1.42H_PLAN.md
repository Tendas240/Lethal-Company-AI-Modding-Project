# S1.42H Plan — Late Enemy Isolation, BCMER Off

Status: **AUTHORIZED DIAGNOSTIC BUILD**

Base:
`Profiles/LC V1 S1.42G Routed Moon Performance Fix.r2z`

Base SHA-256:
`09364c11f8032645205b869ad760471259520cd57758e4d2d09a35665cf0d35a`

## Purpose

Produce a deterministic focused enemy-interaction test without BCMER modifying spawn curves or injecting event enemies.

## Changes

### Compatibility plugin v1.3.4
- apply EnemyIsolation late during every generated round;
- hook FinishGeneratingNewLevelClientRpc / PredictAllOutsideEnemies / BeginEnemySpawning;
- remove currentLevel-reference gating;
- no continuous EnemyAI scene scan;
- diagnostic min spawn attempts: inside >= 2, outside >= 1;
- emit final pool/rarity/curve diagnostics;
- remove per-call ship-door stacktrace logging;
- retain state-change DoorAudit + anti-lockout;
- retain transition-only BCMER DoorFailure compatibility recognition.

### Temporary mod state
Disable only:
`SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0`

BCMER MUST be restored after the isolated enemy test.

### Functional Microwave
- editing gate remains true;
- volume remains 0.7;
- Inside Moon Spawn Weights = 75% of CodeRebirth 1.6.9 defaults.

No other package changes.

## Expected test manifest

188 packages total.
BCMER changes enabled -> disabled for this diagnostic profile only.

## Runtime test

Import:
**Gale -> Advanced options -> Import all files**

Recommended moon:
**Offense** for continuity with the previous evidence.

After generation, verify the log contains late isolation markers showing:
- Indoor only Crawler/Puffer (+ Pikmin family if registered there);
- Outdoor only BaboonHawk (+ Pikmin family if registered there);
- no unrelated normal enemies;
- non-sentinel spawn curves;
- minInside >= 2;
- minOutside >= 1.

Then:
1. use terminal `Enemies` after landing and later in the day;
2. locate Baboon Hawk and test generic invincible-Pikmin state recovery;
3. locate Thumper/Crawler and test zero interaction both directions;
4. locate Puffer and test no Pikmin smoke/attack effect;
5. test Jetpack ~140 seconds and no sustained/high-speed self-explosion;
6. confirm Microwave volume 0.7 and note whether 25% rarity reduction feels appropriate.

Upload the complete log to `RuntimeInbox/Current/`.
ZIP is acceptable if the log exceeds GitHub's file-size limit.
