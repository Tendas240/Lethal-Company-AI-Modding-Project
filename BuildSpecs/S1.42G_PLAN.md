# S1.42G Plan — Routed-Moon Stall + Coroner Jetpack Spam Fix

Status: **AUTHORIZED HOTFIX BUILD**

Base:
`Profiles/LC V1 S1.42F Enemy Isolation Freeze Fix.r2z`

Base SHA-256:
`f09404a8195b46261570331f736d921fb1cb25cd304e8952e5f6fcb404ed9e6b`

Evidence:
`RuntimeEvidence/S1.42F/20260903T092728Z/`

Diagnosis:
- S1.42F fixed the Gordion constructor exception loop;
- routed moons still activate the once-per-second diagnostic live-enemy scene scan;
- Coroner independently spams two warnings per frame from its unheld-Jetpack `JetpackItem.Update` death detector.

## S1.42G delta

Project-local compatibility plugin:
**v1.3.3**

Only local compatibility code changes.

1. EnemyIsolation
   - apply spawn-pool filtering once when current SelectableLevel changes;
   - no per-second pool rewrite;
   - no continuous `FindObjectsOfType<EnemyAI>()` scan.
2. Coroner
   - keep package enabled;
   - remove only Coroner's `JetpackItemUpdatePatch` prefix/postfix from `JetpackItem.Update`;
   - retain all other Coroner functionality.

No package/config/version changes.

## Runtime test

Import:
Gale -> Advanced options -> Import all files.

Gate:
1. Main Menu.
2. Host into ship lobby in orbit.
3. Route to a moon.
4. Wait at least 20-30 seconds before landing.
5. Confirm:
   - no once-per-second short freezes;
   - no continuous `PlayerController was null` / `Index not assigned!` Coroner flood;
   - `[CoronerJetpackGuard] Removed 2/2 ...` marker;
   - `[EnemyIsolation] One-shot level isolation applied; continuous global EnemyAI scanning is disabled.`
6. If smooth, continue with:
   - Baboon Hawk state recovery;
   - Thumper/Crawler zero interaction;
   - Puffer immunity;
   - Jetpack ~140 seconds / no sustained-use self-explosion;
   - Microwave volume 0.7.

Do not retest S1.42D.
Do not use S1.42E or S1.42F for the interaction stage.
