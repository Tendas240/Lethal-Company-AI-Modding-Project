# S1.42AC Plan — BCMER EventType Equal Distribution

**Status:** ARMED FOR REPOSITORY BUILD  
**Accepted baseline:** S1.42AB — Interior Weight Normalization — ACCEPTED FULL NORMAL STACK  
**Scope:** isolated BCMER EventType balancing only

## Accepted base

Profile:

`Profiles/LC V1 S1.42AB Interior Weight Normalization.r2z`

SHA-256:

`3f2387886daaf68d0d55ddc1b3cffb913565a658db0072b11f3b975ff07860ca`

Acceptance:

`Current/102_S1.42AB_RUNTIME_ACCEPTANCE_INTERIOR_WEIGHT_NORMALIZATION.md`

## Intended candidate

Build ID:

`S1.42AC`

Profile:

`Profiles/LC V1 S1.42AC BCMER EventType Equal Distribution.r2z`

Gale profile name:

`LC V1 S1.42AC BCMER EventType Equal Distribution`

## Requirement

Make all eight BrutalCompanyMinusExtraReborn EventTypes equally likely globally while preserving exact BCMER `1.71.0` and all accepted S1.42AB behavior.

Target distribution:

- Insane = 12.5%
- VeryBad = 12.5%
- Bad = 12.5%
- Neutral = 12.5%
- Good = 12.5%
- VeryGood = 12.5%
- Rare = 12.5%
- Remove = 12.5%

## Exact config architecture

Target file:

`BepInEx/config/BrutalCompanyMinusExtraReborn/Difficulty_Settings.cfg`

Preserve:

`[_Event Settings] / Use custom weights? = false`

BCMER therefore continues to select from its EventType weight system rather than switching to individual custom event weights.

Under `[_EventType Weights]`, set all eight type scales to the same constant curve:

`12.5, 0.0, 12.5, 12.5`

Exact keys:

- `Insane event scale`
- `VeryBad event scale`
- `Bad event scale`
- `Neutral event scale`
- `Good event scale`
- `VeryGood event scale`
- `Rare event scale`
- `Remove event scale`

Because all eight positive EventType weights are identical and difficulty-independent, each type has an exact normalized share of `1 / 8 = 12.5%` whenever BCMER performs its normal EventType roll.

## Frozen build delta

Exactly one existing config file may change, in addition to the mandatory Gale profile-name change inside `export.r2x`:

- `BepInEx/config/BrutalCompanyMinusExtraReborn/Difficulty_Settings.cfg`

No mod state changes.

No mod additions.

No mod removals.

No DLL injections.

No project-local code patch.

Do not alter:

- BCMER package/version (`SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0` remains exact);
- event amount / bonus-event settings;
- individual VanillaEvents/ModdedEvents weights;
- disabled BCMER rain routes (`Raining`, `HeavyRain`, `AllWeather`, `Hurricane`);
- `Experimental Dont Handle Power? = true`;
- `Experimental Dont Handle Spawn Chance? = true`;
- `Let Brutal handle properties outside of events? = false`;
- `Enable Randomizer? = false`;
- `Randomize Event Weights? = false`;
- S1.42AB interior normalization;
- accepted Jetpack/Pikmin/CodeRebirth/Snail/Microwave/enemy compatibility tuning.

## Build verification contract

Automated output must prove:

- base SHA guard matches accepted S1.42AB;
- output profile is new and unique;
- changed existing members are exactly `export.r2x` and `BepInEx/config/BrutalCompanyMinusExtraReborn/Difficulty_Settings.cfg`;
- added members = `0`;
- removed members = `0`;
- mod state/add/remove delta = `0`;
- `Use custom weights? = false` remains present;
- all eight EventType scale lines equal `12.5, 0.0, 12.5, 12.5`.

## Runtime acceptance gate

S1.42AC is not accepted merely because the build passes.

Minimum fresh runtime test after import:

1. startup/main menu/lobby succeeds;
2. exact BCMER `1.71.0` loads normally;
3. normal moon routing/landing succeeds;
4. BCMER emits all eight `Set eventType weight for ...` runtime values;
5. all eight resulting EventType weights are equal in the same roll context;
6. BCMER events still occur normally;
7. disabled BCMER rain-event guards remain preserved;
8. S1.42AB `[InteriorWeightNormalization] Final effective viable pool` remains healthy when emitted;
9. accepted enemy/Pikmin/Jetpack/CodeRebirth behavior remains healthy;
10. Work/no-task = 0 preferred;
11. Leader-null = 0;
12. Fatal = 0;
13. no new project-local/config-induced error flood;
14. complete fresh `LogOutput.log` is ingested before acceptance.

A single normal gameplay run is sufficient if all eight EventType-weight runtime lines are present and equal and the normal stack remains healthy.

## Rollback

Rollback is deterministic: return to accepted S1.42AB. No other accepted file/config behavior is intentionally modified.
