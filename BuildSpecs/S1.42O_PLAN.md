# S1.42O Plan — Baboon Hawk Native Task Finalization

Status: **AUTHORIZED FOCUSED SUCCESSOR AFTER S1.42N STATE-FINALIZATION FAILURE**

Base:
`Profiles/LC V1 S1.42N Baboon Hawk Death Target Resolver.r2z`

Base SHA-256:
`c87d48464a750f87274e2848c44e5e1e24d4f1da087f59a33e2889744ebc13e9`

Evidence:
`RuntimeEvidence/S1.42N/20260903T165818Z/`

Log SHA-256:
`8ed6a79230e28535a01a4cb86a8971c358a5c9f57a53722e18ff4342474f51cf`

## Confirmed S1.42N result

PASS:
- one-shot SpawnedEnemies resolver found real attack participants;
- 6/6 Pikmin were selected;
- corpse behavior remains valid from predecessor gate.

FAIL:
- direct `RemoveCurrentTask()` left five attackers in `Work state with no task assigned!`;
- one attacker continued stale hit-loop activity after Hawk death;
- user observed 4–5 Pikmin running in place, non-following and unusable.

Natural control:
`PerDu` executed LethalMin's own `Task finished -> Setting to idle -> Removing current task` path and remained functional enough to take a later CarryItem task.

## S1.42O scope

Compatibility plugin:
**v1.3.10**

Keep:
- exact `BaboonBirdAI.KillEnemy(bool)` hook;
- one-shot `RoundManager.Instance.SpawnedEnemies` candidate registry;
- exact/assignable `LethalMin.PikminAI` filtering;
- 4.0 m death-release zone;
- S1.42M corpse pickup protection unchanged;
- EnemyIsolation enabled;
- BCMER 1.71.0 disabled.

Replace:
- direct `PikminAI.RemoveCurrentTask()` death cleanup

With:
- exact declared zero-argument `PikminAI.TaskFinished()`;
- validate declared type, void return, zero parameters and method body;
- no low-level RemoveCurrentTask fallback if TaskFinished is unavailable.

Diagnostics:
- startup resolution marker for TaskFinished;
- per-Pikmin native finalization marker;
- aggregate finalized/candidate count;
- candidate-method list if TaskFinished cannot be resolved.

## Runtime acceptance

1. attack/latch living Baboon Hawk;
2. let Pikmin kill it;
3. attackers remain responsive / recoverable and do not enter no-task Work loop;
4. no stale hit loop against dead Hawk;
5. player can regain/follow with the affected Pikmin;
6. corpse still appears and is Onion-carryable;
7. living Hawks ignore corpse;
8. Hawk -> Pikmin ignore still works;
9. no leader-null loop.
