# S1.42AK — LC Office Camera Render and Enemy Balance Fix

**Status:** PREPARED / NOT BUILT  
**Date:** 2026-09-18  
**Parent:** exact S1.42AJ — LC Office V81 Integration — NOT ACCEPTED  
**Base profile:** `Profiles/LC V1 S1.42AJ LC Office V81 Integration.r2z`  
**Base SHA-256:** `7c1441aeb0732208bb8e910d89348c2e0129ce202422103a025e8f8aea707dba`  
**Runtime decision evidence:** `Current/156_S1.42AJ_DIAG2_LC_OFFICE_CAMERA_RENDER_CONFIRMATION_AND_S1.42AK_SUCCESSOR_DECISION.md`

## Objective

Create the next balanced LC Office successor directly from exact S1.42AJ. Carry forward the DIAG2 camera-render mitigation without inheriting diagnostic force-selection, disable Men-stalker, and set Aloe's PowerLevel to zero.

Do not mix universal-interior availability, LC Office scrap tuning, RandomEnemiesSize tuning, or other deferred scopes into this build.

## Exact authorized delta

### LC Office
Create `BepInEx/config/Piggy.LCOffice.cfg` with:
```ini
[General]
Camera Frame Speed = 0
```

### Men-stalker
Change exact package state only:
- `YaBoiDucki-men_stalker`
- version `3.1.2`
- `enabled: true -> false`

Do not remove the package and do not patch its spawn-weight config.

### Aloe
Patch:
- `BepInEx/config/me.biodiversity.aloe.cfg`
- section `[SpawnSettings]`
- `PowerLevel = 0`

Preserve Aloe enabled state, rarity table, max amount and all unrelated Biodiversity settings.

## Explicit non-changes

- `Wexop-RandomEnemiesSize 1.1.20` remains enabled and unchanged.
- `BepInEx/config/wexop.random_enemies_size.cfg` must remain byte-identical to S1.42AJ.
- No LC Office scrap setting is changed.
- No DIAG1/DIAG2 force-selection DLL or diagnostic-only file may be inherited.
- No package addition/removal/version transition is authorized.
- No unrelated config or local-plugin delta is authorized.

## Expected archive delta versus S1.42AJ

Changed existing members:
1. `export.r2x` — Gale profile name plus Men-stalker enabled state only.
2. `BepInEx/config/me.biodiversity.aloe.cfg` — `[SpawnSettings] PowerLevel = 0` only.

Added members:
1. `BepInEx/config/Piggy.LCOffice.cfg` — `[General] Camera Frame Speed = 0`.

Removed members: none. Every other existing archive member must remain byte-identical to exact S1.42AJ.

## Planned build identity

- Build ID: `S1.42AK`
- Output profile: `Profiles/LC V1 S1.42AK LC Office Camera Enemy Balance.r2z`
- Gale profile name: `LC V1 S1.42AK LC Office Camera Enemy Balance`
- Readable snapshot: `ProfileSources/S1.42AK/`

## Build-controller contract

Before the atomic trigger, `BuildSpecs/current.json` stays disabled.

The atomic build request must use:
- `enabled = true`;
- exact S1.42AJ base path and SHA above;
- `overwrite = false`;
- exactly one `mod_state_changes` entry for `YaBoiDucki-men_stalker 3.1.2 -> false`;
- exactly two `config_patches`: LC Office Camera Frame Speed `0` and Aloe PowerLevel `0`;
- no additions, removals, file injections or local plugin builds;
- assertions proving Men-stalker disabled, Aloe PowerLevel `0`, Camera Frame Speed `0`, and unchanged RandomEnemiesSize config.

Do not expose S1.42AK for runtime testing until the repository build and static byte-delta verification pass.
