# S1.42F Plan — Enemy Isolation Freeze Fix

Status: **AUTHORIZED HOTFIX BUILD**

Base:
`Profiles/LC V1 S1.42E Startup Safe Enemy Regression.r2z`

Base SHA-256:
`4df5d6417aad35ad327b183eb2dd25ecb6bd20382840198f74f0201007d57348`

## Reason

S1.42E passed the startup gate and registered the narrowed LethalMin state guard on four declared enemy-adapter methods.

However, the temporary diagnostic enemy-isolation layer caused short periodic freezes in the ship lobby.

Evidence:
`RuntimeEvidence/S1.42E/20260903T091053Z/`

Diagnosis:
- Gordion/Company was processed every second;
- missing diagnostic pool entries triggered repeated `Resources.FindObjectsOfTypeAll<EnemyType>()` scans;
- `Activator.CreateInstance(SpawnableEnemyWithRarity)` then failed because there is no parameterless constructor;
- six `MissingMethodException` warnings were produced per second.

## S1.42F delta

Project-local compatibility plugin:
**v1.3.2**

Only diagnostic-isolation implementation changes:
- skip Gordion/Company pool work while in orbit;
- construct spawn entries using `EnemyType, int` constructor;
- fallback to cloning an existing pool entry;
- retain actual-moon allowlist enforcement.

No config/package changes.

Carry forward unchanged:
- safe DeclaredOnly LethalMin state guard from S1.42E;
- Thumper Bite Limit 0;
- Crawler Attack Blacklist;
- Puffer poison false + smoke guard;
- Jetpack 140-second target;
- JetpackFixes MidAirExplosions Off;
- Functional Microwave edit gate true / volume 0.7;
- all prior accepted compatibility guards.

## Runtime test

1. Import with Gale -> Advanced options -> Import all files.
2. Reach Main Menu.
3. Host and load into ship lobby in orbit.
4. Verify there is no once-per-second freeze cadence.
5. Verify:
   - `S1.39 Compatibility Fixes loaded.`
   - `[LethalMinStateGuard] Safe generic grab/bite state repair registered on 4 declared enemy method(s).`
   - `[EnemyIsolation] Skipping diagnostic pool work on Gordion/Company while in orbit.`
6. If lobby is smooth, continue to the isolated Enemy / Jetpack / Microwave regression test without requiring another candidate first.

Do not retest S1.42D.
Do not use S1.42E for the interaction stage.
