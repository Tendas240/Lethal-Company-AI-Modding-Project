# S1.42T Plan — Normal Enemy Restore

**Status:** next build plan; not built at handover  
**Base:** S1.42S — Baboon Adapter Lifecycle Restore

Base profile:

`Profiles/LC V1 S1.42S Baboon Adapter Lifecycle Restore.r2z`

Base SHA-256:

`addc5f0cd2508bf821e4e8eda80aca0f94234c7f2823c9acc6e8655060790fee`

Output target:

`Profiles/LC V1 S1.42T Normal Enemy Restore.r2z`

## Goal

Close the temporary EnemyIsolation phase and restore the normal enemy population without changing any unrelated gameplay/package behavior.

## Exact required delta

### Disable temporary diagnostic

`BepInEx/config/tendas.s139.compatibilityfixes.cfg`

Set:

`Isolated Enemy Regression = false`

### Preserve later accepted LethalMin values

`BepInEx/config/NoteBoxz.LethalMin.cfg`

Keep:

`Thumper Bite Limit = 3`

Keep Crawler absent from:

`Attack Blacklist`

### Preserve normal spawn-owner configs

Use `Current/ENEMY_SPAWN_BASELINE_S1.42C.json` as the restore contract.

Spot-checked S1.42S normal spawn-owner config hashes already match S1.42C; do not rewrite them unnecessarily.

### BCMER

Keep exact:

`SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0`

**disabled** for S1.42T.

Do not upgrade.

## Package delta

None.

## Custom patch delta

None expected.

Compatibility plugin stays:

**v1.3.14**

Do not modify patch code for this restore build.

## Patch Safety Review

No new custom patch is introduced.

Patch surface remains unchanged from runtime-accepted S1.42S.

The purpose of S1.42T is specifically to change only the diagnostic config state, minimizing attribution ambiguity.

Forbidden broader alternative:

- do not roll back the compatibility DLL;
- do not wholesale copy old S1.42C configs over S1.42S;
- do not re-enable BCMER in this same first restore gate;
- do not combine repository migration or unrelated tuning.

## Runtime acceptance

After Gale import via **Advanced options -> Import all files**:

1. game reaches main menu and loads moon normally;
2. EnemyIsolation startup warning must be absent;
3. normal non-allowlisted enemies are again eligible/visible;
4. terminal EnemyScan can show normal active enemies;
5. no startup freeze/crash;
6. no Work/no-task loop;
7. no Leader-null loop;
8. no new compatibility exception flood;
9. accepted Pikmin interaction behavior remains unchanged;
10. commit full fresh `LogOutput.log` to `RuntimeInbox/Current/`.

Do not require a repeated heavy Baboon-Hawk stress test unless S1.42T unexpectedly changes the compatibility DLL or relevant LethalMin config.
