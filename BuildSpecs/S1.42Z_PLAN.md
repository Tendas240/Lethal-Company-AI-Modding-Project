# S1.42Z Plan — Jetpack 18f / Indoor Pikmin 0.09 / Accepted Microwave

**Status:** FROZEN FOR BUILD  
**Accepted rollback baseline:** S1.42U  
**Latest runtime-tested tuning candidate:** S1.42Y — technical paths PASS; Microwave and CarryStrength accepted; Jetpack/Indoor Pikmin balance retune requested

## Base profile

Build directly from:

`Profiles/LC V1 S1.42U BCMER 1.71.0 Reactivation Gate.r2z`

SHA-256:

`ff5fdebf22fefdd5515b95677174290f9666e491447138f074e5b65673173969`

Do not stack V/W/X/Y tuning DLLs. Reapply only the current desired config and inject only the two S1.42Z project-local DLLs.

## Frozen S1.42Z scope

### Jetpack

Project source:

`Patches/S142ZJetpackAcceleration/`

Target:

- validated ButteRyBalance local-player baseline `10f -> 18f`;
- exact parameterless `JetpackItem.Update()` Prefix after ButteRyBalance;
- V49 handling/deceleration untouched;
- JetpackFixes safety behavior untouched;
- no max-power/speed mutation in the custom DLL.

S1.42Y proved the same architecture at 22f. Z changes only the magnitude to the user-requested 18f.

### More Ship Upgrades progression

Jet Fuel:

- `Initial Acceleration Increase = 18`;
- `Incremental Acceleration Increase = 18`.

This keeps the absolute purchased gain per tier near the previous ~3.2–3.3f target:

- 18 × 18% = 3.24f.

Approximate acceleration progression:

- base 18.00;
- +18% 21.24;
- +36% 24.48;
- +54% 27.72;
- +72% 30.96.

Jetpack Thrusters:

- `Initial Maximum Speed Increase = 25`;
- `Incremental Maximum Speed Increase = 20`.

### LethalMin

Main config:

`BepInEx/config/NoteBoxz.LethalMin.cfg`

Target:

`Indoor Pikmin Spawn Chance = 0.09`

CarryStrength remains accepted and unchanged:

- Blue / Red / Yellow / White / Winged / Rock / Ice / Glow / Bulbmin = `3`;
- Purple = `30`.

No attack, speed, throw-force, growth, hazard resistance or lifecycle setting is changed.

### CodeRebirth aerial defense

Project source:

`Patches/S142ZCodeRebirthAerialDefenseSpawnTuning/`

Preserve the S1.42Y-proven transactional two-target contract:

1. `code_rebirth:air_control_unit` — exactly 18 curves × `0.5`;
2. `code_rebirth:gunslinger_greg` — exactly 18 curves × `0.5`.

No other map-object provider is modified.

### Functional Microwave

Preserve accepted:

- `Functional Microwave | Volume = 0.15`;
- `Functional Microwave | Allow Editing Config = true`.

User accepted the 0.15 volume in S1.42Y. Microwave spawn rarity remains deferred.

### Immortal Snail

Preserve:

- `Rarity = 40`;
- `Max Snails = 2`.

## Patch Safety Review — Jetpack

Full source review:

`Patches/S142ZJetpackAcceleration/README.md`

Safety properties:

- exact declared method/signature;
- local player only;
- no original skip;
- no extra transpiler;
- exact dependency and Harmony-owner validation;
- no network/RPC/save mutation;
- only the proven approximately-10f baseline is replaced.

## Patch Safety Review — aerial defense

Full source review:

`Patches/S142ZCodeRebirthAerialDefenseSpawnTuning/README.md`

Safety properties:

- exact ACU and G.R.E.G. keys only;
- no Harmony patch;
- exact one-provider / 18-key contract per target;
- validate both before changing either;
- no global outside-hazard scaling;
- fail closed on drift.

## Deliberately excluded

Do not mix into S1.42Z:

- Microwave spawn rarity reduction;
- equal interior probability implementation;
- future-interior equal-probability rule implementation;
- CullFactory `junkrooms` / `shatteredrooms` exceptions;
- Mausoleum fog tuning;
- BCMER EventType 8 × 12.5% balancing;
- AdditionalNetworking patching;
- LethalMin DespawnLumiknulls repair;
- cosmetic documentation cleanup;
- final long full-stack acceptance stage.

## Expected archive delta vs S1.42U

Changed existing members:

1. `export.r2x`;
2. `BepInEx/config/dev.idjut.SnailFork.cfg`;
3. `BepInEx/config/CodeRebirth.cfg`;
4. `BepInEx/config/com.malco.lethalcompany.moreshipupgrades.cfg`;
5. `BepInEx/config/NoteBoxz.LethalMin.cfg`;
6. ten configured `BepInEx/config/Pikmin/*.cfg` files.

Added members only:

- `BepInEx/plugins/S142ZJetpackAcceleration/S142ZJetpackAcceleration.dll`;
- `BepInEx/plugins/S142ZCodeRebirthAerialDefenseSpawnTuning/S142ZCodeRebirthAerialDefenseSpawnTuning.dll`.

Forbidden historical tuning DLLs include all S142V/S142W/S142X/S142Y Jetpack tuning DLLs and prior X/Y aerial-defense tuning DLLs.

No mod package add/remove/enable/disable changes are allowed.

## Runtime gate

1. startup/main menu succeeds;
2. `S1.42Z Jetpack Acceleration 1.0.0` loads;
3. dependency/Harmony-owner validation succeeds;
4. exact `10 -> 18` armed marker appears;
5. `S1.42Z CodeRebirth Aerial Defense Spawn Tuning 1.0.0` loads;
6. CodeRebirth 1.6.9 / DawnLib 0.9.25 / Dusk 0.9.25 validate;
7. ACU exact provider validates with 18 curves;
8. G.R.E.G. exact provider validates with 18 curves;
9. final transactional marker confirms both 18-curve sets ×0.5;
10. no aerial-defense contract refusal;
11. Jetpack 18f feels acceptable and calmer than Y/22f while still improving on the old weak 10/16 behavior;
12. V49 handling, release, landing, hard contact and repeated flight remain sane;
13. Jet Fuel 18/18 and Thrusters 25/20 remain reasonable if practical to test;
14. Indoor Pikmin density at 0.09 feels better than Y/0.08;
15. CarryStrength remains correct at 3 / Purple 30;
16. Microwave 0.15 remains accepted and functional;
17. Snail remains functional at 40 / max 2;
18. normal enemies remain active;
19. BCMER 1.71.0 and Compatibility Fixes 1.3.14 remain healthy;
20. Work/no-task = 0;
21. Leader-null = 0;
22. Fatal = 0;
23. no new project-local exception flood;
24. ingest a complete fresh runtime log using the mandatory self-contained PowerShell one-liner.
