# S1.42AI-DIAG1R2 Build Request

**Status:** SUCCESSOR DETERMINED / BUILD REQUEST PREPARED / NOT BUILT / NOT A RUNTIME CANDIDATE  
**Date:** 2026-09-15  
**Accepted gameplay baseline:** `S1.42AH`  
**Parent full-normal candidate:** `S1.42AI`  
**Failed predecessor diagnostic:** `S1.42AI-DIAG1R1`

## Decision

The next diagnostic artifact is `S1.42AI-DIAG1R2`. It is a second repair revision of the existing DIAG1 ShyGuy-isolation contract, not a new diagnostic scope.

`S1.42AI-DIAG1R1` cannot be rerun unchanged because its runtime bytes contain the failed unconditional `ElevatorMod.Patches.EndlessElevator` prevalidation path. The permanent source repair landed through PR #91 and therefore requires a newly compiled diagnostic plugin and a new profile artifact before the isolation contract can be tested again.

The ordinary full-normal `S1.42AI` runtime gate remains deferred and is not waived. Diagnostic success cannot accept the full-normal candidate.

## Exact build identity

- Build ID: `S1.42AI-DIAG1R2`
- Base profile: `Profiles/LC V1 S1.42AI ShyGuy Interior Only.r2z`
- Base SHA-256: `d993bc0fca265fe7a2b069bd654b5e2c1f590623eaf7f4fabb325f8b4d863cb2`
- Output profile: `Profiles/LC V1 S1.42AI-DIAG1R2 ShyGuy Isolation Applicability Repair.r2z`
- Gale profile name: `LC V1 S1.42AI-DIAG1R2 ShyGuy Isolation Applicability Repair`
- Readable snapshot: `ProfileSources/S1.42AI-DIAG1R2/`
- Local diagnostic project: `Patches/S142AIDiag1Isolation/S142AIDiag1Isolation.csproj`
- Diagnostic configuration source: `BuildSpecs/S1.42AI-DIAG1_OVERLAY.json` plus `BuildSpecs/generate_s142ai_diag1_spec.py`

The build remains directly derived from the exact full-normal `S1.42AI` profile. The failed R1 profile is evidence only and must not be used as the build base.

## Required repair binding

The new local-plugin bytes must contain the landed EndlessElevator applicability contract proven by `AnalysisEvidence/S1.42AI-DIAG1R1/ENDLESS_ELEVATOR_APPLICABILITY.json`:

- exact BepInEx dependency GUID `kite.ZelevatorCode` absent: this compat target is `NOT_APPLICABLE`; do not resolve the foreign CLR type and do not invalidate the complex-owner layer;
- dependency present: exact provider assembly `kite.ZelevatorCode`, type `ElevatorMod.Patches.EndlessElevator`, LethalMin owner/signature and patch installation remain required and fail closed;
- no generic `missing target => ignore` fallback is permitted.

All prior DIAG1 isolation configuration, exact Shy Guy identity restrictions, package set, static assertions and non-ShyGuy prevention scope remain unchanged apart from the newly compiled repaired diagnostic DLL and the new profile identity.

## Controller and validation boundary

`.github/workflows/s142ai-diag1r2-build-request.yml` is a one-shot request workflow. After merge to `main` it must:

1. require the current idle/no-candidate lifecycle and the landed applicability-repair authority;
2. regenerate and SHA-bind the existing disabled DIAG1 draft;
3. refuse any R2 output/snapshot reuse;
4. transform only the build identity into the exact enabled R2 request;
5. delete itself in the same controller-transition commit;
6. dispatch the canonical `profile-build.yml` workflow.

`RuntimeInbox/ACTIVE_BUILD.txt` must remain `S1.42AI-DIAG1R1` during the build because it is still evidence attribution for the last completed runtime run. Building R2 alone does not authorize gameplay.

R2 may become the active runtime candidate only after the generated artifact is repository-natively verified, including the exact static/materialized DIAG1 applicability semantics. Only that later coordinated lifecycle transition may update runtime attribution and set `runtime_test_outstanding = true`.
