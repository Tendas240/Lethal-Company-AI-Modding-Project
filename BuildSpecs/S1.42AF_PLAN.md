# S1.42AF — Path-Length-Safe Microwave Packaging Successor — Build Contract

**Status:** READY TO ARM  
**Accepted base:** S1.42AC — BCMER EventType Equal Distribution  
**Base profile:** `Profiles/LC V1 S1.42AC BCMER EventType Equal Distribution.r2z`  
**Base SHA-256:** `0ce58ab1fa0f0d76d6fbe1a4bff1dce9defc92e3d4b70cfb3056306e617e47d9`  
**Superseded runtime candidate:** S1.42AE — Functional Microwave Provider Contract Correction

## Purpose

Preserve the exact S1.42AE Functional Microwave provider-contract correction while removing the runtime launch blocker caused by the long Gale profile name pushing the nested `loaforc-loaforcsSoundAPI_LethalCompany` DLL path beyond the legacy Windows/Mono compatibility boundary.

Observed evidence on the runtime host:

- S1.42AE nested LC-binding DLL exists and is non-empty before launch;
- the same DLL still exists and is non-empty after the failed launch;
- the exact S1.42AE nested DLL path length is 262 characters;
- BepInEx/Mono fails to open that path with `DirectoryNotFoundException` / `FileNotFoundException` before the S1.42AE plugin can execute;
- accepted S1.42AC uses the same SoundAPI dependency family and previously passed the preloader with a shorter 247-character nested path.

This is packaging/path-length evidence, not a rejection of the S1.42AE Microwave provider logic.

## One-variable successor design

Build S1.42AF directly from accepted S1.42AC, not from S1.42AE.

Preserve the exact S1.42AE local plugin source/project and archive path:

- project: `Patches/S142AECodeRebirthMicrowaveSpawnTuning/S142AECodeRebirthMicrowaveSpawnTuning.csproj`;
- archive path: `BepInEx/plugins/S142AECodeRebirthMicrowaveSpawnTuning/S142AECodeRebirthMicrowaveSpawnTuning.dll`.

Do not change the plugin code, GUID, assembly name, version, dependency contract, provider validation, or `SpawnScale = 0.5f`. Reusing the same project is deliberate: S1.42AF is intended to isolate only Gale profile/package identity length while preserving the exact functional payload under test.

Use the path-length-safe Gale profile identity:

`LC V1 S1.42AF Microwave Fix`

On the validated runtime host, the previously failing nested LC-binding path is expected to fall from 262 characters to approximately 226 characters, providing meaningful margin below the legacy 260-character boundary.

## Patch Safety Review

This build introduces **no new runtime patch surface**. It reuses the already-reviewed S1.42AE plugin unchanged.

- exact target/owner: unchanged from `BuildSpecs/S1.42AE_PLAN.md`;
- smallest safe surface: unchanged; validate exact Functional Microwave provider contract and scale only the 18 Moon/tag curves;
- inherited/base lifecycle: unchanged;
- secondary responsibilities: unchanged;
- reverse/adjacent behavior: unchanged;
- forbidden broader alternative: do not alter SoundAPI packages, BepInEx/Mono, Gale internals, or unrelated configs merely to compensate for the long profile path;
- runtime acceptance: first prove preloader/main-menu success under the shorter profile path, then apply the same S1.42AE deterministic provider/regression gate.

## Build gate

The repository-native builder must:

1. build from the guarded S1.42AC profile/SHA only;
2. compile the unchanged S1.42AE plugin project in Release;
3. inject only `BepInEx/plugins/S142AECodeRebirthMicrowaveSpawnTuning/S142AECodeRebirthMicrowaveSpawnTuning.dll`;
4. set Gale profile name to exactly `LC V1 S1.42AF Microwave Fix`;
5. preserve package/config state outside generated profile-name metadata;
6. assert `Functional Microwave | Allow Editing Config = true`;
7. assert `Functional Microwave | Volume = 0.15`;
8. run archive/profile diff QC;
9. record the generated profile SHA-256 and plugin DLL SHA-256;
10. return `BuildSpecs/current.json` to a disabled guard after successful generation.

Expected archive delta versus accepted S1.42AC:

- changed existing member: only `export.r2x` profile-name metadata;
- added member: only the unchanged S1.42AE plugin DLL;
- zero mod-state/package/config drift.

## Runtime gate

After build success and controller/lifecycle promotion to active candidate:

1. import through the canonical Gale replacement helper;
2. require the two SoundAPI materialization contracts to pass;
3. verify the exact nested LC-binding DLL exists and is non-empty under the shortened profile path;
4. start the game and require BepInEx preloader/main menu/lobby success;
5. require `S1.42AE CodeRebirth Microwave Spawn Tuning 1.0.0` to load — the preserved plugin identity is expected and intentional;
6. require dependency versions CodeRebirth `1.6.9`, DawnLib/Dusk `0.9.25`;
7. require provider marker `PrioritiseMoons=true, MoonCurves=18, InteriorCurves=18`;
8. require both exact keyset markers;
9. require final marker that 18 Moon/tag curves were scaled by `0.5` and 18 Interior/tag curves were validation-only;
10. require normal moon/interior generation and ordinary gameplay without new fatal/project-critical regression.

S1.42AF is not accepted until this runtime gate passes.
