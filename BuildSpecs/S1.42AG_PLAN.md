# S1.42AG — Mouth Dog Pikmin One-Way Protection

**Status:** PLANNED / NOT BUILT / NOT ARMED  
**Guarded base:** `Profiles/LC V1 S1.42AF Microwave Fix.r2z`  
**Guarded base SHA-256:** `6a82a42bfe010767f4f39aab4d108fa45268407d9658a3e2410162cf9f6f47d0`  
**Patch-boundary authority:** `Current/131_MOUTHDOG_PIKMIN_PATCH_BOUNDARY_AND_SUCCESSOR_PLAN.md`  
**Exact decompile evidence:** `Current/130_LETHALMIN_1.1.108_MOUTHDOG_SOURCE_CONTRACT_DECOMPILE.txt`

## Objective

Implement the smallest evidence-backed asymmetric compatibility correction for the inherited LethalMinNightly 1.1.108 Mouth Dog / Eyeless Dog -> Pikmin gap.

Required gameplay contract:

- Mouth Dog / Eyeless Dog -> Pikmin targeting/bite/grab/kill pipeline is prevented before Pikmin state mutation.
- Pikmin -> Mouth Dog combat remains native LethalMin behavior.
- Mouth Dog behavior toward players remains native.

## Exact planned code delta

Modify `Patches/S139CompatibilityFixes/Plugin.cs` to install a `Priority.First` prevention-only prefix on exact declared:

`LethalMin.MouthDogPikminEnemy.DoCheckInterval()`

Installation must validate:

- exact type `LethalMin.MouthDogPikminEnemy`;
- exact declared method `DoCheckInterval`;
- instance method;
- return type `void`;
- zero parameters;
- non-null implementation body;
- `DeclaringType` exactly the resolved MouthDog adapter type.

If any contract check fails, log an error and install no guessed fallback.

Prefix behavior:

- return `false` to suppress only the MouthDog adapter's Pikmin bite-dispatch check;
- do not inspect or mutate Pikmin state;
- do not disable the MouthDog adapter;
- do not patch `PikminEnemy.Update`, `EnemyAI`, generic collision handling or scene-wide targeting;
- do not alter the existing common `PikminAI.GrabPikmin(Transform,float,int)` guards for Crawler/Thumper and Baboon Hawk unless implementation review reveals a strictly necessary logging-only clarification.

## Preserved behavior

The following must remain unchanged from S1.42AF:

- full mod list and versions;
- all LethalMin config values, including Eyeless Dog Bite Cooldown 5.5 and Bite Limit 7;
- native Pikmin -> Mouth Dog attack/latch/death lifecycle;
- Mouth Dog -> player behavior;
- S1.42AF Microwave contract and path-length-safe packaging;
- all other local compatibility fixes.

## Planned build form

The build must use the repository-native profile/build pipeline with S1.42AF as the guarded base and only the rebuilt local compatibility plugin as the functional delta.

No profile/config workaround is authorized for the Mouth Dog issue.

## Required candidate assertions

A built S1.42AG candidate should prove at packaging/build time that:

- the expected base SHA-256 is S1.42AF's accepted hash;
- the rebuilt `S139CompatibilityFixes` plugin is included;
- the exact MouthDog guard initialization marker is present in source/build evidence;
- LethalMin remains version 1.1.108;
- the profile does not change `Eyeless Dog Bite Cooldown` or `Eyeless Dog Bite Limit`;
- no unrelated mod/config delta appears.

## Required runtime test after a successful build

A future S1.42AG runtime test must include all of the following:

1. Reach a live Mouth Dog / Eyeless Dog encounter with Pikmin nearby during a Dog lunge.
2. Confirm no LethalMin Mouth Dog `Biting N Pikmin` event is produced for those nearby Pikmin.
3. Confirm no affected Pikmin receives MouthDog `GrabPikmin(..., 2.5f, 5)` consequences: no `EnemyAttackMouth` attachment, no `IsGrabbedByEnemy`/2.5-second death-timer state and no resulting work-state warning burst attributable to the Dog interaction.
4. Confirm Pikmin can still attack/latch to the Mouth Dog and native enemy death/unlatch cleanup remains functional.
5. Confirm the Mouth Dog can still attack a player normally.
6. Confirm normal startup, moon/interior generation and the previously accepted S1.42AF Microwave runtime markers remain healthy.

If the interaction cannot be exercised in the run, the candidate is not runtime-accepted merely from clean startup.

## Runtime-test UX

When this candidate is eventually built and marked runtime-test-outstanding, the same response that tells the user what to test must include:

- the canonical repository-driven Gale replacement/import PowerShell one-liner when import is required;
- the exact build-specific self-contained PowerShell runtime-log uploader.

## Current controller rule

This plan file does not arm or build S1.42AG. `BuildSpecs/current.json` remains disabled until the explicit implementation/build segment.
