# 77 — S1.42U Build Verification: BCMER 1.71.0 Reactivation Gate

**Date:** 2026-09-04  
**Build:** S1.42U — BCMER 1.71.0 Reactivation Gate  
**Build verdict:** **PASS — repository build verified; runtime validation still required**

## Role separation

Last fully accepted full normal gameplay baseline remains:

**S1.41 — BCMER Reactivation**

Newest runtime-accepted technical descendant remains:

**S1.42T — Normal Enemy Restore**

S1.42U is now the newest **built candidate**, but it is **not runtime accepted yet**.

## Build identity

Base profile:

`Profiles/LC V1 S1.42T Normal Enemy Restore.r2z`

Base SHA-256:

`a2714d04777edc95490398367c9dad2e320b44b664e20e9fe0b0f85d6a5fea10`

Output profile:

`Profiles/LC V1 S1.42U BCMER 1.71.0 Reactivation Gate.r2z`

Output SHA-256:

`ff5fdebf22fefdd5515b95677174290f9666e491447138f074e5b65673173969`

Profile name:

`LC V1 S1.42U BCMER 1.71.0 Reactivation Gate`

GitHub Actions build run:

`33818241873`

Result:

**success**

Build request commit:

`c01e3ef3b8ec0516dce19294cf6f78327304f892`

Canonical generated result:

`Current/AUTO_BUILD_RESULT.json`

Readable snapshot:

`ProfileSources/S1.42U/`

## Exact build delta

Archive members:

**331**

Changed existing members:

- `export.r2x`

Added members:

**none**

Removed members:

**none**

Exact package-state change:

- `SoftDiamond-BrutalCompanyMinusExtraReborn`
- version `1.71.0`
- `enabled: false -> true`

No package addition/removal, no config patch, no local plugin build and no compatibility-code change was included.

Because the repository builder permits only the declared changed members, every non-`export.r2x` archive member remained byte-identical to S1.42T.

## Verified preserved invariants

Compatibility diagnostics:

`BepInEx/config/tendas.s139.compatibilityfixes.cfg`

- `Isolated Enemy Regression = false`

LethalMin:

`BepInEx/config/NoteBoxz.LethalMin.cfg`

- `Thumper Bite Limit = 3`
- `Crawler` remains absent from `Attack Blacklist`
- `Puffer Can Poison Pikmin = false`

Compatibility plugin:

`BepInEx/plugins/Tendas-S139CompatibilityFixes/S139CompatibilityFixes.dll`

SHA-256 remains:

`3fd38c0e8ff76b55c5c335cd9eb867e254a422caea2287fb95d46447e2167960`

BCMER configuration carried forward unchanged. Spot-check hashes still match the canonical S1.42C restore baseline, including:

- `BrutalCompanyMinusExtraReborn/AllEnemies.cfg` = `b0c7d4e16496f8a4014bf8ff4011e179c5012d9a44cb2c5f756e7189d1e3fbcf`
- `BrutalCompanyMinusExtraReborn/CoreProperties.cfg` = `d828cd42c11c1a723bfb56fe36721c4b4092471c4102f41cc62ad926a5ae7de8`
- `BrutalCompanyMinusExtraReborn/LevelProperties.cfg` = `e40fc3d8ce7e3bf84a565fef94245d93f2364d26d4d8e6b1e2dc7bdb1b5776dc`

## Controller state after build

`BuildSpecs/current.json` is disarmed:

- `enabled = false`
- `build_id = IDLE_AFTER_S1.42U_BUILD_AWAITING_RUNTIME_VALIDATION`
- base = S1.42U
- base SHA-256 = `ff5fdebf22fefdd5515b95677174290f9666e491447138f074e5b65673173969`

Runtime router:

`RuntimeInbox/ACTIVE_BUILD.txt = S1.42U`

This ensures the next ingested normal runtime log is attributed to the correct candidate.

## Runtime gate — still open

S1.42U must not be promoted until runtime evidence confirms:

1. main menu/startup succeeds;
2. exact BCMER 1.71.0 is runtime-active;
3. normal non-isolated enemies remain available/spawn normally;
4. no crash/freeze;
5. `Work state with no task assigned!` remains 0;
6. `Leader is null when following` remains 0;
7. no new S1.39 Compatibility Fixes error/exception flood;
8. no BCMER-driven catastrophic event/system regression;
9. accepted Pikmin interaction behavior remains intact;
10. a fresh full runtime log is ingested and compared against S1.42T.

A heavy Baboon-Hawk stress retest is not required solely because BCMER was re-enabled; reopen that focused gate only if new runtime evidence indicates a regression.

## Immediate user action

Import/run:

`Profiles/LC V1 S1.42U BCMER 1.71.0 Reactivation Gate.r2z`

Perform a normal gameplay run with BCMER active, then upload the complete `LogOutput.log` to:

`RuntimeInbox/Current/`

The repository runtime-ingest workflow will route it to S1.42U using `RuntimeInbox/ACTIVE_BUILD.txt`.

Keep the existing S1.42T raw log until this BCMER-on comparison gate is closed.
