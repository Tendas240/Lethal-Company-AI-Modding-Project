# S1.42AK-SCRAPDIAG1 Runtime Placement Finding

**Date:** 2026-09-18  
**Status:** VALID DIAGNOSTIC CAPTURE / INVESTIGATION CLOSED / NO GAMEPLAY DELTA  
**Accepted gameplay baseline:** S1.42AK — unchanged  
**Diagnostic:** S1.42AK-SCRAPDIAG1 — diagnostic-only, never a gameplay base  
**Runtime evidence:** `RuntimeEvidence/S1.42AK-SCRAPDIAG1/20260918T200602Z/`  
**Runtime log SHA-256:** `e32e7d9b858fd78c046d5206c519a7709b3dcd5b2b8c432500233f51c6f13348`

## Question

Close the LC Office Scrap Quantity/Distribution Investigation by determining whether the repeated impression of sparse scrap is explained by a materially low generated count or by a strong spatial placement/clustering defect that warrants a narrow gameplay change.

## Diagnostic validity

The ingested Offense run satisfies the complete runtime gate from `Current/162_S1.42AK_SCRAPDIAG1_RUNTIME_ACTIVATION.md`:

- `[AJDIAG1] ARMED` appears;
- `[AJDIAG1] SELECTED Offense LC Office / OfficeDungeonFlow` appears;
- `[ScrapPlacementDiag] ARMED exact RoundManager.SpawnScrapInLevel read-only postfix` appears;
- snapshot A count is 23;
- snapshot B count is 23;
- `[ScrapPlacementDiag] STABLE id-set count=23` appears;
- four `[ANCHOR]` records appear;
- one `[ITEM]` record exists for every member of the stable 23-object set;
- `[ScrapPlacementDiag] COMPLETE stable=23` appears;
- no `[ScrapPlacementDiag] REFUSED` or `INCONCLUSIVE` marker appears.

The capture is therefore valid for placement interpretation.

## Quantity evidence

The same run records BCMER `scrapAmountMultiplier = 1`, `Number of scrap to spawn: 18`, then replication batches of 18 and 3 scrap values. The stable scene scan contains 23 objects whose `itemProperties.isScrap == true`.

Reconciliation with replication/tracker evidence identifies 21 relevant generated/replicated interior scrap objects. The two remaining stable scrap-flagged objects are the LC Office `Upturned Apparatus` map apparatus and the ship `Crisp Dollar Bill`.

This reinforces the earlier finding that LC Office does not exhibit a reproducible low-count regression. Its base target of 18 is higher than the 14-15 targets seen in the earlier Office diagnostics and nearby normal Offense comparison evidence.

## Placement evidence

The 21 relevant interior objects form three clear height bands:

- lower, approximately Y -253.396 to -251.782: **4 items**;
- middle, approximately Y -217.842 to -212.563: **11 items**;
- upper, approximately Y -180.361 to -178.435: **6 items**.

Support-collider evidence spans **13 distinct LevelGenerationRoot tile roots**: `BigRoom(Clone)`, `ConcreteIntersection4(Clone)`, `ConcreteSecureDoor(Clone)`, `ConcreteStraight(Clone)`, `ControlRoom(Clone)`, `LargeRoom(Clone)`, `LargeRoom2(Clone)`, `LongRoom(Clone)`, `OfficeStartRoom(Clone)`, `ServerRoom(Clone)`, `SmallIntersection4(Clone)`, `URoom(Clone)`, and `VFanRoom(Clone)`.

The most repeated roots are `ConcreteIntersection4(Clone)` and `ConcreteStraight(Clone)` with four items each, followed by `LargeRoom(Clone)` with three. The closest pair in the analyzed 21-item set is still roughly 10.25 world units apart (`Vent` and `Friendship ender`), so the capture does not show stacked or near-identical placement positions.

Nearest-EntranceTeleport-by-Y correlation is 15 objects to `EntranceTeleportB(Clone)` and 6 to `EntranceTeleportA(Clone)`. This remains diagnostic context only and is not treated as an authoritative floor label.

## Interpretation

The runtime evidence does not reproduce either mechanism that would justify a narrow gameplay correction: a materially low generated scrap count or a strong one-floor, one-room or near-identical-position concentration.

The sparse-local-density impression remains compatible with LC Office's large, multi-level layout: globally adequate scrap is spread across many rooms and height bands, so individual traversal branches can feel empty. No narrow faulty placement owner is established.

## Decision

**Close the LC Office Scrap Quantity/Distribution Investigation with no gameplay delta.**

- Do not increase LC Office scrap quantity.
- Do not add a broad placement/distribution patch.
- Keep accepted S1.42AK byte-for-byte unchanged.
- Keep S1.42AK-SCRAPDIAG1 as completed diagnostic evidence only.
- Do not repeat the diagnostic solely for another sample unless new reproducible evidence reopens the question.
- Return runtime attribution to accepted S1.42AK.
- Leave `BuildSpecs/current.json` disabled.
- Select the next independent roadmap scope separately.

This decision does not promote the diagnostic selector/logger and does not alter any other deferred scope.
