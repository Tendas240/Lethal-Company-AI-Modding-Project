# S1.42AK-BMDSFIX1-DIAG1 — Profile-Index Mapping Reconciliation

**Date:** 2026-09-24  
**Status:** RECONCILIATION COMPLETE / PROFILE INDEX GREEN / EXACT-HEAD KNOWLEDGE GATE GREEN / GAMEPLAY STATE UNCHANGED  
**Accepted gameplay baseline:** S1.42AK — unchanged  
**Active gameplay candidate:** S1.42AK-BMDSFIX1 — unchanged / not accepted

## Context

Exact-byte DIAG1 publication integration PR `#155` merged successfully to `main` as `0b3f5347fd642855122ca8ef9ef61eaa7daf6845` after the exact PR-head Knowledge Architecture gate passed.

The direct main Knowledge Architecture run `36054400312` / #732 on that exact merge commit also passed, including artifact-byte integrity and lifecycle semantics.

The separate automatic profile-index run `36054400269` / #19 then failed in its `Find and index changed profiles` step before writing or committing any repository change.

## Root cause

`BuildSystem/index_profile.py` intentionally refuses a current `Profiles/*.r2z` file when its profile path has no canonical build mapping in either `Profiles/EXPECTED_HASHES.json` or `Current/BUILD_LINEAGE.json`.

The newly integrated inactive diagnostic profile

`Profiles/LC V1 S1.42AK-BMDSFIX1-DIAG1 Deterministic Deep Sewers Selector.r2z`

was exact and already had its readable snapshot, but its canonical path/build/hash mapping had not been added to `Profiles/EXPECTED_HASHES.json` before merge.

This was a metadata/index-routing defect only. The failed run stopped before the indexer could modify the worktree or create a follow-up commit.

## Repair and verified closure

Repair PR `#156` added only the diagnostic-profile mapping:

- build ID: `S1.42AK-BMDSFIX1-DIAG1`;
- profile SHA-256: `31c24a3752aefe040b74c5dc2c3b7f677c17068a91f8c8e2893ace615050b78e`;
- canonical readable snapshot: `ProfileSources/S1.42AK-BMDSFIX1-DIAG1/`.

This follows the existing `EXPECTED_HASHES` pattern used for diagnostic profiles such as `S1.42AK-SCRAPDIAG1` and `S1.42AK-BMGHDIAG1/2` rather than promoting DIAG1 into gameplay lineage.

Verified repair chain:

- repair PR `#156` exact head: `d908dce4e2e685402445d235d6df4d9c871e513f`;
- repair PR Knowledge Architecture run `36054768395` / #734: **success**;
- repair main integration commit: `c7907cad6628c35ecf451bca17080cb29f46f7f7`;
- repaired automatic profile-index run `36054835453` / #20: **success**;
- profile-index follow-up commit: `4697d52cd7ce377324015b051f30901b29a63a69`;
- that follow-up commit adds only `ProfileSources/S1.42AK-BMDSFIX1-DIAG1/PROFILE_INDEX_RESULT.json`;
- indexed build ID: `S1.42AK-BMDSFIX1-DIAG1`;
- indexed profile SHA-256: `31c24a3752aefe040b74c5dc2c3b7f677c17068a91f8c8e2893ace615050b78e`;
- indexed archive members: `338`;
- indexed snapshot summary: `338` entries / `331` text entries;
- build-ID resolution: `EXPECTED_HASHES`;
- exact-head Knowledge Architecture workflow dispatched for `4697d52cd7ce377324015b051f30901b29a63a69`: run `36054861589` / #736 / **success**.

The original profile-index #19 failure is therefore resolved without rebuilding or altering the reviewed DIAG1 profile bytes.

## Preserved lifecycle boundary

The repair and index reconciliation changed no gameplay/config/package/profile/plugin bytes and did not arm the diagnostic:

- S1.42AK remains accepted;
- S1.42AK-BMDSFIX1 remains the active gameplay runtime candidate / not accepted;
- `runtime_test_outstanding=true` remains the regular BMDSFIX1 Black Mesa x DeepSewersFlow target gate;
- `BuildSpecs/current.json` remains disabled and pinned to the exact BMDSFIX1 parent;
- `RuntimeInbox/ACTIVE_BUILD.txt` remains `S1.42AK-BMDSFIX1`;
- `Current/AUTO_BUILD_RESULT.*` remains bound to BMDSFIX1;
- DIAG1 remains **DIAGNOSTIC ONLY / NEVER ACCEPT / NOT ARMED**;
- Gale and runtime remain unchanged.

## Result

The DIAG1 exact-byte publication is now repository-integrated, canonically indexable, and exact-head CI-green. The profile-index incident is closed.

## Next bounded gate

A separate DIAG1 runtime-activation checkpoint may now be considered. Such activation must remain diagnostic-only and may provide supporting deterministic Black Mesa x DeepSewersFlow evidence, but it cannot itself accept BMDSFIX1 or waive the regular exact-byte BMDSFIX1 qualification contract.
