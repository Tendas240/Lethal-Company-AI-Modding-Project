# 58 — S1.42P Runtime — Exact 20→18 Pikmin Loss / Dead-Hawk Reacquisition

**Date:** 2026-09-03  
**Game:** Lethal Company V81  
**Evidence build:** S1.42P — Baboon Hawk Exact FinishTask Recovery

## Primary evidence

Evidence:

`RuntimeEvidence/S1.42P/20260903T181706Z/`

Raw log:

`RuntimeEvidence/S1.42P/20260903T181706Z/raw/LogOutput.log`

Log SHA-256:

`d656095fb874a415a1bd2377c0411339d3d6eb002dce4ec3f6216e879294127f`

Raw-log Git blob SHA:

`47c05ca602b3b1c37430a8e24e77a2c16a702f26`

Size:

1,699,166 bytes.

## Verdict

**S1.42P = PARTIAL / FAIL**

The runtime-confirmed `LethalMin.PikminAI.FinishTask()` method resolves and executes correctly, but S1.42P does **not** solve the Baboon-Hawk death regression.

Two separate failure mechanisms are confirmed:

1. the 4.0 m proximity-only selector misses a real Hawk attacker;
2. Pikmin that are successfully finalized can immediately rediscover the already-dead Baboon Hawk and assign a fresh `AttackEnemy` task.

The user's observed 20 -> 18 following-count drop is reproduced exactly in the log state.

## Exact 20 -> 18 confirmation

Before the fight, exactly 20 Yellow Pikmin were assigned to leader `MasaeAhmya (0)`.

The 20 names were:

- Yellow Pikmin_hcRGph
- Yellow Pikmin_PerDu
- Yellow Pikmin_3wnH
- Yellow Pikmin_VhJAgo
- Yellow Pikmin_IkVZWQe
- Yellow Pikmin_Aq9v9
- Yellow Pikmin_apYy5
- Yellow Pikmin_LuMKh
- Yellow Pikmin_G5l3h
- Yellow Pikmin_ruCpzY
- Yellow Pikmin_khCd
- Yellow Pikmin_XYErHj
- Yellow Pikmin_pxom
- Yellow Pikmin_2nE2lyy
- Yellow Pikmin_8LNgEB
- Yellow Pikmin_iTSzvu7
- Yellow Pikmin_4BXv3
- Yellow Pikmin_lMw922
- Yellow Pikmin_6iu7kr
- Yellow Pikmin_GOOb5H

Immediately before session teardown, exactly **18/20** are again leader-assigned.

The two that are not recovered are:

- **Yellow Pikmin_hcRGph**
- **Yellow Pikmin_ruCpzY**

This exactly matches the user's observed following count of 18 after beginning with 20.

## Hawk death timeline

Baboon Hawk death:

`18:14:53.2111643`

The compatibility patch then invokes native FinishTask on three Pikmin:

- `Yellow Pikmin_hcRGph` — 2.25 m
- `Yellow Pikmin_apYy5` — 3.78 m
- `Yellow Pikmin_khCd` — 1.66 m

Aggregate marker:

`native-finished 3/3 Pikmin in 4.0m death-release zone; total Pikmin registry candidates=46`

Therefore the exact runtime-confirmed FinishTask resolver itself is working.

## Failure A — real attacker missed by 4.0 m selector

A fourth Pikmin was continuously attacking the same Hawk:

**Yellow Pikmin_ruCpzY**

It was already latched/attacking before death, but it is absent from the 3/3 FinishTask list.

After Hawk death it continues:

`Hitting enemy with: 0.03`

for **168 post-death hit entries**.

First post-death hit:

`18:14:53.4867195`

Last post-death hit:

`18:16:17.7764323`

That is approximately **84.565 seconds after Hawk death**.

It does not receive normal recovery during the run. Its first later unlatch/removal occurs only at session teardown around `18:16:18.4246328`.

Conclusion:

**A fixed 4.0 m world-distance test is not a reliable identity test for the actual AttackEnemy participants.**

Do not fix this merely by widening the radius. The next implementation should identify the Pikmin whose current attack task/target is the dying Hawk, or otherwise use a direct target identity route.

## Failure B — FinishTask works, then dead Hawk is reacquired

The three selected Pikmin receive native FinishTask successfully.

However the dead `BaboonHawkEnemy(Clone)` remains discoverable to LethalMin's enemy-selection logic.

Reacquisition after death:

- `Yellow Pikmin_apYy5`: sets `AttackEnemy` again at `18:14:53.3713026`
- `Yellow Pikmin_khCd`: sets `AttackEnemy` again at `18:14:53.5783999`
- `Yellow Pikmin_hcRGph`: sets `AttackEnemy` again at `18:14:53.7666989`

Post-death hit behavior:

### Yellow Pikmin_apYy5

- FinishTask executes successfully.
- Dead Hawk is reacquired.
- 5 post-death hit entries occur.
- Last post-death hit is about 4.499 s after death.
- It later goes out of range, recovers, carries the corpse, and becomes leader-assigned again.

### Yellow Pikmin_khCd

- FinishTask executes successfully.
- Dead Hawk is reacquired.
- 2 post-death hit entries occur.
- Last post-death hit is about 4.201 s after death.
- It later goes out of range, carries the corpse, and becomes leader-assigned again.

### Yellow Pikmin_hcRGph

- FinishTask executes successfully.
- It immediately reacquires the dead Hawk and sets a fresh `AttackEnemy` task.
- No later leader assignment occurs before teardown.
- Its last meaningful runtime state after the death sequence is the new `AttackEnemy` task against the dead Hawk.
- This is one of the exact two missing Pikmin.

Conclusion:

**Native FinishTask is necessary but not sufficient.**

The next fix must prevent already-dead Baboon Hawks from being selected/reselected as valid Pikmin attack targets.

## Corpse behavior — PASS

The SellBodies corpse side remains healthy.

Confirmed:

- Dead Baboon Hawk body is created.
- Living Baboon Hawk corpse guard fires:
  `Living Baboon Hawk ignored corpse item BaboonHawkBody(Clone) / Dead Baboon Hawk.`
- 13 Yellow Pikmin join the corpse carry.
- The body creates a valid `ToOnion` route.
- At `18:16:01.6912919`:
  `Sucking item BaboonHawkBody(Clone) into onion`
- The body reaches the Onion successfully.

Do not regress this behavior.

## Other active acceptance checks

PASS / not reproduced:

- `Work state with no task assigned!`: **0**
- `Leader is null when following`: **0**
- previous Coroner `PlayerController was null` Jetpack flood: **0**

The Thumper/Pikmin guard also continues firing during the run, showing the existing Crawler/Thumper protection remains active.

## General log noise

The log contains many warnings/errors unrelated to this Hawk regression.

Notable high-volume warning noise includes Coroner collider/index warnings before the focused Hawk fight. These are distinct from the previously fixed `PlayerController was null` Jetpack spam.

No S1.42P startup crash or FinishTask reflection failure occurred.

The Hawk regression is independently demonstrated by direct LethalMin task/hit/leader evidence and does not depend on those unrelated warnings.

## Root cause

S1.42P solved the wrong-method-name problem from S1.42O but exposed two remaining design defects:

1. **attacker selection is proximity-based rather than target-identity-based**;
2. **dead Baboon Hawk target eligibility remains active after KillEnemy**, allowing immediate AttackEnemy reacquisition.

## Required S1.42Q direction

Do **not** restore normal enemies or BCMER yet.

Do **not** simply increase the 4.0 m radius.

Do **not** return to direct low-level `RemoveCurrentTask()`.

The successor should preserve native `PikminAI.FinishTask()`, but add both:

1. an exact/direct way to select every Pikmin whose active enemy target is the dying Baboon Hawk;
2. a narrow guard that makes an already-dead Baboon Hawk invalid for future Pikmin AttackEnemy acquisition while leaving:
   - Pikmin -> living Hawk attack/latch/kill enabled;
   - living Hawk -> Pikmin blocked;
   - Dead Baboon Hawk corpse carry enabled;
   - living Hawks unable to pick up the corpse.

If exact LethalMin target-selection members cannot be established safely from existing evidence, add narrow runtime reflection diagnostics first rather than guessing method/field names.

## Current gate state

EnemyIsolation remains enabled.

Exact BCMER 1.71.0 remains disabled.

Normal enemy restore remains deferred.

S1.42P should not be accepted as a gameplay fix.
