# S1.42AG Runtime Rejection — Remaining Mouth Dog Targeting Path

**Date:** 2026-09-06  
**Status:** RUNTIME REJECTED / PARTIAL FIX / TARGETED ANALYSIS REQUIRED  
**Accepted baseline:** S1.42AF — Path-Length-Safe Microwave Packaging  
**Rejected candidate:** S1.42AG — Mouth Dog Pikmin One-Way Protection

## Runtime evidence

- Runtime evidence: `RuntimeEvidence/S1.42AG/20260906T085500Z/`
- Raw log SHA-256: `3e8ca4c8fe045bbd2c62576dbbd5aaba2a226990e6b4af4149481f2672c35dfe`
- User-observed failure: a Mouth Dog visibly targeted and attacked a Purple Pikmin that was carrying scrap. The Purple Pikmin was not visibly harmed by the attack, but the Dog should not have selected/attacked it at all under the S1.42AG contract.
- Reverse-direction Pikmin -> Mouth Dog combat was **not actively exercised** in this run: no Pikmin was deliberately thrown/assigned onto the Mouth Dog for an attack/latch test. Nearby follower Pikmin merely remained passive, which is expected normal behavior and is not evidence of a reverse-direction defect.

## What S1.42AG did fix

The candidate patch armed successfully:

- `[MouthDogPikminGuard] Patched exact declared LethalMin.MouthDogPikminEnemy.DoCheckInterval() with a Priority.First prevention-only prefix.`

During the Mouth Dog encounter the guard executed and logged that it blocked `DoCheckInterval()` before Pikmin target collection, bite RPC dispatch, `GrabbedPikmin` bookkeeping, or `GrabPikmin` state mutation.

The prior harmful LethalMin bite/grab state-mutation signature is absent from this run:

- no `Biting N Pikmin` event was observed for the Mouth Dog interaction;
- no `EnemyAttackMouth` attachment marker was observed for the interaction;
- `Work state with no task assigned!` count is `0` versus `707` in the accepted S1.42AF evidence that exposed the original gap;
- no `[Error  :S1.39 Compatibility Fixes]` marker;
- no fatal marker.

This proves S1.42AG is a real partial fix: the exact LethalMin `MouthDogPikminEnemy.DoCheckInterval()` bite/grab/death-timer mutation path is successfully prevented.

## Why S1.42AG is rejected

The runtime contract requires the Mouth Dog / Eyeless Dog -> Pikmin direction to be blocked before target/attack/bite/grab state mutation, not merely to prevent the final harmful grab/death-timer state.

The user directly observed a Mouth Dog select and attack a scrap-carrying Purple Pikmin. The same encounter contains repeated native Mouth Dog noise-targeting diagnostics such as:

- `dog 'MouthDog(Clone)': Heard noise!`
- `Mouth dog targetPos ...`
- `Dog lastheardnoisePosition ...`

These lines do not by themselves prove which object generated each noise or establish the complete root cause, but they are consistent with a second/native Mouth Dog targeting path outside the patched LethalMin `DoCheckInterval()` dispatcher. The next step must identify that remaining path from exact current code/runtime evidence rather than guessing.

The reverse-direction gate has **no pass/fail result from this run** because it was not deliberately tested. Passive follower Pikmin not independently attacking the Mouth Dog is expected behavior. A future candidate must validate Pikmin -> Mouth Dog attack/latch/death-unlatch cleanup with a deliberate player-directed attack test; this is not a current failure signal.

Therefore S1.42AG cannot be accepted because of the observed remaining Mouth Dog -> Pikmin targeting/attack behavior alone.

## Full-stack regression result

The inherited S1.42AF Functional Microwave contract remained healthy in this run:

- `PrioritiseMoons=true`;
- `MoonCurves=18`;
- `InteriorCurves=18`;
- all 18 Functional Microwave Moon/tag curves were scaled by `0.5`;
- all 18 Interior/tag curves were validation-only and not mutated.

The `loaforcsSoundAPI.LethalCompany.Patches.RoundManagerPatch::Reporting()` Harmony `TypeLoadException` is not an S1.42AG regression: the same marker already exists in the accepted S1.42AF runtime evidence.

## Lifecycle decision

**S1.42AG — RUNTIME REJECTED / PARTIAL FIX.**

S1.42AF remains the accepted full-normal-stack baseline.

Do not build a successor yet. The exact next scope is targeted analysis of the remaining Mouth Dog targeting/attack path that can still react to a Pikmin (especially a scrap-carrying Pikmin) despite the successful `MouthDogPikminEnemy.DoCheckInterval()` prevention guard.

Do not treat passive follower Pikmin behavior as a defect. Reverse-direction Pikmin -> Mouth Dog combat remains a **future deliberate runtime validation gate** for any successor candidate, not part of the present root-cause finding. Preserve the enabled `MouthDogPikminEnemy` adapter and native LethalMin lifecycle ownership; do not replace them with guessed state repair.
