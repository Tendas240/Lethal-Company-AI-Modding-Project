# S1.42Z Runtime Acceptance — Jetpack / Pikmin Retune

**Date:** 2026-09-04  
**Verdict:** ACCEPTED  
**Canonical accepted full-normal-stack baseline:** S1.42Z

## Accepted profile

Profile:

`Profiles/LC V1 S1.42Z Jetpack Pikmin Retune.r2z`

Gale profile name:

`LC V1 S1.42Z Jetpack Pikmin Retune`

Profile SHA-256:

`a030d4b280b4768f6859f6fea43981004c48f31060f100322206b6016a1477e4`

Build run:

`33874737048` — success

Automated build commit:

`267543634bb884bb447bf4bec320103ba75c9ff8`

Candidate record:

`Current/87_S1.42Z_BUILD_CANDIDATE_JETPACK_PIKMIN_RETUNE.md`

## Runtime evidence

Evidence root:

`RuntimeEvidence/S1.42Z/20260904T135820Z/`

Raw log:

`RuntimeEvidence/S1.42Z/20260904T135820Z/raw/LogOutput.log`

Raw LogOutput.log SHA-256:

`ca61e82e5a7d12f96dcb51849e291582df4d45568da4fa1e10b476551c897db8`

Coverage:

- 1,586,159 bytes;
- 16,094 lines;
- 15,325 parsed runtime events;
- 32 Error-severity events;
- Fatal = 0.

The runtime upload was successfully ingested by the repository automation. `RuntimeInbox/Current/` was cleared back to `.gitkeep` after ingestion.

## Technical gate — PASS

The fresh S1.42Z log proves the intended project-local code paths were present and armed:

- `S1.42Z Jetpack Acceleration 1.0.0` loaded;
- ButteRyBalance `0.7.0` validated;
- JetpackFixes `1.6.3` validated;
- More Ship Upgrades `3.14.1` validated;
- exact Jetpack marker confirmed local-player base acceleration `10 -> 18`;
- `S1.42Z CodeRebirth Aerial Defense Spawn Tuning 1.0.0` loaded;
- CodeRebirth `1.6.9` validated;
- DawnLib `0.9.25` validated;
- DawnLib.Dusk `0.9.25` validated;
- Air Control Unit provider `code_rebirth:air_control_unit` validated with exactly 18 moon/tag curves;
- G.R.E.G. / Advanced Airspace Control provider `code_rebirth:gunslinger_greg` validated with exactly 18 moon/tag curves;
- final transactional marker confirmed all 18 ACU curves and all 18 G.R.E.G. curves were scaled by `0.5` and no other map-object provider was modified;
- no aerial-defense contract-refusal marker was observed;
- EnemyIsolation runtime marker confirms diagnostic isolation is disabled;
- Compatibility Fixes Error marker = 0;
- Work state with no task assigned = 0;
- Leader is null when following = 0;
- unspawned NetworkObjectReference marker = 0;
- PikminNoticeZone regression marker = 0;
- Fatal = 0.

Neither S1.42Z project-local plugin emitted an Error-severity entry in the complete ingested log.

## User runtime acceptance

The user reported the tested S1.42Z behavior as satisfactory and explicitly stated that everything is in order from their side.

This closes the subjective balance portion of the gate:

- Jetpack base acceleration `18f` is accepted;
- Indoor Pikmin Spawn Chance `0.09` is accepted;
- Pikmin CarryStrength remains accepted at `3` for configured non-Purple types and `30` for Purple;
- Functional Microwave volume remains accepted at `0.15`;
- Immortal Snail tuning remains `Rarity = 40`, `Max Snails = 2`;
- normal gameplay/BCMER/Compatibility behavior observed by the user is accepted for this gate.

## Accepted S1.42Z tuning

### Jetpack

- project-local base acceleration `10f -> 18f`;
- Jet Fuel `18 / 18`;
- Jetpack Thrusters `25 / 20`;
- ButteRyBalance V49 handling/deceleration preserved;
- JetpackFixes collision/death/control safety preserved.

Jetpack DLL:

`BepInEx/plugins/S142ZJetpackAcceleration/S142ZJetpackAcceleration.dll`

SHA-256:

`9624de844ab3913605eab2c35d96d9d9dec17b34d77823b33aaa434488022add`

### LethalMin

- Indoor Pikmin Spawn Chance `0.09`;
- configured non-Purple CarryStrength `3`;
- Purple CarryStrength `30`.

### CodeRebirth aerial defense

- `code_rebirth:air_control_unit`: exact 18 curves ×0.5;
- `code_rebirth:gunslinger_greg`: exact 18 curves ×0.5;
- transactional all-or-nothing validation retained;
- no other map-object provider modified.

Aerial-defense DLL:

`BepInEx/plugins/S142ZCodeRebirthAerialDefenseSpawnTuning/S142ZCodeRebirthAerialDefenseSpawnTuning.dll`

SHA-256:

`7313501540c3945ee3782903b8bb328574a87587859fce30faa2a301b7f1d98b`

The ×0.5 operation is exact curve-amplitude/spawn-weight scaling. DawnLib subsequently evaluates and rounds the resulting spawn amount, so it is not a guarantee of mathematically exact half-counts in a short gameplay sample.

## Error-severity assessment

The log contains 32 Error-severity events, the same total as the preceding S1.42Y evaluation. The observed exception classes remain within the already monitored non-project-local/setup classes, including the known loaforcsSoundAPI/HarmonyX `TypeLoadException` and SoftMask/SoftMasking setup `NullReferenceException` class.

There is no S1.42Z project-local Error-severity source, no Fatal marker, and no evidence in this gate that justifies reopening those monitor-only issues.

Do not patch monitor-only error classes without stronger reproducibility or user-facing impact.

## Promotion decision

S1.42Z is promoted from candidate to the **canonical accepted full-normal-stack baseline**.

S1.42U remains useful as the previous pre-retune rollback artifact, but it is no longer the latest accepted baseline.

## Permanent invariants preserved

- exact BCMER `1.71.0` enabled; do not silently upgrade to 2.x;
- EnemyIsolation disabled;
- Compatibility Fixes `1.3.14`, DLL SHA-256 `3fd38c0e8ff76b55c5c335cd9eb867e254a422caea2287fb95d46447e2167960`;
- `BaboonBirdPikminEnemy` enabled;
- only narrow Hawk -> Pikmin entry points blocked;
- native inherited PikminEnemy death/unlatch/task lifecycle preserved;
- Pikmin -> Baboon Hawk attack remains allowed;
- Puffer -> Pikmin protection preserved;
- Thumper Bite Limit `3`;
- Crawler absent from LethalMin Attack Blacklist;
- accepted S1.42C-derived moon power/spawn baseline preserved;
- SpawnCycleFixes `Consistent Spawn Times = true`.

Never repeat the S1.42R approach of disabling the complete `LethalMin.BaboonBirdPikminEnemy` component merely to suppress one interaction.

## Next state

No successor build is armed by this acceptance.

`BuildSpecs/current.json` should remain disabled with S1.42Z as its base and an idle-after-acceptance identifier.

`RuntimeInbox/ACTIVE_BUILD.txt` remains `S1.42Z` until another runtime candidate is explicitly designated.

The next planned gameplay/config scope, subject to explicit user direction, is the deferred interior-probability work:

- equal effective probability for all currently installed interiors;
- preserve the same equal-probability rule automatically for future added interiors unless explicitly overridden.

Keep later deferred scopes separate unless the user explicitly groups them:

- CullFactory exceptions for `junkrooms` / `shatteredrooms`;
- Mausoleum fog reduction;
- Functional Microwave spawn rarity reduction;
- BCMER EventTypes fixed equal distribution `8 × 12.5%`;
- final long full-stack acceptance;
- AdditionalNetworking repair only with reproducible/user-facing evidence;
- LethalMin `DespawnLumiknulls()` repair only with stronger evidence;
- cosmetic documentation cleanup.
