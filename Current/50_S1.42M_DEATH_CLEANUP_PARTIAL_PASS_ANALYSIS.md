# 50 — S1.42M Baboon Hawk Death Cleanup Runtime — Partial Pass / Target-Selection Failure

**Date:** 2026-09-03  
**Game:** Lethal Company V81  
**Evidence build:** S1.42M

## Primary runtime evidence

Evidence:
`RuntimeEvidence/S1.42M/20260903T163446Z/`

Raw log:
`RuntimeEvidence/S1.42M/20260903T163446Z/raw/LogOutput.log`

Log SHA-256:
`0639d5cc04aa54f5d7943ef4689e0d705c818871b019287ca1a1cdc2aa2492fb`

## User-observed result

**PASS**
- Dead Baboon Hawk body appears.
- Pikmin can carry the Dead Baboon Hawk body to the Onion.

**FAIL**
- Pikmin that were attacking/latching the living Baboon Hawk still disappear when that Hawk dies.

## Log-confirmed S1.42M patch state

The intended v1.3.8 patch loaded:
- exact declared `BaboonBirdAI.KillEnemy(bool)` hook installed;
- exact declared `BaboonBirdAI.CanGrabScrap(GrabbableObject)` hook installed;
- exact declared `LethalMin.PikminAI.RemoveCurrentTask()` resolved successfully.

Critical death marker:
`[BaboonHawkDeathCleanup] Dying BaboonHawkEnemy(Clone): released 0/0 latched Pikmin before SellBodies can move the original enemy transform.`

This proves the failure is **not** that `RemoveCurrentTask()` could not be resolved. The S1.42M target-selection assumption was wrong: attacking/latching Pikmin are not discoverable as `PikminAI` children under the dying Baboon Hawk transform.

Immediately after the 0/0 marker, the same named Pikmin continue producing `Hitting enemy with: 0.03` activity against the stale dead attack target.

No `Leader is null when following` entries occurred.

## Corpse behavior result

The corpse-side fix is validated:
`[BaboonHawkCorpseGuard] Living Baboon Hawk ignored corpse item BaboonHawkBody(Clone) / Dead Baboon Hawk. Pikmin/player carrying remains allowed.`

The run later records LethalMin carrying/despawn behavior for `BaboonHawkBody(Clone)`, and the user explicitly confirmed successful transport to the Onion.

Therefore do **not** remove or redesign the S1.42M corpse guard.

## Root cause

**CONFIRMED implementation error in S1.42M target discovery**

S1.42M searched:
`baboon.GetComponentsInChildren<MonoBehaviour>(true)`

Result:
`0/0`

The latched/attacking Pikmin are maintained by LethalMin through attack/task state rather than as `PikminAI` components parented under the Hawk transform.

## S1.42N implementation direction

Use a one-shot, death-triggered resolver only.

Do **not** restore:
- continuous Update-driven scene scans;
- broad inherited LethalMin Harmony scanning;
- global per-frame enemy/Pikmin discovery.

Preferred candidate source:
`RoundManager.Instance.SpawnedEnemies`

LethalMinNightly 1.1.108 includes the Rev6 fix that adds mod-spawned Pikmin to the server SpawnedEnemies list. At Hawk death, filter that existing registry for exact/assignable `LethalMin.PikminAI` instances.

For the focused compatibility fix:
- release Pikmin close enough to the dying Hawk to represent active latch/attack participants;
- optionally retain direct target/reference checks if safely available;
- invoke exact `PikminAI.RemoveCurrentTask()`;
- log candidate count, release count, reason and distance;
- keep the already-passing corpse guard unchanged.

This is an event-driven death-time operation, not a continuous global scan.

## Temporary state remains binding

EnemyIsolation:
**enabled**

BCMER exact 1.71.0:
**disabled**

Restore baseline:
`Current/ENEMY_SPAWN_BASELINE_S1.42C.json`

Do not restore normal enemies or BCMER until the death-unlatch gate passes.

## Next gate

Build S1.42N from S1.42M with only the death-target resolver correction.

Runtime acceptance:
1. Pikmin can attack/latch living Baboon Hawk;
2. on Hawk death, attacking Pikmin remain visible/usable;
3. Dead Baboon Hawk body still appears;
4. body remains Pikmin-carryable to Onion;
5. living Hawks still ignore the corpse;
6. Hawk -> Pikmin ignore remains intact;
7. no leader-null loop.
