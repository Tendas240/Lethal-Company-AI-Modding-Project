# S1.42AH Handover Authority and Integrity Repair

**Status:** REPOSITORY-GOVERNANCE REPAIR CANDIDATE / GAMEPLAY BYTES UNCHANGED / CI REQUIRED  
**Date:** 2026-09-08  
**Starting main:** `7cd6e7c2f354de10fc02a81c12eaec67b4c458ff`  
**Last CI-validated parent before repair:** `4fd57f8c4b9d765680d132d27c44f00e9d59cfc2`  
**Knowledge Architecture run on that parent:** `34196364590` — SUCCESS  
**Gameplay baseline:** S1.42AF accepted  
**Active runtime candidate:** S1.42AH, runtime validation outstanding

## Why this repair exists

A repository-native handover audit found that the S1.42AH build/controller state was correct while several manual CURRENT/Knowledge authorities still contained pre-S1.42AH instructions. Examples included claims that no runtime test was pending, that S1.42AG remained the latest built artifact, and that the successor Patch Safety Review/implementation still had to happen.

The same audit found two structural gaps:

1. the profile indexer fell back to a filename-derived build id when a current profile was absent from `Profiles/EXPECTED_HASHES.json`, creating a duplicate `ProfileSources/LC_V1_S1.42AH_Mouth_Dog_Fix/` snapshot beside canonical `ProfileSources/S1.42AH/`;
2. the profile-index workflow committed that derived snapshot with `[skip ci]`, producing main `7cd6e7c2...` with no workflow run on that exact head even though its parent had green gates.

The existing validators checked routing/path/controller/provenance invariants but did not prove natural-language live-state agreement across Lifecycle, Knowledge Map, Roadmap, Work Queue and Artifact Evidence.

## Repair contract

This repair must not alter `.r2z` gameplay artifacts, project gameplay source, configs, runtime evidence, accepted/rejected decisions, or the S1.42AH runtime-test contract.

It performs only repository-governance/information-integrity changes:

- synchronize all current live authorities on accepted S1.42AF / active S1.42AH / runtime test outstanding;
- remove stale pre-build instructions from `CURRENT_STATE.selected_scope`;
- reduce volatile build/evidence duplication in the machine Knowledge Map;
- add machine-checkable `LIVE_STATE` markers to current human authorities;
- add `RepositoryTools/current_state_semantic_validator.py` and a negative self-test for stale live runtime instructions;
- model `ACTIVE_RUNTIME_CANDIDATE_PENDING` in the artifact evidence index and byte validator so profile/snapshot bytes are verified before runtime without inventing a runtime log;
- resolve current profile snapshot build ids through `EXPECTED_HASHES` or `BUILD_LINEAGE`, fail-closed for unregistered current `Profiles/` artifacts, and retain filename fallback only for legacy reference profiles;
- register S1.42AF/S1.42AG/S1.42AH hashes explicitly;
- remove `[skip ci]` from automatic profile-index commits so a generated `ProfileSources/**` main head receives its own Knowledge Architecture run;
- remove the duplicate filename-derived S1.42AH snapshot tree while preserving canonical `ProfileSources/S1.42AH/`.

## Post-repair lifecycle

After this repair is merged and the permanent gates are green on the exact resulting `main` (including any automatic follow-up commit), the gameplay lifecycle is unchanged:

- S1.42AF remains the sole accepted gameplay baseline.
- S1.42AH remains the active runtime candidate and is not accepted yet.
- No successor beyond S1.42AH is armed.
- The next gameplay action is the full-normal MouthDog/Pikmin runtime gate from `Current/139_S1.42AH_BUILD_CANDIDATE_MOUTHDOG_DUAL_PREVENTION.md`.

Do not accept S1.42AH from build/startup success alone.
