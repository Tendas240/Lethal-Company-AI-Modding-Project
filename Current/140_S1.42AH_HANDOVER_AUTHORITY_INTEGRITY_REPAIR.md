# S1.42AH Handover Authority and Integrity Repair

**Status:** REPOSITORY-GOVERNANCE REPAIR INTEGRATED / POST-MERGE VALIDATION PASS / GAMEPLAY BYTES UNCHANGED  
**Date:** 2026-09-08  
**Starting main:** `7cd6e7c2f354de10fc02a81c12eaec67b4c458ff`  
**Primary repair PR:** `#42`  
**Primary repair merge:** `4342d3bd3dfe9ec3a46dd54ce89c94a54601391e`  
**Primary merge Knowledge Architecture run:** `34204750427` — SUCCESS  
**Indexer follow-up:** `4146de7cc2217954db7ab7c64289647554830f2d` — legacy snapshot metadata only  
**Trigger-gap follow-up PR:** `#43`  
**Trigger-gap repair merge:** `c1bd196dee2b5943d3b94b06da272b5be5bafa6b`  
**Knowledge Architecture run on that exact merge:** `34205094473` — SUCCESS  
**Gameplay baseline:** S1.42AF accepted  
**Active runtime candidate:** S1.42AH, runtime validation outstanding

## Why this repair exists

A repository-native handover audit found that the S1.42AH build/controller state was correct while several manual CURRENT/Knowledge authorities still contained pre-S1.42AH instructions. Examples included claims that no runtime test was pending, that S1.42AG remained the latest built artifact, and that the successor Patch Safety Review/implementation still had to happen.

The same audit found structural gaps:

1. the profile indexer fell back to a filename-derived build id when a current profile was absent from `Profiles/EXPECTED_HASHES.json`, creating a duplicate `ProfileSources/LC_V1_S1.42AH_Mouth_Dog_Fix/` snapshot beside canonical `ProfileSources/S1.42AH/`;
2. the profile-index workflow committed that derived snapshot with `[skip ci]`, producing main `7cd6e7c2...` with no workflow run on that exact head even though its parent had green gates;
3. after `[skip ci]` was removed, a legacy-profile index follow-up still exposed a second trigger blind spot because `References/LegacyProfiles/**` was not yet part of the Knowledge Architecture path set.

The existing validators checked routing/path/controller/provenance invariants but did not prove natural-language live-state agreement across Lifecycle, Knowledge Map, Roadmap, Work Queue and Artifact Evidence.

## Integrated repair

The repair did not alter `.r2z` gameplay artifacts, project gameplay source, configs, runtime evidence, accepted/rejected gameplay decisions, or the S1.42AH runtime-test contract.

It performed repository-governance/information-integrity changes only:

- synchronized current live authorities on accepted S1.42AF / active S1.42AH / runtime test outstanding;
- removed stale pre-build instructions from `CURRENT_STATE.selected_scope`;
- reduced volatile build/evidence duplication in the machine Knowledge Map;
- added machine-checkable `LIVE_STATE` markers to current human authorities;
- added `RepositoryTools/current_state_semantic_validator.py` and a negative self-test for stale live runtime instructions;
- modeled pending runtime candidates separately in the artifact evidence index and byte validator so profile/snapshot bytes are verified before runtime without inventing a runtime log;
- resolved current profile snapshot build ids through `EXPECTED_HASHES` or `BUILD_LINEAGE`, fail-closed for unregistered current `Profiles/` artifacts, while retaining filename fallback only for legacy reference profiles;
- registered S1.42AF/S1.42AG/S1.42AH profile hashes explicitly;
- removed `[skip ci]` from automatic profile-index commits;
- removed the duplicate filename-derived S1.42AH snapshot tree while preserving canonical `ProfileSources/S1.42AH/`;
- extended Knowledge Architecture triggers to `References/LegacyProfiles/**`, closing the follow-up legacy-index validation gap.

## Validation evidence

PR #42 was not merged on the first attempt merely because the repair looked correct. CI first caught generated-navigation drift, then a missing explicit Gale v2.4 lifecycle route. Both were corrected before merge. The final PR #42 head passed all permanent Knowledge Architecture checks.

After PR #42 merged, the profile indexer produced `4146de7...` and thereby demonstrated the remaining legacy-path trigger gap. PR #43 added `References/LegacyProfiles/**` to both push and pull-request Knowledge Architecture path sets. Its PR run passed all permanent checks, and merge `c1bd196d...` received Knowledge Architecture run `34205094473` with all checks green.

This sequence is retained as positive evidence that the repair was validated against the exact failure modes found during the handover audit rather than accepted from static review alone.

## Current gameplay lifecycle after repair

The gameplay lifecycle remains unchanged by this repository repair:

- S1.42AF remains the sole accepted gameplay baseline.
- S1.42AH remains the active runtime candidate and is not accepted yet.
- `RuntimeInbox/ACTIVE_BUILD.txt = S1.42AH`.
- `BuildSpecs/current.json` remains disabled and guarded on S1.42AH while runtime validation is open.
- No successor beyond S1.42AH is armed.
- The next gameplay action is the full-normal MouthDog/Pikmin runtime gate from `Current/139_S1.42AH_BUILD_CANDIDATE_MOUTHDOG_DUAL_PREVENTION.md`.

Do not accept S1.42AH from build/startup success alone.
