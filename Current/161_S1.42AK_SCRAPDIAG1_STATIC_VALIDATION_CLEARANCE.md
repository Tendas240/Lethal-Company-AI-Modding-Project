# S1.42AK-SCRAPDIAG1 Static Validation Clearance

**Date:** 2026-09-18  
**Status:** IMPLEMENTED / STATIC PASS / REVIEW ARTIFACT PINNED / NOT PUBLISHED / NOT ARMED  
**Accepted gameplay baseline:** S1.42AK — unchanged  
**Scope:** LC Office Scrap Quantity/Distribution Investigation  
**Implementation PR:** #127  
**Design authority:** `Current/160_LC_OFFICE_SCRAP_PLACEMENT_DIAGNOSTIC_DESIGN.md`  
**Build plan:** `BuildSpecs/S1.42AK-SCRAPDIAG1_PLAN.md`  
**Static evidence:** `BuildSpecs/S1.42AK-SCRAPDIAG1_BUILD_EVIDENCE/STATIC_VERIFICATION.md`

## Decision

The approved placement diagnostic is implemented and has passed the complete repository-native static source/build/archive gate. It is cleared to merge as diagnostic infrastructure.

It is **not** a gameplay acceptance, is **not** a published runtime candidate, and does **not** arm a runtime test.

The accepted gameplay baseline remains exact S1.42AK. `BuildSpecs/current.json`, `RuntimeInbox/ACTIVE_BUILD.txt`, active-candidate state and `runtime_test_outstanding` remain unchanged.

## Validated implementation

The implementation consists of:

- existing reviewed `Patches/S142AJDiag1OfficeSelection/` source, unchanged by PR #127;
- new `Patches/S142AKDiagScrapPlacement/` read-only placement logger;
- separate non-active `BuildSpecs/S1.42AK-SCRAPDIAG1.json`;
- repository-native static validation workflow and validator.

The new logger has exactly one Harmony interception, the declared `RoundManager.SpawnScrapInLevel()` postfix, ordered after `tendas.s139.compatibilityfixes`. It performs bounded delayed observation only and does not mutate scrap pools, rarity, values, transforms, gameplay state, RPC state or network ownership.

## Static gate

Primary build/source/archive validation:

- workflow: `S1.42AK SCRAPDIAG1 build and static gate`;
- run: `35383680389`;
- validated implementation head: `758816b6ce09a3406f256b1a9376ccb66d996afa`;
- result: **SUCCESS**;
- exact S1.42AK base SHA-256: `b39aa550a517ec727de6eb1ae825383933047d3c556cb6e8d4aa7611c9f89dee`;
- existing archive members changed: **none**;
- existing archive members removed: **none**;
- added archive members: exactly the two diagnostic DLLs;
- AJDIAG1 selection-boundary tests: **PASS**;
- both diagnostic projects compile cleanly;
- accepted S139 and Interior Weight Normalization DLLs remain byte-identical.

The current PR head was subsequently revalidated by run `35384879980`, also **SUCCESS**, including controller/lifecycle-state unchanged checks.

## Exact pinned review artifact

The publication candidate is the exact successful review artifact from run `35383680389`:

- GitHub Actions artifact ID: `10563033130`;
- artifact name: `S1.42AK-SCRAPDIAG1-review`;
- artifact ZIP SHA-256: `08c676c2a0f9c7a3df3a0cd58854dbcc0b53ba21dd514a7c7d661cfcfe231bc3`;
- diagnostic profile SHA-256: `233bcc058a3fa95d63e0577c4ff74b5a0dc137db49757b50376e447dd082d3b1`;
- `S142AJDiag1OfficeSelection.dll` SHA-256: `51d8927838f003f8ea00d69942d21dda3937d46c0cff5b220e99651c3a61a191`;
- `S142AKDiagScrapPlacement.dll` SHA-256: `f1c0b7450d60b8c0400c342779abcae8dfb11c34d9112d2e945a02349610df7b`.

This exact artifact is the default input for any later publication/arming transition.

## Rebuild byte-variance qualification

A later clean CI rebuild from the same implementation source produced a different profile/DLL byte hash while still passing the complete source/archive gate. Therefore the local .NET build output is not treated as byte-deterministic across independent CI rebuilds.

This does **not** invalidate the static safety result: the archive delta and source contract remain exact. It does mean runtime publication must fail closed on artifact identity.

A later arming step must either:

1. publish the exact pinned review artifact above; or
2. intentionally select a newly rebuilt artifact, persist its fresh profile/DLL hashes as the new publication identity, and re-bind the runtime candidate to those exact bytes.

No rebuild may silently replace the pinned publication identity.

## Runtime boundary

No placement conclusion exists yet. Runtime remains required to prove:

- AJDIAG1 arms and selects LC Office on Offense;
- the placement logger arms after the accepted S139 path;
- snapshot A/B stabilize;
- per-item and anchor markers are complete;
- no REFUSED/INCONCLUSIVE marker invalidates the run;
- the resulting positions actually establish or reject a floor/room concentration hypothesis.

No scrap quantity increase or other gameplay tuning is authorized.

## Exact next action

Merge the statically validated diagnostic implementation and this clearance state to `main` while leaving runtime controllers idle.

After that, perform a **separate explicit publication/arming transition** for the exact pinned S1.42AK-SCRAPDIAG1 artifact. Only that later transition may set a runtime candidate/test outstanding and must then provide the canonical Gale replacement/import command plus the exact build-specific autonomous PowerShell log uploader in the same user-facing response.
