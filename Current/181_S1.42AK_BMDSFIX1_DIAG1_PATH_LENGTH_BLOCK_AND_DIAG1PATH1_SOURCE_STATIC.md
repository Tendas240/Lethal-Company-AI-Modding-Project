# S1.42AK-BMDSFIX1-DIAG1 Path-Length Block / DIAG1PATH1 Source-Static Preparation

**Date:** 2026-09-24  
**Status:** DIAG1 PRELOADER-BLOCKED BEFORE DIAGNOSTIC EXECUTION / DIAG1PATH1 SOURCE-STATIC PREPARED / NOT BUILT / NOT PUBLISHED / NOT ARMED  
**Accepted gameplay baseline:** S1.42AK — unchanged  
**Active gameplay candidate:** S1.42AK-BMDSFIX1 — unchanged / not accepted

## Observed launch block

Two consecutive local launches of the exact published DIAG1 profile failed inside the BepInEx/Mono preloader before any `[BMDSFIX1-DIAG1] ARMED` or target-selection evidence could occur.

The failures named these runtime-consumed files:

- `BepInEx\patchers\MonkeySolutions-LC_Office_v81_Unofficial_Compatibility_Fix\LCOfficeV81Preloader\LCOfficeV81Preloader.dll`;
- `BepInEx\plugins\loaforc-loaforcsSoundAPI_LethalCompany\loaforcsSoundAPI_LethalCompany\me.loaforc.soundapi.lethalcompany.dll`.

A subsequent read-only local inspection established that both files, the base SoundAPI DLL and `export.r2x` were physically present and non-empty. The local `export.r2x` SHA-256 was `2bd515d2790d452937ebec2afd2d90a976f7f828aca32cf68a11ac503d858880`, exactly matching the published DIAG1 archive entry. No retained DIAG1 `.r2z` remained after the verified Gale import.

Therefore this is not evidence that DIAG1 gameplay logic or BMDSFIX1 failed. Diagnostic execution was never reached.

## Path-length finding

On the observed Gale root `C:\Users\Milan\AppData\Roaming\com.kesomannen.gale\lethal-company\profiles`, the exact published profile name

`LC V1 S1.42AK-BMDSFIX1-DIAG1 Deterministic Deep Sewers Selector`

produces full paths of 260 and 262 characters for the two failing files above. This reproduces the classic Windows path-length boundary precisely enough to treat the long local profile identity as the bounded launch blocker.

No loader-wide workaround, Windows policy mutation, package removal or gameplay patch is authorized.

## Minimal successor preparation

Prepared successor build ID:

`S1.42AK-BMDSFIX1-DIAG1PATH1`

Its recipe uses the exact published DIAG1 profile SHA-256 `31c24a3752aefe040b74c5dc2c3b7f677c17068a91f8c8e2893ace615050b78e` as immutable base and changes only the Gale/profile identity to:

`LC V1 S1.42AK-D1P1`

The two observed full paths then become 215 and 217 characters.

The recipe contains zero package changes, zero config changes, zero mod-state changes, zero file injections and zero local plugin builds. A later review build must therefore differ from exact DIAG1 only in `export.r2x` profile identity metadata. The DIAG1 selector DLL, BMDSFIX1 DLL and accepted normalizer DLL must remain byte-identical.

Canonical preparation files:

- `BuildSpecs/S1.42AK-BMDSFIX1-DIAG1PATH1.json`
- `BuildSpecs/S1.42AK-BMDSFIX1-DIAG1PATH1_PLAN.md`
- `AnalysisTools/validate_s142ak_bmdsfix1_diag1path1_source.py`
- `.github/workflows/s142ak-bmdsfix1-diag1path1-source-static.yml`

## Lifecycle boundary

This preparation is deliberately controller-neutral. It does not relabel the blocked DIAG1 launches as runtime evidence, does not accept BMDSFIX1, does not waive the regular Black Mesa x `DeepSewersFlow` BMDSFIX1 qualification gate and does not mix Black Mesa x Greenhouse/BMGHDIAG into this scope.

The blocked long-name DIAG1 profile must not be run again. A later separately versioned lifecycle reconciliation must de-arm/replace it only after DIAG1PATH1 has passed the required review/publication gates.

## Next bounded gate

Run the inactive DIAG1PATH1 review-build gate and prove the exact identity-only archive delta before any publication or runtime-controller change.
