# S1.42AG Pre-Build Checklist

Before S1.42AG is armed/built in a later explicit segment, verify:

- [ ] branch is based on accepted S1.42AF state;
- [ ] exact `MouthDogPikminEnemy.DoCheckInterval()` contract is still present in source evidence;
- [ ] local plugin implementation uses exact `Priority.First` prevention-only prefix;
- [ ] no component disable or delayed state repair was introduced;
- [ ] no LethalMin config value was changed as a workaround;
- [ ] S1.42AF guarded base profile hash remains `6a82a42bfe010767f4f39aab4d108fa45268407d9658a3e2410162cf9f6f47d0`;
- [ ] controller is armed only together with the implementation/build delta;
- [ ] no runtime-test state is declared until a candidate artifact actually exists;
- [ ] future runtime instructions include Gale replacement/import plus the build-specific log uploader in the same response.

This checklist is planning evidence only and does not itself arm or build S1.42AG.
