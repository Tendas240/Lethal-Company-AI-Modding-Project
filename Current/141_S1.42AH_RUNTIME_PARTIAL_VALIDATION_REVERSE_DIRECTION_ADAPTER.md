# S1.42AH Runtime Partial Validation — Reverse Direction, Death Cleanup and Adapter Protection

**Date:** 2026-09-08  
**Status:** EXTENDED PARTIAL RUNTIME PASS / FINAL NON-PIKMIN NEIGHBOR REMAINDER OUTSTANDING  
**Lifecycle effect:** none — S1.42AF remains accepted; S1.42AH remains active and neither accepted nor rejected; S1.42AG remains rejected  
**Candidate:** `Current/139_S1.42AH_BUILD_CANDIDATE_MOUTHDOG_DUAL_PREVENTION.md`  
**Prior partial decision:** `Current/140_S1.42AH_RUNTIME_PARTIAL_VALIDATION_MOUTHDOG_COLLISION_PLAYER.md`  
**Runtime evidence:** `RuntimeEvidence/S1.42AH/20260908T174352Z/`  
**Runtime index:** `RuntimeEvidence/S1.42AH/20260908T174352Z/INDEX.json`  
**Raw log SHA-256:** `6fca32623eb350c53b4f81c98ea3ef1218dee9a4256a4bb70a8950f1b3ab06a6`

## Candidate identity

- Profile: `Profiles/LC V1 S1.42AH Mouth Dog Fix.r2z`
- Profile SHA-256: `06e07fe6805e5e41786c16b5c1ea2132c4f65b385517c902f8aa566ccf49cd4e`
- Compatibility DLL SHA-256: `bf86f338dba1428327088f0aaa2af8d9816f647c3b3c12214a5fc52db8e34573`
- Parent: exact accepted S1.42AF.
- `BuildSpecs/current.json` remains disabled; no successor beyond S1.42AH is armed.

## Relationship to Current/140

`Current/140` remains the preserved first-run evidence for patch installation, repeated live Vanilla MouthDog -> Pikmin collision blocking, and MouthDog -> player preservation including a real `Mauling` death. This record advances only the previously open targeted remainder.

## What the second targeted runtime run proves

### Explicit native Pikmin -> MouthDog combat

The focused gameplay sequence records explicitly thrown Pikmin landing on `MouthDogModel`, `Latching Pikmin`, `Latching onto MouthDogModel`, `Setting current task to AttackEnemy`, `Started attack on local client`, and repeated `Hitting enemy with: 0.03` events. This closes the command/throw, latch, attack and damage gate.

### MouthDog death and native cleanup

The same sequence records `Client #0 hit enemy MouthDog(Clone) with force of 1.`, `Kill enemy called! destroy: False`, the MouthDog death sound, and `MouthDog(Clone) ... has died`. Immediately afterwards attacking Pikmin record `Setting to idle on local client`, `Removing current task`, `Removed Leader`, and `Unlatching Pikmin`.

This proves the exercised native Pikmin attack path can kill the MouthDog and that death-triggered unlatch/task cleanup remains functional without project-local state reconstruction.

### Native corpse-carry transition

After death, `MouthDogBody(Clone)` is initialized as a Pikmin item. Pikmin find the body, target its grab positions, switch to `CarryItem`, and grab/carry the corpse. Native post-death ownership therefore remains functional beyond unlatch cleanup.

### Adapter-side prevention executes during live gameplay

The run contains `[MouthDogPikminGuard]` execution during real MouthDog/Pikmin gameplay, explicitly blocking `MouthDogPikminEnemy.DoCheckInterval()` before target collection, bite RPC dispatch, `GrabbedPikmin` bookkeeping or `GrabPikmin` mutation. In the targeted interval there is no `Biting N Pikmin`, `EnemyAttackMouth`, 2.5-second MouthDog `GrabPikmin` mutation or Dog-path Pikmin `HitEnemy(2)` processing. `GrabPikmin` text that appears is the prevention diagnostic itself, not evidence that the mutation ran.

### Vanilla Pikmin collision guard, noise response and regression markers

The run again records the bounded `[MouthDogVanillaCollisionGuard]` blocks for validated runtime `LethalMin.PikminAI` instances. Ordinary MouthDog `Heard noise!`, `targetPos` and `lastheardnoisePosition` behavior remains present.

According to the ingested index/marker analysis, all of the following are zero in the second log:

- `Work state with no task assigned!`;
- `Leader is null when following`;
- `[Error  :S1.39 Compatibility Fixes]`;
- `[Fatal`.

## What remains unproven

One adjacent contract from the original runtime gate still lacks a deliberate positive exercise:

**MouthDog/Paw -> non-Pikmin `EnemyAI` pass-through / native Paw-initiated `BiteKillEnemyAI` behavior.**

The log contains other non-Pikmin enemies in the environment, but no unambiguous MouthDog/Paw collision sequence positively proves that neighboring native path actually ran. Absence of a Pikmin-filter marker is not sufficient by itself.

The final remainder is therefore intentionally narrow:

1. deliberately create a MouthDog/Paw interaction with a **non-Pikmin `EnemyAI`**;
2. positively exercise native Paw-initiated `BiteKillEnemyAI` / generic non-Pikmin collision behavior;
3. verify the Pikmin-only prefix does not filter that path;
4. upload the complete fresh S1.42AH `LogOutput.log`.

Do not repeat the now-proven Pikmin -> MouthDog attack/death/unlatch/task-cleanup or adapter-protection sequence merely to satisfy this final neighboring contract.

## Decision

**S1.42AH remains neither accepted nor rejected.**

Across the first and second ingested runs, the candidate now positively covers patch installation, repeated Pikmin-specific collision prevention, MouthDog -> player preservation, explicit native Pikmin -> MouthDog combat, MouthDog death and cleanup, corpse carry, live adapter prevention, preserved noise response and clean known project regression markers.

Only the final non-Pikmin neighbor/Paw pass-through remains. If it passes, S1.42AH can receive explicit acceptance. If it fails, reject S1.42AH and return to an S1.42AF-derived follow-up.

## Evidence integrity

The second ingested raw log is 1,948,955 bytes with 19,775 lines. Its authoritative SHA-256 is `6fca32623eb350c53b4f81c98ea3ef1218dee9a4256a4bb70a8950f1b3ab06a6`. The first-run evidence at `RuntimeEvidence/S1.42AH/20260908T162411Z/` remains preserved and reasoning-relevant.