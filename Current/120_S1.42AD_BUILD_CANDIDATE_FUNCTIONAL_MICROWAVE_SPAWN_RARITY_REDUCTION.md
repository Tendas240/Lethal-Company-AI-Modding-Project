# S1.42AD Build Candidate — Functional Microwave Spawn Rarity Reduction

**Date:** 2026-09-05  
**Status:** BUILD PASS / GAMEPLAY RUNTIME VALIDATION OPEN / NOT ACCEPTED  
**Accepted rollback baseline:** S1.42AC — BCMER EventType Equal Distribution

## Candidate identity

Profile: `Profiles/LC V1 S1.42AD Functional Microwave Spawn Rarity Reduction.r2z`  
Gale profile name: `LC V1 S1.42AD Functional Microwave Spawn Rarity Reduction`  
Profile SHA-256: `9fea61e677a154cbfe68380e7c9d6a1b9285ca821d7dcec93772413ede27cf8c`

Successful build workflow run: `33959742235`  
Automated build commit: `1463a6cde5e8cb7655dd233f83da5157c91b036e`

Plan / Patch Safety Review: `BuildSpecs/S1.42AD_PLAN.md`  
Patch source: `Patches/S142ADCodeRebirthMicrowaveSpawnTuning/`  
Readable snapshot: `ProfileSources/S1.42AD/`

Injected DLL:

`BepInEx/plugins/S142ADCodeRebirthMicrowaveSpawnTuning/S142ADCodeRebirthMicrowaveSpawnTuning.dll`

DLL SHA-256:

`45f22f9b27e3ab7c853fe742bb7c2ce9bc94abc5a0856bb278c747076a2f99c7`

## User-authorized balance target

The user explicitly requested that Functional Microwaves be encountered half as often.

S1.42AD therefore uses:

`SpawnScale = 0.5f`

The patch proportionally scales the currently effective Functional Microwave Moon/tag spawn curves to 50% amplitude. It does not replace them with absolute fixed weights.

## Verified fail-closed owner contract

The patch validates before mutation:

- CodeRebirth `1.6.9`;
- DawnLib `0.9.25`;
- DawnLib.Dusk `0.9.25`;
- exact map-object key `code_rebirth:functional_microwave`;
- target `InsideInfo.SpawnWeights` provider table;
- exactly one `Dusk.MapObjectSpawnMechanics` provider;
- `PrioritiseMoons = true`;
- exactly 18 expected Moon/tag curves;
- exactly 0 Interior/tag curves;
- every expected curve is non-null and non-empty.

Any drift causes an error and no curve mutation. No fallback to another key/provider or broad Inside-Hazard scaling is allowed.

The 18/0 contract follows the actual relevant CodeRebirth 1.6.9 asset-bundle provenance. The relevant `coderebirthasset` rebuild was commit `c4020f20f5a05f1aee6519a2eb1554903890e08f` on 2026-04-06. A later 2026-04-09 source edit (`eb4d5148047c625076b4735784a7ca2477ef17b6`) added source-only Interior curves after that bundle rebuild and is not treated as the shipped runtime contract.

## Exact archive delta vs accepted S1.42AC

Automated verification:

- ZIP members: `335`;
- changed existing members: exactly `export.r2x` (Gale profile-name metadata);
- added members: exactly the S1.42AD DLL above;
- mod state changes: `0`;
- mod additions: `0`;
- mod removals: `0`;
- config patches: `0`.

Accepted Functional Microwave config remains unchanged, including:

- `Functional Microwave | Allow Editing Config = true`;
- `Functional Microwave | Volume = 0.15`.

## Runtime gate

One normal gameplay run is sufficient for the immediate technical gate if it produces a complete fresh log and normal round generation.

Required checks:

1. startup/main menu/lobby succeeds;
2. `S1.42AD CodeRebirth Microwave Spawn Tuning 1.0.0` loads;
3. CodeRebirth `1.6.9`, DawnLib `0.9.25`, and Dusk `0.9.25` validation succeeds;
4. armed marker appears for the Moon-registry-freeze lifecycle;
5. provider marker reports `PrioritiseMoons=true, MoonCurves=18, InteriorCurves=0`;
6. final marker reports all 18 `code_rebirth:functional_microwave` Moon/tag curves scaled by `0.5`;
7. no S1.42AD refusal/error marker appears;
8. normal moon/interior generation and ordinary gameplay remain healthy;
9. no new fatal/project-critical regression appears;
10. accepted S1.42AC behavior outside the isolated Microwave rarity scope remains unchanged;
11. upload the complete fresh `LogOutput.log`.

A short runtime sample is not expected to statistically prove an exact observed 50% occurrence ratio. Acceptance relies primarily on deterministic provider mutation plus clean adjacent runtime behavior.

## Gale replacement helper

Canonical repository-driven replacement/import launcher:

```powershell
$u='https://raw.githubusercontent.com/Tendas240/Lethal-Company-AI-Modding-Project/main/RuntimeTools/ReplaceActiveGaleProfile.ps1?cb='+[DateTime]::UtcNow.Ticks;iex (iwr -UseBasicParsing $u).Content
```

## Exact S1.42AD runtime uploader

After the gameplay test, use this exact self-contained one-line PowerShell command:

```powershell
$src=Join-Path $env:APPDATA 'com.kesomannen.gale\lethal-company\profiles\LC V1 S1.42AD Functional Microwave Spawn Rarity Reduction\BepInEx\LogOutput.log';if(!(Test-Path -LiteralPath $src)){throw "Log not found: $src"};$gh=(Get-Command gh -ErrorAction SilentlyContinue).Source;if(!$gh){$gh=@("$env:ProgramFiles\GitHub CLI\gh.exe","$env:LOCALAPPDATA\Programs\GitHub CLI\gh.exe","${env:ProgramFiles(x86)}\GitHub CLI\gh.exe")|Where-Object{$_ -and (Test-Path -LiteralPath $_)}|Select-Object -First 1};if(!$gh){winget install --id GitHub.cli -e --source winget --accept-package-agreements --accept-source-agreements;$gh=(Get-Command gh -ErrorAction SilentlyContinue).Source;if(!$gh){$gh=@("$env:ProgramFiles\GitHub CLI\gh.exe","$env:LOCALAPPDATA\Programs\GitHub CLI\gh.exe","${env:ProgramFiles(x86)}\GitHub CLI\gh.exe")|Where-Object{$_ -and (Test-Path -LiteralPath $_)}|Select-Object -First 1}};if(!$gh){throw 'GitHub CLI gh.exe could not be found after resolution/install attempt'};& $gh auth status --hostname github.com *> $null;if($LASTEXITCODE -ne 0){& $gh auth login --hostname github.com --git-protocol https --web;if($LASTEXITCODE -ne 0){throw 'GitHub authentication failed'}};$repo='Tendas240/Lethal-Company-AI-Modding-Project';$dst='RuntimeInbox/Current/LogOutput.log';$sha=(& $gh api "repos/$repo/contents/$dst" --jq '.sha' 2>$null);$p=@{message='Upload S1.42AD runtime log';content=[Convert]::ToBase64String([IO.File]::ReadAllBytes($src));branch='main'};if($sha){$p['sha']=$sha};($p|ConvertTo-Json -Compress)|& $gh api --method PUT "repos/$repo/contents/$dst" --input -;if($LASTEXITCODE -ne 0){throw 'Runtime log upload failed'}
```

## Acceptance boundary

S1.42AD is **not accepted yet**. S1.42AC remains the accepted rollback/full-normal-stack baseline until the S1.42AD runtime gate is explicitly evaluated and promoted or rejected.
