# 119 — S1.42AD Interrupted Implementation Recovery

**Date:** 2026-09-05  
**Status:** RECOVERY REVIEW COMPLETE / SOURCE DRAFT ONLY / NOT BUILT / NOT ARMED  
**Accepted gameplay baseline remains:** S1.42AC — BCMER EventType Equal Distribution  
**Selected next scope:** S1.42AD — Functional Microwave Spawn Rarity Reduction

## Why this record exists

The previous ChatGPT session completed the corrected S1.42AC acceptance, then the user explicitly requested the next build. The session selected the Functional Microwave spawn-rarity scope and began CodeRebirth/DawnLib ownership analysis, but the conversation stalled before a build/candidate handoff was completed.

The repository was inspected independently after that interruption. Four source-only commits had been written directly to `main` after the S1.42AC acceptance commit, but no S1.42AD profile was built and no runtime test was armed.

## Repository work that already exists

Starting from accepted S1.42AC commit `2efad51f1e23699f547292482d184fe883dae54b`, the interrupted session added exactly four files under:

`Patches/S142ADCodeRebirthMicrowaveSpawnTuning/`

Commits:

1. `8f09cb5e70e569ef418dba950bac1d38d5d9583c` — `NuGet.Config`;
2. `8c06d88b0fbf940d4c83fea11ef3910a927e2098` — project file;
3. `61d4e48c77c58b09bbb02786251fce53d5d68ea8` — `Plugin.cs` implementation draft;
4. `f8b547b06786371b875c6fa0b21ac2ded4f7498e` — implementation README/contract draft.

No other files changed between accepted S1.42AC and `f8b547b...`.

## What the draft implementation does

The draft follows the same narrow DawnLib provider-scaling pattern already used by the accepted S1.42Z aerial-defense tuner:

- hard-requires CodeRebirth `1.6.9`;
- hard-requires DawnLib `0.9.25`;
- hard-requires DawnLib.Dusk `0.9.25`;
- waits for the DawnLib Moon registry freeze;
- requires the MapObjects registry to be frozen;
- resolves exactly `code_rebirth:functional_microwave`;
- requires `InsideInfo`;
- reflects the exact ProviderTable `_providers` field;
- requires exactly one `Dusk.MapObjectSpawnMechanics` provider;
- requires an exact 19-key Microwave curve set;
- scales every curve keyframe value, incoming tangent and outgoing tangent;
- refuses to apply on dependency/provider/key/curve-contract drift;
- modifies no other map-object provider.

This architecture is plausibly narrow and is structurally analogous to the already accepted S1.42Z CodeRebirth provider tuner. That is not yet equivalent to build/runtime acceptance.

## Critical unresolved point: the `0.5` target is not currently authorized

The draft hard-codes:

`SpawnScale = 0.5f`

and the draft README describes a 50% occurrence reduction.

Current canonical roadmap/item-tuning authorities only require a **Functional Microwave spawn-rarity reduction** / that the Microwave should be rarer. They do not currently define an exact reduction percentage or scale.

Therefore `0.5` must be treated as a **proposal embedded in the interrupted draft**, not as an accepted user target.

Do not arm/build S1.42AD with `0.5` merely because the draft already contains it. Resolve the exact magnitude with the user or with a genuinely authoritative prior project instruction first.

## What has NOT been completed

The following mandatory gates are still open:

- no independent confirmation of the exact CodeRebirth/DawnLib registration/ownership path has been recorded for this Microwave target;
- no explicit source-provenance record for the 19-key contract exists yet;
- the Patch Safety Review in `BuildSpecs/S1.42AD_PLAN.md` remains blocked/incomplete pending target and ownership confirmation;
- the new project has not been proven to compile in the repository build environment;
- no S1.42AD DLL SHA-256 exists;
- no S1.42AD profile has been generated;
- no archive/profile diff exists;
- `BuildSpecs/current.json` has never been armed for S1.42AD;
- `RuntimeInbox/ACTIVE_BUILD.txt` remains S1.42AC;
- no S1.42AD runtime test/evidence exists;
- no S1.42AD acceptance/rejection decision exists.

The Knowledge Architecture push run on `f8b547b...` was green, but that workflow validates repository architecture/integrity. It does not compile this arbitrary new patch project and must not be interpreted as a patch build gate.

## Current classification

- S1.42AC remains the accepted Full-Normal-Stack baseline.
- S1.42AD is the explicitly selected next scope.
- The existing `Patches/S142ADCodeRebirthMicrowaveSpawnTuning/` tree is preserved as an **UNBUILT IMPLEMENTATION DRAFT**.
- `BuildSpecs/S1.42AD_PLAN.md` now exists as a **blocked pre-build plan**, not an armed build instruction.
- There is no active candidate.
- There is no runtime test outstanding.
- There is no armed successor build controller.
- The draft has no gameplay/runtime effect because it has not been built into a profile.

## Exact next action

Before building anything:

1. resolve the exact desired Microwave spawn reduction magnitude; `0.5` is only the draft proposal;
2. independently verify the exact CodeRebirth `1.6.9` / DawnLib `0.9.25` / Dusk `0.9.25` ownership and the exact 19-curve Microwave provider contract;
3. complete the mandatory Patch Safety Review in `BuildSpecs/S1.42AD_PLAN.md` under `Current/68_PROJECT_LOCAL_PATCH_SAFETY_AND_REGRESSION_POLICY.md`;
4. update/finalize that plan with the approved target and provenance;
5. only then arm `BuildSpecs/current.json` and run the repository-native profile build;
6. if a candidate is built and passes automated QC, update the current candidate/runtime controller atomically and give the user the Gale replacement/import one-liner plus the exact S1.42AD runtime-log uploader in the same response.

Do not ask the user to repeat the already completed S1.42AC acceptance run. Do not treat the source draft or blocked plan itself as a candidate profile.

## Rollback / preservation rule

Do not delete the interrupted source draft merely because the session did not finish. It is useful implementation evidence and may become the final implementation after the open target/provenance/build-safety questions are resolved.

If the final approved reduction magnitude differs from `0.5`, update the S1.42AD source/build plan explicitly before building rather than pretending the original draft already represented the approved target.
