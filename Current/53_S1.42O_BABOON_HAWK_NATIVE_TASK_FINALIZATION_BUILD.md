# 53 — S1.42O Baboon Hawk Native Task Finalization Build

**Date:** 2026-09-03  
**Game:** Lethal Company V81

## Candidate

**S1.42O — Baboon Hawk Native Task Finalization**

Profile:
`Profiles/LC V1 S1.42O Baboon Hawk Native Task Finalization.r2z`

SHA-256:
`04d7e0df7f7a3b51d75832e9052c3e217ec07f91526c767323e1b5b0a3078d4d`

Base:
`Profiles/LC V1 S1.42N Baboon Hawk Death Target Resolver.r2z`

Base SHA-256:
`c87d48464a750f87274e2848c44e5e1e24d4f1da087f59a33e2889744ebc13e9`

Compatibility plugin:
**v1.3.10**

Build:
- GitHub Actions run #46: success;
- build and verify: success;
- generated profile commit: success;
- artifact upload: success;
- 331 archive members;
- 330 readable snapshot files;
- changed existing archive members only:
  - compatibility DLL;
  - `export.r2x`;
- no added members.

## Evidence opening this build

S1.42N:
`RuntimeEvidence/S1.42N/20260903T165818Z/`

Log SHA-256:
`8ed6a79230e28535a01a4cb86a8971c358a5c9f57a53722e18ff4342474f51cf`

S1.42N proved the one-shot target resolver works:
`native predecessor resolver released 6/6 real attack participants`.

However, direct `RemoveCurrentTask()` left five of those attackers in LethalMin Work state with no task. The log contains **2155** `Work state with no task assigned!` warnings after Hawk death, concentrated on five resolver-touched Pikmin. One of them continued stale attack-hit output for more than a minute.

Control case `PerDu` naturally executed:
`Task finished -> Setting to idle -> Removing current task`
and remained functional enough to later carry the Dead Baboon Hawk corpse.

## S1.42O change

Retain:
- one-shot `RoundManager.Instance.SpawnedEnemies` resolver;
- exact/assignable `LethalMin.PikminAI` candidates;
- 4.0 m Hawk death zone;
- Dead Baboon Hawk corpse guard;
- EnemyIsolation enabled;
- BCMER 1.71.0 disabled.

Replace low-level death cleanup:
- **remove direct `RemoveCurrentTask()` invocation**.

Use native high-level completion:
- resolve exact declared zero-argument `LethalMin.PikminAI.TaskFinished()`;
- validate void return, exact declaring type, zero parameters and implementation body;
- invoke `TaskFinished()` for selected attack participants;
- do not use low-level RemoveCurrentTask fallback if resolution fails.

Expected native behavior is the same path observed on the healthy PerDu control.

## Exact runtime gate

Import with:
**Gale -> Advanced options -> Import all files**

Test:
1. throw multiple Pikmin onto a living Baboon Hawk;
2. let them kill it;
3. verify all attackers stop the dead-Hawk attack;
4. verify they do not run in place / become permanently unresponsive;
5. whistle/regain them and verify they can follow and be reused;
6. compare active/following Pikmin count before and after the interaction;
7. verify no repeated `Work state with no task assigned!` loop;
8. verify no stale `Hitting enemy with` loop after Hawk death;
9. verify corpse still appears and reaches the Onion;
10. verify living Hawks still ignore corpse;
11. verify Hawk -> Pikmin ignore;
12. verify no leader-null loop;
13. commit the complete fresh log to `RuntimeInbox/Current/`.

Do not restore EnemyIsolation or BCMER until this gate passes.
