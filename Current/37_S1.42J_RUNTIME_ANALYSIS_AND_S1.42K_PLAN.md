# 37 — S1.42J Runtime Analysis and S1.42K Thumper Counterattack Plan

**Date:** 2026-09-03  
**Game:** Lethal Company V81

## Runtime evidence

Build:
**S1.42J — Baboon Hawk Zero Interaction**

Profile:
`Profiles/LC V1 S1.42J Baboon Hawk Zero Interaction.r2z`

Profile SHA-256:
`736d7a3b495e124d2469e392b9956c0c3a381a6ce0502baee30d05fabb346cb7`

Evidence:
`RuntimeEvidence/S1.42J/20260903T145657Z/`

Log:
`RuntimeEvidence/S1.42J/20260903T145657Z/raw/LogOutput.log`

Log SHA-256:
`a8ce035bf64fa5b704e18c588215f43cd1fd184eef4f467dfbafa6fcb1379963`

## S1.42J verdict

### Startup / exact compatibility hooks

**PASS.**

Observed:
- `S1.39 Compatibility Fixes loaded.`
- exact common `LethalMin.PikminAI.GrabPikmin(Transform,float,int)` hook loaded exactly once;
- no S1.42D-style broad reflection/Harmony startup failure;
- run reached normal gameplay.

### Baboon Hawk <-> Pikmin

**PASS.**

Binding rule remains:
**complete zero interaction in both directions.**

Runtime evidence:
- initialization marker present:
  `[BaboonHawkPikminGuard] Zero-interaction initialized; bitePatched=True; baboonStartPatched=True; ...`
- spawned Baboon Hawks repeatedly produced the adapter-disable marker;
- no runtime Baboon `BitePikmin` / `GrabPikmin` failsafe event was required;
- zero `Leader is null when following` errors;
- user deliberately tested the interaction and observed that Baboon Hawks now completely ignore Pikmin;
- Pikmin -> Baboon Hawk remains blocked by the exact `Baboon hawk` LethalMin Attack Blacklist entry.

Keep this implementation unchanged.

### Thumper/Crawler -> Pikmin

**PASS for the required Pikmin protection.**

Runtime evidence:
- `[ThumperPikminGuard]` fired **19 times** during deliberate direct contact;
- every marker confirms the Crawler/Thumper-owned `GrabPikmin` was blocked before leader/grab/death-timer mutation;
- zero `Leader is null when following` errors;
- user observed that the Thumper can still perform its visible snap/bite animation toward Pikmin, but Pikmin are not functionally affected.

Keep the exact Thumper/Crawler -> Pikmin grab/death-state guard unchanged.

### Revised Thumper/Crawler reverse-direction rule

The earlier project rule "Thumper/Crawler <-> Pikmin zero interaction in both directions" is **superseded by the user's S1.42J runtime decision**.

New binding rule:

- **Thumper/Crawler -> Pikmin:** no functional bite/grab/leader-removal/death-timer effect.
- **Pikmin -> Thumper/Crawler:** normal LethalMin attack/latch behavior must be allowed.

Reason:
the user explicitly wants to be able to throw Pikmin onto a Thumper so they latch onto and attack it normally.

Root cause of the missing reverse interaction in S1.42J:
`BepInEx/config/NoteBoxz.LethalMin.cfg`
still contains exact runtime enemy name `Crawler` in:
`[Pikmin Behavior] Attack Blacklist`.

Therefore the next build must remove **only `Crawler`** from that blacklist.

Do not modify the project-local Thumper `GrabPikmin` guard.

### Puffer -> Pikmin

**PASS / reconfirmed.**

Runtime evidence:
- Puffer guard patched `PufferAI.Start`;
- first Puffer smoke instance removed 3 LethalMin Pikmin effect/latch components;
- second removed 2;
- user deliberately observed another smoke cloud and confirmed Pikmin were not affected.

No further forced Puffer test is required.

### Jetpack

**PASS / ACCEPTED.**

Runtime evidence:
- `Jetpack Fixes 1.6.3` loaded;
- project-local marker confirms loaded Item registry duration changed:
  `[Jetpack140] Jetpack battery duration changed 50 -> 140 seconds via loaded Item registry.`
- historical Coroner `PlayerController was null` flood count remained 0;
- user reports Jetpack behavior now works correctly and explicitly considers the Jetpack task complete.

Accepted target:
- approximately 140-second configured duration;
- no unwanted sustained/high-speed normal-use self-explosion.

Remove Jetpack from active validation work unless a future regression is reported.

## Next build — S1.42K

Purpose:
**restore intended Pikmin -> Thumper/Crawler attack/latch behavior without weakening Thumper/Crawler -> Pikmin protection.**

Base:
`Profiles/LC V1 S1.42J Baboon Hawk Zero Interaction.r2z`

Required delta:
- in `BepInEx/config/NoteBoxz.LethalMin.cfg`,
  remove `Crawler` from `[Pikmin Behavior] Attack Blacklist`;
- retain `Baboon hawk` in the blacklist;
- retain all compatibility-plugin code unchanged;
- retain temporary EnemyIsolation;
- retain BCMER 1.71.0 disabled for this last isolated-direction validation;
- retain Puffer guard and Baboon Hawk zero-interaction unchanged;
- retain Jetpack accepted configuration unchanged.

Expected Attack Blacklist:
`Docile Locust Bees,Manticoil,Red Locust Bees,Blob,Nemo,InternNPC,BellCrab,Nancy,Transporter,Janior,Peace Keeper,Guardsman,Tornado,FireStorm,Hurricane,Cabinet, Leaf boy,Baboon hawk`

## S1.42K runtime acceptance

Deliberately test one Thumper/Crawler with Pikmin:

1. throw Pikmin onto the Thumper;
2. Pikmin must latch/attack normally through LethalMin;
3. Thumper may visually snap/bite toward Pikmin, but its attack must not put Pikmin into grabbed/death-timer/leader-null state;
4. `[ThumperPikminGuard]` should still appear when the Thumper attempts its blocked GrabPikmin route;
5. no `Leader is null when following` loop;
6. Baboon Hawk and Puffer only need regression spot-checks if convenient.

If S1.42K passes this directional rule, the isolated enemy compatibility gate is complete. Then restore normal enemy configuration from:
`Current/ENEMY_SPAWN_BASELINE_S1.42C.json`

and re-enable exact:
`SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0`

while preserving the accepted permanent interaction rules.

Repository structural migration remains deferred until this final isolated gate is evaluated and documented.
