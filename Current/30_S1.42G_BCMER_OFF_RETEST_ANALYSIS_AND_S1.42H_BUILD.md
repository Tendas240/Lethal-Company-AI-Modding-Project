# 30 — S1.42G BCMER-off Retest Analysis and S1.42H Build

**Date:** 2026-09-03  
**Game:** Lethal Company V81

## Valid runtime evidence

Variant:
`S1.42G_BCMER_OFF_RETEST`

Evidence:
`RuntimeEvidence/S1.42G_BCMER_OFF_RETEST/20260903T115643Z/`

Log SHA-256:
`ac410c42e8174eb4f01aba1d3b7bf54100454e033ed87659a932f3b7f4a3c87e`

## Confirmed from the clean BCMER-off retest

### Routed-moon freeze regression
**PASS / resolved.**

The user reported the periodic freezes are gone.

S1.42G runtime markers confirm the diagnostic isolation is no longer driven by a once-per-second global `FindObjectsOfType<EnemyAI>()` scan.

### Enemy spawning without BCMER
**PASS.**

The log shows actual interior target spawns:
- Crawler/Thumper: 4 vent spawns;
- Puffer: 2 vent spawns.

Baboon Hawk was present in the outside Spawn Cycle Fixes predictor and nest path.

Conclusion:
the isolated spawn path works when BCMER is disabled. BCMER is therefore a confirmed confounder for this diagnostic stage.

### Puffer Pikmin guard
**Technical PASS.**

On Puffer spawn the project-local guard removed LethalMin smoke/latch components:
- first Puffer: 3 components;
- second Puffer: 2 components.

Player/vanilla Puffer behavior is left intact.

### Thumper/Crawler -> Pikmin
**FAIL.**

Observed by the user:
- Thumper ran into a Pikmin group;
- bite sound played;
- Thumper visually vanished from the encounter.

Log sequence:
- `Yellow Pikmin_apYy5: Removed Leader`
- `Grabbed by enemy, will die in 0.75s if not released.`
- repeated `Leader is null when following`
- 236 leader-null errors total in the run;
- grabbed death timer later attempted a Pikmin kill, blocked by `Invinceable Pikmin = true`.

Important:
the `Kill enemy called! destroy: True` line belongs to the Pikmin kill attempt, not proven Thumper destruction. All four Crawler instances remain accounted for until scene teardown.

Root cause:
S1.42G's safe state guard only registered on four declared enemy-adapter methods and did not cover the common `LethalMin.PikminAI.GrabPikmin(Transform,float,int)` base path used by the Thumper interaction.

### Coroner
The previous per-frame Jetpack null-player flood is fixed:
- `PlayerController was null`: 0.

Separate batched Coroner warnings remain when many invincible Pikmin hit Out-of-Bounds / Spike Trap death paths. Treat separately from the solved Jetpack spam.

### DoorAudit with BCMER disabled
Only four normal DoorAudit state/call events occurred, all with `doorPower=1.000`.

No BCMER DoorFailure stack flood occurred.

This strongly supports BCMER `Door System: ERROR` / DoorFailure as the source of the prior repeated forced-open door behavior and stack amplification.

## Built candidate — S1.42H

Profile:
`Profiles/LC V1 S1.42H Thumper Grab Guard.r2z`

SHA-256:
`5859e15ce71d8cd71d27e20205640af1f10ff91fe6d4b956d4a7064ac8400e58`

Base:
`Profiles/LC V1 S1.42G Routed Moon Performance Fix.r2z`

Base SHA-256:
`09364c11f8032645205b869ad760471259520cd57758e4d2d09a35665cf0d35a`

Compatibility plugin:
- version: **1.3.5**
- DLL SHA-256: `d67f8f4bc2012f5b74086eb268fcb191f6990c93041617e9ef35c635ea33f186`

GitHub Actions:
- success;
- 0 warnings;
- 0 errors;
- 331 archive members;
- only changed existing members:
  - compatibility DLL;
  - `export.r2x`;
- no added archive members.

## S1.42H behavior

### Exact Pikmin grab hook
Patches exactly once:
`LethalMin.PikminAI.GrabPikmin(Transform,float,int)`

No broad derived/inherited Pikmin Harmony scan.

Expected startup marker:
`[LethalMinStateGuard] Directly patched declared LethalMin.PikminAI.GrabPikmin(Transform,float,int) exactly once.`

### Thumper zero interaction
When the GrabPikmin snap position belongs to Crawler/Thumper:
- block before leader removal;
- block before grabbed death timer;
- leave Crawler on LethalMin Attack Blacklist for reverse Pikmin -> Thumper direction.

Expected encounter marker:
`[ThumperPikminGuard] Blocked Crawler/Thumper -> Pikmin GrabPikmin before leader/grab/death-timer state mutation.`

### Generic non-Thumper recovery
Other enemy GrabPikmin interactions remain allowed and use the pre-grab state recovery path if an invincible Pikmin survives leader-less.

### Late EnemyIsolation
Carries forward the prepared late-lifecycle isolation:
- FinishGeneratingNewLevelClientRpc;
- PredictAllOutsideEnemies;
- BeginEnemySpawning;
- no Update-driven continuous EnemyAI scan.

### BCMER
Exact `SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0` is **disabled inside S1.42H** for this final isolated regression stage.

Expected manifest:
- 188 total;
- 182 enabled;
- 6 disabled.

After the isolated enemy stage passes:
- disable the temporary EnemyIsolation diagnostic;
- restore the full enemy state from `Current/ENEMY_SPAWN_BASELINE_S1.42C.json`;
- re-enable exact BCMER 1.71.0.

### Functional Microwave
No rarity change in S1.42H.

Retained:
- editing gate true;
- volume 0.7.

Requested rarity reduction remains deferred until after this enemy regression stage.

## Runtime route

`RuntimeInbox/ACTIVE_BUILD.txt = S1.42H`

`BuildSpecs/current.json = disabled / IDLE_AFTER_S1.42H_BUILD_AWAITING_RUNTIME`

## Next test

Import:
**Gale -> Advanced options -> Import all files**

Primary acceptance:
1. Main Menu/startup.
2. No periodic routed-moon freezes.
3. Thumper touches/crosses Pikmin without grabbing them or breaking leader state.
4. No new Thumper-caused `Leader is null when following` loop.
5. Baboon Hawk bite/grab on an invincible Pikmin recovers to valid state.
6. Puffer smoke does not affect Pikmin.
7. Target enemies continue to spawn and appear in `Enemies` terminal output.

Upload the full new log to:
`RuntimeInbox/Current/`
