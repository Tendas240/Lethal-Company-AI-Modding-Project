# S1.42Y Runtime Assessment — Technical PASS / Balance Retune / S1.42Z Next

**Date:** 2026-09-04  
**Accepted rollback baseline:** S1.42U  
**Runtime-tested tuning candidate:** S1.42Y  
**Verdict:** TECHNICAL PATHS PASS / MICROWAVE + CARRY TUNING ACCEPTED / JETPACK + INDOOR PIKMIN BALANCE RETUNE REQUIRED / NOT ACCEPTED

## S1.42Y identity

Profile:

`Profiles/LC V1 S1.42Y Jetpack Aerial Defense Retune.r2z`

Profile SHA-256:

`f4ae0d93c9cff4f9441c24d1021e5d9b816861b8317d9ed8995fde67ebbd8d89`

Runtime evidence:

`RuntimeEvidence/S1.42Y/20260904T123817Z/`

Raw runtime log:

`RuntimeEvidence/S1.42Y/20260904T123817Z/raw/LogOutput.log`

Raw log SHA-256:

`dc32b104b880199ef0b210be254946bad280d1992e6142f6913bf9602921435a`

Size / coverage:

- 1,507,644 bytes;
- 15,447 lines;
- 14,730 parsed runtime events;
- 32 Error-severity events;
- Fatal marker count = 0.

## Technical runtime result

S1.42Y successfully proved the complete intended project-local paths:

- `S1.42Y Jetpack Acceleration 1.0.0` loaded;
- ButteRyBalance 0.7.0 validated;
- JetpackFixes 1.6.3 validated;
- More Ship Upgrades 3.14.1 validated;
- exact Jetpack patch armed with `10 -> 22`;
- `S1.42Y CodeRebirth Aerial Defense Spawn Tuning 1.0.0` loaded;
- CodeRebirth 1.6.9 validated;
- DawnLib 0.9.25 validated;
- DawnLib.Dusk 0.9.25 validated;
- Air Control Unit provider contract validated with exactly 18 curves;
- G.R.E.G. / Advanced Airspace Control provider contract validated with exactly 18 curves;
- one transactional applied marker confirmed all 18 ACU curves and all 18 G.R.E.G. curves were scaled by 0.5;
- no aerial-defense contract refusal occurred;
- EnemyIsolation remained disabled;
- normal enemy activity was present (`ADDING ENEMY` count = 9);
- Work/no-task = 0;
- Leader-null = 0;
- S1.39 Compatibility Fixes Error = 0;
- unspawned NetworkObjectReference marker = 0;
- PikminNoticeZone regression marker = 0;
- Jetpack blast death marker = 0;
- Fatal = 0.

The S1.42Y Jetpack interception architecture and the complete two-target CodeRebirth aerial-defense provider architecture are therefore technically usable.

## Error-severity classification

The 32 Error-severity events do not identify a new Y project-local regression. They consist of:

- 20 Unity Netcode duplicate `GlobalObjectIdHash` / already-registered prefab messages during content registration;
- 5 `Scan node is missing for item!` messages for ordinary ship items including the Jetpack;
- 1 SoftMasking-related missing `nunit.framework` FileNotFoundException;
- 1 BepInEx ConfigDefinition whitespace-key ArgumentException;
- 2 inactive-object coroutine-start errors for Clipboard / Sticky Note setup;
- 1 ReservedItemSlotCore `Synced with host!` message emitted at Error level despite successful sync;
- 1 TMPro selection-caret graphic-rebuild-loop error;
- 1 known loaforcsSoundAPI / HarmonyX TypeLoadException during `RoundManager.GenerateNewFloor` reporting.

None is emitted by either S1.42Y project-local plugin, no Fatal follows, and the tested gameplay continued. Keep these as existing non-blocking setup/mod observations unless future evidence makes one reproducible and user-facing.

Coroner also emits high-volume out-of-bounds warnings for non-player colliders; this is noisy but not a Y-specific failure.

## User gameplay findings

### Functional Microwave — ACCEPTED

`Functional Microwave | Volume = 0.15` is now subjectively accepted.

Preserve `0.15` in successors unless the user later changes the preference.

Microwave spawn rarity reduction remains a separate deferred task.

### Pikmin CarryStrength — ACCEPTED

Preserve:

- Blue / Red / Yellow / White / Winged / Rock / Ice / Glow / Bulbmin: `CarryStrength = 3`;
- Purple Pikmin: `CarryStrength = 30`.

Runtime startup data also reflected the expected values.

### Jetpack 22f — RETUNE DOWN

The user wants the next base acceleration target reduced from `22f` to:

`18f`

The proven Jetpack interception architecture stays unchanged; only the project-local target magnitude changes.

### Indoor Pikmin spawn chance — RETUNE UP

The user wants:

`Indoor Pikmin Spawn Chance = 0.09`

instead of Y's `0.08`.

### CodeRebirth aerial defense — TECHNICAL PATH ACCEPTED

Preserve the complete two-target transactional scaling:

1. `code_rebirth:air_control_unit` — all 18 exact curves × 0.5;
2. `code_rebirth:gunslinger_greg` — all 18 exact curves × 0.5.

Do not revert to the incomplete X ACU-only scope.

Short-run object counts are not required to equal an exact 50% reduction because DawnLib evaluates and rounds curve output.

## S1.42Z frozen direction

Build directly from S1.42U and cumulatively reapply the accepted/current state without stacking V/W/X/Y tuning DLLs.

Targets:

- Jetpack base acceleration `10 -> 18`;
- More Ship Upgrades Jet Fuel `18 / 18`;
- Jetpack Thrusters `25 / 20`;
- Indoor Pikmin Spawn Chance `0.09`;
- all configured non-Purple Pikmin CarryStrength `3`;
- Purple Pikmin CarryStrength `30`;
- CodeRebirth ACU all 18 curves × `0.5`;
- CodeRebirth G.R.E.G. all 18 curves × `0.5`;
- Functional Microwave Volume `0.15` preserved as accepted;
- Immortal Snail `Rarity = 40`, `Max Snails = 2` preserved.

Jet Fuel `18 / 18` preserves approximately the same absolute acceleration gain per purchased tier as previous tuning:

- W: `16 × 20% = 3.2`;
- X: `32 × 10% = 3.2`;
- Y: `22 × 15% = 3.3`;
- Z: `18 × 18% = 3.24`.

## Acceptance state

S1.42Y is **not promoted** because the user wants a narrower Jetpack target and a slightly higher indoor Pikmin spawn chance. The accepted rollback baseline remains S1.42U until a later candidate passes its own fresh runtime gate.
