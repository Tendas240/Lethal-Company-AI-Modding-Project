# S1.42AB Runtime Acceptance — Interior Weight Normalization

**Date:** 2026-09-04  
**Verdict:** ACCEPTED  
**Canonical accepted full-normal-stack baseline:** S1.42AB

## Accepted profile

Profile:

`Profiles/LC V1 S1.42AB Interior Weight Normalization.r2z`

Gale profile name:

`LC V1 S1.42AB Interior Weight Normalization`

Profile SHA-256:

`3f2387886daaf68d0d55ddc1b3cffb913565a658db0072b11f3b975ff07860ca`

Successful build workflow run:

`33892396551`

Automated build commit:

`9bf3085d82990ca565ad81f992d896855c21f1c6`

Candidate record:

`Current/96_S1.42AB_BUILD_CANDIDATE_INTERIOR_WEIGHT_NORMALIZATION.md`

Injected DLL:

`BepInEx/plugins/S142ABInteriorWeightNormalization/S142ABInteriorWeightNormalization.dll`

DLL SHA-256:

`901c02a8e85d33af24d0aa906faa6052a7de33faa7dfbeeca590bbd8a8f59a06`

## Runtime evidence

Evidence root:

`RuntimeEvidence/S1.42AB/20260904T174010Z/`

Raw log:

`RuntimeEvidence/S1.42AB/20260904T174010Z/raw/LogOutput.log`

Raw LogOutput.log SHA-256:

`42cfba3d157f6abdbeee114909d90749d1bfd043d4b0c224922ad5be976194ae`

Coverage:

- 1,731,946 bytes;
- 17,347 lines;
- 16,087 parsed runtime events;
- 32 Error-severity events;
- Fatal = 0.

The repository runtime-ingest automation consumed the upload and cleared `RuntimeInbox/Current/` back to `.gitkeep`.

## Startup / contract gate — PASS

Fresh runtime proves the exact S1.42AB project-local path loaded and armed:

- `S1.42AB Interior Weight Normalization 1.0.0` loaded;
- dependency `imabatby.lethallevelloader v1.7.12` validated;
- exact LethalLevelLoader dungeon-selection contract validated for post-viability rarity normalization;
- armed marker confirmed that LLL retains full ownership of viability/exclusion membership;
- no contract-refusal marker was observed.

The accepted S1.42Z project-local components also loaded normally, including Jetpack Acceleration, CodeRebirth aerial-defense tuning and Compatibility Fixes `1.3.14`.

## Interior-weight gate — PASS

The test was performed on **Offense**, as required by the candidate contract.

LLL's own pre-Postfix viable-flow report still contained the expected unequal author/planet/mod values, including:

- LiminalHouse `300`;
- Sub Systems `275`;
- Abandoned Foundry `250`;
- Shatteredrooms `75`;
- Lead Factory `70`;
- Spelunkers Caverns (Random) `50`;
- Crimson Keep `35`;
- Gray Apartments `25`;
- DeepcoreMines `25`;
- Belleville Appartements `20`;
- Goldstay Hotel `20`.

Immediately after that pre-normalization report, the authoritative project-local marker showed:

`[InteriorWeightNormalization] Final effective viable pool for Offense: ...`

Runtime result:

- viable entries before normalization: `40`;
- viable entries after normalization: `40`;
- entries changed by the Postfix: `12 / 40`;
- pre-normalization rarity range: `20..300`;
- every positive final effective rarity: exactly `100`;
- Black Mesa appears exactly once and has final rarity `100`;
- Facility, Mineshaft and the expected vanilla/custom viable interiors remain present;
- pre- and post-normalization membership is unchanged;
- no excluded/unviable flow was inserted by the project-local patch.

A second final-pool marker during the actual selection path (`debugResults=False`) again showed all `40` returned viable interiors at exactly `100`.

This directly proves the intended architecture: LethalLevelLoader decides membership first, then S1.42AB normalizes only the positive effective rarity values of that returned set.

## Dungeon generation / gameplay gate — PASS

Offense loaded normally and dungeon generation proceeded successfully. LLL selected `Expanded facility` for the run. The user entered the generated interior and played a normal gameplay round until death, reporting no problematic behavior.

There was no user-visible regression attributable to S1.42AB.

## Regression markers — PASS

Fresh S1.42AB runtime markers:

- `Work state with no task assigned!` = `0`;
- `Leader is null when following` = `0`;
- Compatibility Fixes Error marker = `0`;
- `NetworkObjectReference can only be created from spawned NetworkObjects` = `0`;
- `PikminNoticeZone.OnTriggerStay` regression marker = `0`;
- Fatal = `0`;
- EnemyIsolation marker confirms diagnostic enemy isolation remains disabled.

The S1.42AA two-warning Work/no-task lifecycle finding did **not** reproduce in this AB run.

## Error-severity assessment

The complete log contains `32` Error-severity events, matching the accepted S1.42Z runtime total.

Observed exception classes remain within already-known non-project-local/setup monitor classes, including the loaforcsSoundAPI/HarmonyX `TypeLoadException` during `RoundManager.GenerateNewFloor` and SoftMask/SoftMasking setup `NullReferenceException` warnings.

S1.42AB itself emitted exactly the expected informational validation/normalization messages and no project-local Error-severity flood or refusal condition.

Do not reopen the known monitor-only error classes without stronger reproducibility or user-facing impact.

## Acceptance decision

S1.42AB passes its complete intended runtime gate and is promoted to the **canonical accepted full-normal-stack baseline**.

S1.42Z remains the previous accepted rollback artifact and provenance baseline from which AB was built, but it is no longer the latest accepted baseline.

S1.42AA remains runtime-rejected and must not be revived as the active approach.

## Permanent interior rule now accepted

For every installed interior:

- if LethalLevelLoader does not return it as viable, it remains unavailable;
- if LethalLevelLoader returns it with a positive rarity, its effective rarity is normalized to exactly `100`;
- no flow membership is added, removed, re-registered or deduplicated by the project-local patch;
- Enemy, Scrap and MapObject rarity systems remain untouched.

Future added interiors therefore inherit effective rarity `100` automatically whenever LLL itself considers them viable.

Preserve the Shatteredrooms Experimentation/Embrion restriction until dedicated compatibility evidence proves it safe to remove.

## Permanent invariants preserved

- exact BCMER `1.71.0`; do not silently upgrade to 2.x;
- EnemyIsolation disabled;
- Compatibility Fixes `1.3.14`;
- `BaboonBirdPikminEnemy` enabled;
- narrow Hawk -> Pikmin prevention only;
- native inherited PikminEnemy lifecycle preserved;
- Pikmin -> Baboon Hawk attack remains allowed;
- Puffer -> Pikmin protection;
- Thumper Bite Limit `3`;
- Crawler absent from LethalMin Attack Blacklist;
- accepted S1.42C-derived moon power/spawn baseline;
- SpawnCycleFixes `Consistent Spawn Times = true`;
- Jetpack base acceleration `18`;
- Jet Fuel `18 / 18`;
- Jetpack Thrusters `25 / 20`;
- Indoor Pikmin Spawn Chance `0.09`;
- non-Purple CarryStrength `3` and Purple CarryStrength `30`;
- CodeRebirth ACU exact 18 curves ×`0.5`;
- CodeRebirth G.R.E.G. exact 18 curves ×`0.5`;
- Functional Microwave volume `0.15`;
- Immortal Snail Rarity `40`, Max Snails `2`.

Never repeat the S1.42R whole-component disable strategy.

## Next state

No successor is armed by this acceptance.

`BuildSpecs/current.json` must remain disabled with S1.42AB as the guarded base and an idle-after-acceptance identifier.

`RuntimeInbox/ACTIVE_BUILD.txt` remains `S1.42AB` until another runtime candidate is explicitly designated.

Deferred scopes remain separate and require an explicit next-scope decision. They include:

- BCMER EventTypes fixed equal distribution `8 × 12.5%`;
- Functional Microwave spawn-rarity reduction;
- CullFactory `junkrooms` / `shatteredrooms` exceptions;
- Mausoleum fog reduction;
- Black Mesa table/NavMesh/Pikmin route recovery;
- isolated evaluation of `woah25-LethalEscapeUpdated 2.5.0`;
- final long full-stack acceptance;
- AdditionalNetworking/LethalMin teardown repairs only with stronger evidence;
- cosmetic documentation cleanup.

The BCMER `8 × 12.5%` scope is now technically eligible to be selected next because the S1.42AB gate is closed, but it is **not** armed or implicitly authorized by this acceptance.
