# 28 — S1.42G Oversized Evidence Discarded / BCMER-Off Retest

**Date:** 2026-09-03  
**Game:** Lethal Company V81

## Discarded runtime evidence

The runtime ingest formerly stored at:

`RuntimeEvidence/S1.42G/20260903T100914Z/`

was intentionally deleted from the repository.

Reason:
- the uploaded ZIP expanded to an approximately 40 MB combined log;
- repeated attempts to analyze the complete log through repository tooling stalled;
- the first three user runs were explicitly non-representative:
  1. player death;
  2. player death;
  3. player locked out of the dungeon by a Teleporter Trap;
- the fourth run was not fully and reliably analyzed before the evidence was discarded.

Therefore:
**do not cite or treat that deleted evidence as accepted S1.42G runtime proof.**

S1.42G remains:
**built, not yet cleanly runtime-validated.**

## User observations from the discarded attempt — unconfirmed, must be reproduced

These are retained only as test notes:

- many repeated `[DoorAudit] HangarShipDoor.SetDoorOpen called; currentPower=0.000` messages were visible;
- caller stack included BCMER `HangarShipDoorPatches.OverwriteDoorPower`;
- user suspects correlation with BCMER event `Door System: ERROR`;
- no enemies were seen during the relevant runs;
- the `Enemies` ship-terminal command showed no enemies;
- even around 4:24 PM ingame no enemies were observed;
- Functional Microwaves felt too common and should eventually be made somewhat rarer.

None of those points is considered confirmed from the discarded log.

## Next test variant

Do **not** build a new profile yet.

Use the existing canonical profile:

`Profiles/LC V1 S1.42G Routed Moon Performance Fix.r2z`

SHA-256:
`09364c11f8032645205b869ad760471259520cd57758e4d2d09a35665cf0d35a`

User procedure:
1. import S1.42G with Gale -> Advanced options -> Import all files;
2. manually disable **SoftDiamond-BrutalCompanyMinusExtraReborn** only;
3. make no other profile/config/mod changes;
4. start a fresh test;
5. produce a new, shorter `LogOutput.log` or ZIP if GitHub size requires it.

This manual variant is routed as:

`S1.42G_BCMER_OFF_RETEST`

It is **not a new build** and does not replace the canonical S1.42G archive.

## Retest priorities

Primary:
- confirm routed-moon performance is smooth;
- confirm Coroner Jetpack spam remains gone;
- determine whether isolated enemies now actually populate/spawn;
- use the `Enemies` terminal command at multiple times, including late afternoon.

Secondary:
- see whether the repeated HangarShipDoor / DoorAudit stack spam disappears with BCMER disabled;
- if it disappears, investigate a narrow compatibility patch for BCMER `Door System: ERROR` rather than weakening the general door failsafe;
- note Functional Microwave frequency, but do not rebalance from this test alone unless the observation reproduces clearly.

## Important interpretation rule

If enemies spawn correctly with BCMER disabled, investigate an interaction between BCMER and the temporary EnemyIsolation/spawn ownership path.

If enemies still do not spawn with BCMER disabled, the EnemyIsolation implementation itself remains the primary suspect.

The full normal enemy configuration restore source remains:

`Current/ENEMY_SPAWN_BASELINE_S1.42C.json`
