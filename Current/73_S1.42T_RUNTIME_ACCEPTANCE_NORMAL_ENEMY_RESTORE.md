# 73 — S1.42T Runtime Acceptance: Normal Enemy Restore

**Date:** 2026-09-04  
**Build:** S1.42T — Normal Enemy Restore  
**Verdict:** **PASS**

## Build identity

Profile:

`Profiles/LC V1 S1.42T Normal Enemy Restore.r2z`

SHA-256:

`a2714d04777edc95490398367c9dad2e320b44b664e20e9fe0b0f85d6a5fea10`

Base:

`S1.42S — Baboon Adapter Lifecycle Restore`

Build delta from S1.42S:

- `BepInEx/config/tendas.s139.compatibilityfixes.cfg`
- `Isolated Enemy Regression = true -> false`
- no package state changes;
- no package additions/removals;
- no compatibility-plugin/code changes;
- `export.r2x` changed as expected for the profile metadata.

Compatibility plugin remains:

- version `1.3.14`
- embedded DLL SHA-256 `3fd38c0e8ff76b55c5c335cd9eb867e254a422caea2287fb95d46447e2167960`

BCMER exact `SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0` remained **disabled** for this gate.

## Runtime evidence

Evidence directory:

`RuntimeEvidence/S1.42T/20260903T222109Z/`

Raw log:

`RuntimeEvidence/S1.42T/20260903T222109Z/raw/LogOutput.log`

Raw log size:

`1,965,803 bytes`

Raw log SHA-256:

`b136464c55436fedc1d762aa9d961cea9ef53052d7cf829cdb93a4892184ec8f`

The player died before manually entering `Enemies` in the ship terminal. This does **not** invalidate the gate because the log itself directly proves normal enemy population restoration.

## Normal-enemy restoration — PASS

The run contains real normal/non-isolated enemy population activity. `ADDING ENEMY` events include multiple enemies outside the former focused isolation set, including examples such as:

- Men-stalker;
- Puffer;
- Bagpipe;
- Flowerman / Bracken;
- ImmortalSnail;
- Spring / Coil-Head;
- MouthDog;
- Butler;
- Bunker Spider;
- Aloe;
- HauntedMask.

At disconnect the runtime also tears down active instances including:

- `AloeBracken(Clone)`;
- `SirenHead(Clone)`;
- `menstalker_prefab(Clone)`;
- `SpringMan(Clone)`;
- `Flowerman(Clone)`;
- two `BaboonHawkEnemy(Clone)` instances.

Therefore criterion “normal non-isolated enemies spawn again” is directly runtime-proven.

The terminal `Enemies` command is **non-blocking / redundant for this gate** because direct spawn/runtime-instance evidence is stronger than the manual scan check.

## Regression markers — PASS

Across the complete S1.42T log:

- `Work state with no task assigned!` = **0**
- `Leader is null when following` = **0**
- `[Fatal` = **0**
- `[Error  :S1.39 Compatibility Fixes]` = **0**
- no new compatibility exception flood observed.

The focused S1.42S compatibility repair therefore remains intact after normal enemy restoration.

## Error/exception classification

The full log was searched and compared against accepted S1.42S/S1.41 runtime evidence.

### Known/pre-existing stack noise

The following classes are not S1.42T regressions and were already present in accepted comparison runs or are known non-blocking startup/lobby noise:

- SoftMasking `nunit.framework` FileNotFoundException;
- `loaforcsSoundAPI.LethalCompany` / HarmonyX `TypeLoadException` during dungeon generation reporting;
- duplicate `NetworkPrefab ... GlobalObjectIdHash` messages;
- missing scan-node messages for ship/store items;
- inactive Clipboard/StickyNote coroutine messages;
- `ReservedItemSlotCore` logging `Synced with host!` at Error level despite successful sync.

### S1.42T-specific one-off observation

One startup/audio-load marker appears:

`Failed getting load state of FSB for audio clip "AloeChase"`

It is a one-off load-time message, not a flood, not emitted by the project compatibility plugin, and Aloe later exists as an active runtime enemy instance. No user-facing regression was reported. Classification: **monitor-only / non-blocking**.

### Gameplay phase

The last actual exception/error of interest occurs during setup/dungeon generation around `22:13:09` (the known SoundAPI `TypeLoadException`).

From then through the gameplay run and until the player's death around `22:20:12`, there is no new error/exception cascade.

## Player death classification

The run ends because the local player dies from a normal gameplay Jetpack explosion:

`DeathPlayerJetpackBlast`

This is not a crash, freeze, startup failure, or mod exception.

## Acceptance decision

**S1.42T Normal Enemy Restore = runtime accepted.**

This closes the temporary EnemyIsolation restoration gate.

S1.42T is the newest accepted technical descendant, but **S1.41 remains the last fully accepted normal gameplay baseline** until exact BCMER 1.71.0 is restored and the resulting normal stack passes its separate gate.

## Exact next controlled gate

Next sequential identity:

**S1.42U — BCMER 1.71.0 Reactivation Gate**

Required variable:

- re-enable exact `SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0` only.

Preserve:

- S1.42T normal enemy restoration;
- `Isolated Enemy Regression = false`;
- compatibility plugin v1.3.14 unchanged;
- `Thumper Bite Limit = 3`;
- Crawler absent from LethalMin Attack Blacklist;
- all current normal spawn-owner configs.

Do **not** upgrade BCMER to 2.0.0 and do not mix unrelated tuning into this gate.

## Evidence retention

Keep the S1.42T raw log at least through the BCMER restoration/final normal-stack comparison because it is the clean BCMER-off normal-enemy reference run.

After that dependency is closed, the raw log may be removed under `Current/74_LARGE_RUNTIME_LOG_PIPELINE_AND_RETENTION.md` as long as this acceptance record, provenance SHA-256, and any still-needed compact diagnostic evidence remain.
