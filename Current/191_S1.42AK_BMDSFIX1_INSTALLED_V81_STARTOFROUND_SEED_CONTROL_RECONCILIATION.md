# S1.42AK-BMDSFIX1 Installed-V81 StartOfRound Seed-Control Reconciliation

**Date:** 2026-09-26  
**Status:** EXACT-INSTALLED STARTOFROUND SEED ORIGIN CLOSED / NO VANILLA ASSEMBLY WRITER FOR OVERRIDE FIELDS / ORDINARY DELIBERATE SEED CONTROL NOT PROVEN / TARGET GAMEPLAY GATE NOT RELEASED / NOT ACCEPTED  
**Accepted gameplay baseline:** S1.42AK — unchanged  
**Active candidate:** S1.42AK-BMDSFIX1 — unchanged exact bytes

## Decision

PR #176 integrated the successful repository-native Installed-V81 StartOfRound seed-control capture into `main` at merge commit `d2a64f97f0d404727e6fd41a7dad5d83bf268fc8`. Knowledge Architecture push run `36268695673` / run number `795` passed on that exact head.

This closes the exact-installed upstream `StartOfRound` boundary left open by `Current/190_S1.42AK_BMDSFIX1_INSTALLED_V81_ROUNDMANAGER_SEED_CONTROL_RECONCILIATION.md`. It does not rewrite that historical checkpoint, change gameplay/profile/config/plugin bytes, accept BMDSFIX1, or release a gameplay run.

## Exact evidence identity

- evidence directory: `SourceEvidence/VanillaV81/StartOfRoundSeedControl/20260926T154610Z-0b7be88d/`;
- evidence commit: `58c5d557022bb3078db89d85ece6279c1d885eef`;
- capture parent/main: `4ce830fbec920fa92a0c83ba0859f77a3df2cc1f`;
- `Assembly-CSharp.dll` SHA-256: `5f7db5538b78dc408845a3002907619785ac9f9c6b6059d13dc9a602d9b65731`;
- `Lethal Company.exe` SHA-256: `24f39cbf2060834e8b648833c0c31ed82506ea633a9e8e5609e01102c7d6e8f1`;
- Steam app ID: `1966720`;
- Steam build ID: `22825947`;
- reviewed appmanifest SHA-256: `4974c9249f249053275d93a5ba4f68e92346c1cfa09e89e6fce0b860c0c306a7`;
- focused report SHA-256: `6d778e2ef8e38e77639d28f0118d48a1e158da51107a490570cb9e6b8ccae761`;
- assembly-wide `overrideRandomSeed` occurrences: `2`;
- assembly-wide `overrideSeedNumber` occurrences: `2`.

## Reconciled StartOfRound findings

The exact installed V81 capture establishes the ordinary server-side map-seed path through `StartOfRound`:

1. `StartGameServerRpc()` executes `StartGame()` on the server/host once the start conditions are satisfied.
2. `StartGame()` chooses `randomMapSeed` by exactly three branches in the captured method: `overrideSeedNumber` when `overrideRandomSeed` is true, deterministic weekly challenge generation when `isChallengeFile` is true, otherwise `ChooseNewRandomMapSeed()`.
3. `ChooseNewRandomMapSeed()` assigns `UnityEngine.Random.Range(1, 100000000)`.
4. `OpenShipDoors()` forwards the resulting `randomMapSeed` unchanged into `RoundManager.Instance.LoadNewLevel(randomMapSeed, currentLevel)`.
5. The assembly-wide decompiled-reference scan finds only two occurrences of each override field: the `StartOfRound` declarations and the `StartGame()` consumption. No decompiled `Assembly-CSharp.dll` writer that sets `overrideRandomSeed` or `overrideSeedNumber` is present in the captured evidence.

Together with the exact-installed RoundManager evidence from record 190, the map-seed-to-dungeon-selection chain is now closed end-to-end for Vanilla V81. Candidate map seed `1061420` remains a justified Deep Sewers prediction for the preserved 31-entry equal-rarity Black Mesa pool.

## What this does and does not prove

The exact-installed Vanilla path does **not** prove an ordinary player-facing way to deliberately supply arbitrary seed `1061420`:

- the normal non-challenge branch chooses a fresh random seed;
- the challenge branch derives its seed from the week number and does not expose arbitrary map-seed entry in this evidence;
- the override fields are consumed by `StartGame()`, but the exact decompiled `Assembly-CSharp.dll` scan contains no writer for them.

This closes the proposed simple Vanilla override route. It does not, by itself, prove that no already-installed ordinary mod/config mechanism outside `Assembly-CSharp.dll` can interact with that surface. That narrower normal-stack question must be resolved before declaring the deliberate-seed path impossible across the whole installed profile.

A newly introduced Harmony/plugin/memory writer, deterministic dungeon selector, viable-pool mutation, weight mutation, or pre-selection `LevelRandom` consumption would create the desired outcome rather than prove an existing ordinary mechanism and remains unauthorized for qualification.

## Exact next action

Perform a bounded repository-native **installed normal-stack seed-control availability review** limited to mechanisms already present in the exact S1.42AK-BMDSFIX1 profile. Determine whether any existing ordinary non-diagnostic package/config/plugin mechanism can deliberately supply map seed `1061420` through the native StartOfRound/RoundManager path without adding new code, selecting the dungeon directly, changing the viable pool/weights, or consuming `LevelRandom` before native selection.

If such an existing ordinary mechanism is proven, reconcile it before releasing the regular Black Mesa x `DeepSewersFlow` target gameplay test. If no such mechanism is present, record that limitation explicitly and keep the target gameplay gate outstanding without returning to blind rerolls.

## Preserved lifecycle

- S1.42AK remains the sole accepted gameplay baseline.
- S1.42AK-BMDSFIX1 remains active / not accepted.
- DIAG1PATH1 remains supporting diagnostic evidence only / NEVER ACCEPT.
- The regular exact-byte Black Mesa x `DeepSewersFlow` gameplay qualification remains outstanding and unwaived.
- Blind rerolling solely to hit Deep Sewers remains disallowed.
- `BuildSpecs/current.json` remains disabled at `IDLE_UNIVERSAL_INTERIOR_VIABILITY_ANALYSIS`.
- `RuntimeInbox/ACTIVE_BUILD.txt` remains `S1.42AK-BMDSFIX1`.
- No gameplay/profile/config/package/plugin bytes are changed by this reconciliation.
