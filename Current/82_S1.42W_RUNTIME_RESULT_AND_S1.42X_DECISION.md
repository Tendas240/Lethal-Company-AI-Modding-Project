# 82 — S1.42W Runtime Result and S1.42X Decision

**Date:** 2026-09-04  
**Candidate tested:** S1.42W — Lift-Off Microwave Retune  
**Profile SHA-256:** `f34ebcf18bd2b475da5546e6c391bd15bf70df5648b5f69ffb668d196df057dc`

## Runtime evidence

Evidence directory:

`RuntimeEvidence/S1.42W/20260904T105709Z/`

Raw log:

`RuntimeEvidence/S1.42W/20260904T105709Z/raw/LogOutput.log`

Raw log SHA-256:

`2610e250d738a8fcb2dadda18262b89d87f527f27d8e854f59044dc4cdca8a52`

Runtime window: approximately 10:19:29–10:56:20, 97,270 lines / 95,707 parsed events.

## Technical verdict

**TECHNICAL GATE: PASS ENOUGH TO ITERATE / GAMEPLAY TUNING OBJECTIVE: REJECTED**

The S1.42W profile did not show a critical compatibility/lifecycle failure requiring rollback to a repair branch:

- Fatal = 0;
- Work/no-task = 0;
- Leader-null = 0;
- S1.39 Compatibility Fixes error = 0;
- unspawned NetworkObjectReference marker = 0;
- PikminNoticeZone unspawned-reference regression = 0;
- normal enemy activity remained present across the run;
- JetpackFixes remained active;
- no crash/freeze was reported.

Known loaforcsSoundAPI/HarmonyX setup error-class markers remain non-blocking unless they change behavior or flood beyond the established baseline.

## User-observed tuning result

### Jetpack

S1.42W set the project-local base acceleration target to `16f`.

User report:

- initial straight-up lift-off still did not feel materially different;
- therefore the S1.42W Jetpack magnitude is rejected as a tuning result even though the technical patch path remains valid.

Decision:

- next diagnostic target is `32f` (double S1.42W's 16f);
- this is intentionally an aggressive diagnostic step, not a claim that 32f is the final balanced value;
- if 32f still fails to produce a clear vertical lift-off difference, stop increasing `jetpackAcceleration` and identify the actual lift-force/ramp owner instead.

### Functional Microwave

S1.42W deterministic config remains:

`Functional Microwave | Volume = 0.15`

User could not find a Functional Microwave in the tested runs, so subjective volume validation is **UNTESTED**, not failed.

Carry `0.15` forward unchanged until the object is encountered.

## New requested S1.42X tuning

1. Jetpack diagnostic base acceleration `16 -> 32`.
2. Rebalance More Ship Upgrades Jet Fuel so the free 32f base does not make percentage tiers disproportionately strong.
3. Keep Jetpack Thrusters useful as a separate maximum-speed layer.
4. Halve all CodeRebirth Air Control Unit / Aerial Defense System spawn curves while preserving their relative Moon/tag curve shapes.
5. Reduce LethalMin indoor Pikmin spawn chance modestly from `0.10 -> 0.08`.
6. Reduce Pikmin CarryStrength proportionally by 25%:
   - Blue/Red/Yellow/White/Winged/Rock/Ice/Glow/Bulbmin `4 -> 3`;
   - Purple `40 -> 30`.
7. Preserve Microwave volume `0.15` and Immortal Snail `Rarity = 40`, `Max Snails = 2`.

## LethalMin config-location finding

The apparent missing Gale settings are not missing from the profile. LethalMin stores them primarily as raw BepInEx configuration files:

- main config: `BepInEx/config/NoteBoxz.LethalMin.cfg`;
- per-type configs: `BepInEx/config/Pikmin/*.cfg`.

Only a subset of LethalMin settings is exposed through the Gale/LethalConfig UI. The raw files are authoritative for the remaining settings.

Current S1.42W main value:

`Indoor Pikmin Spawn Chance = 0.10`

Current CarryStrength baseline:

- all non-Purple configured Pikmin types = 4;
- Purple = 40.

## CodeRebirth ACU implementation finding

CodeRebirth 1.6.9 identifies the object as:

`code_rebirth:air_control_unit`

Display name:

`Air Control Unit`

Its Outside Moon/tag spawn curves are supplied through DawnLib/Dusk `MapObjectSpawnMechanics`; the shipped definition includes vanilla/custom, CodeRebirth tag curves and explicit vanilla Moon curves. There is no simple ACU spawn-rate key in the existing canonical `CodeRebirth.cfg` snapshot.

S1.42X therefore uses a narrow project-local DawnLib provider tuner rather than editing the CodeRebirth binary or globally patching `RoundManager.SpawnOutsideHazards`.

Safety contract:

- exact CodeRebirth 1.6.9;
- exact DawnLib/Dusk 0.9.25;
- exact `code_rebirth:air_control_unit` lookup;
- exact expected 18-key curve set required;
- scale only that provider's curve values and tangents by `0.5` after DawnLib's Moon-freeze rebuild;
- no other map-object provider is touched;
- fail closed on version/key/provider/curve-contract drift.

## Monitor-only new W observation

One `InvalidOperationException: Collection was modified; enumeration operation may not execute` was observed in the LethalMin `PikminManager.DespawnLumiknulls()` teardown/despawn path.

Current classification:

**MONITOR ONLY**

Reason:

- teardown/despawn context;
- no crash/freeze;
- no gameplay cascade established;
- unrelated to the requested tuning scope.

Escalate only if it repeats outside teardown, becomes frequent, or produces user-visible lifecycle problems.

## Accepted baseline remains unchanged

S1.42U remains the last fully accepted full-normal-stack rollback baseline:

`Profiles/LC V1 S1.42U BCMER 1.71.0 Reactivation Gate.r2z`

SHA-256:

`ff5fdebf22fefdd5515b95677174290f9666e491447138f074e5b65673173969`
