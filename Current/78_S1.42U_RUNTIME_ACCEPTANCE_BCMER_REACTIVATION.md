# 78 — S1.42U Runtime Acceptance: BCMER 1.71.0 Reactivation

**Date:** 2026-09-04  
**Build:** S1.42U — BCMER 1.71.0 Reactivation Gate  
**Verdict:** **PASS — full normal stack restored and runtime accepted**

## Build identity

Profile:

`Profiles/LC V1 S1.42U BCMER 1.71.0 Reactivation Gate.r2z`

SHA-256:

`ff5fdebf22fefdd5515b95677174290f9666e491447138f074e5b65673173969`

Build verification:

`Current/77_S1.42U_BUILD_VERIFICATION_BCMER_REACTIVATION.md`

Exact build delta from S1.42T:

- `SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0`: disabled -> enabled;
- no package upgrade;
- no config patch;
- no package addition/removal;
- no compatibility-code/DLL change.

## Runtime evidence

Evidence directory:

`RuntimeEvidence/S1.42U/20260904T082412Z/`

Raw log:

`RuntimeEvidence/S1.42U/20260904T082412Z/raw/LogOutput.log`

Raw log size:

`1,959,356 bytes`

Raw log SHA-256:

`0a2e0839b106a7d6f9867d186a835252bc72a869ef63a62517ae1971fd93c5fc`

Line count:

`19,939`

The user reported no gameplay-visible technical problem during the run.

## BCMER activation — PASS

Runtime proves the exact intended package loaded:

`Loading [BrutalCompanyMinusExtraReborn 1.71.0]`

and completed patching:

`BrutalCompanyMinusExtraReborn 1.71.0 is done patching.`

BCMER also detected its expected optional integrations, including CodeRebirth, Biodiversity, CullFactory and other installed content.

No BCMER-driven catastrophic event/system regression was observed or reported.

## Compatibility / anti-regression state — PASS

Runtime proves the correct project-local compatibility plugin loaded:

`Loading [S1.39 Compatibility Fixes 1.3.14]`

The accepted narrow Baboon-Hawk/Pikmin lifecycle path remained active and `BaboonBirdPikminEnemy` remained enabled.

EnemyIsolation marker:

`[EnemyIsolation] Diagnostic enemy isolation is disabled.`

Critical complete-log counters:

- `Work state with no task assigned!` = **0**;
- `Leader is null when following` = **0**;
- `[Error  :S1.39 Compatibility Fixes]` = **0**;
- `[Fatal` = **0**;
- old disconnect-only `NetworkObjectReference can only be created from spawned NetworkObjects` = **0**;
- `PikminNoticeZone.OnTriggerStay` = **0**.

No new compatibility exception flood occurred.

## Normal enemy population — PASS

`ADDING ENEMY` count:

**13**

The run contains ordinary non-isolated population activity including examples such as Bunker Spider, Aloe, Crawler, ImmortalSnail, RollingGiant, Janitor, Jester and Butler.

Direct runtime power evidence from this run includes:

- Aloe = **1** power;
- ImmortalSnail = **1** power;
- Janitor = **1** power;
- Crawler = **3** power;
- Bunker Spider = **2** power.

Therefore re-enabling BCMER did not regress the S1.42T normal-enemy restoration.

## Error / exception classification

The analyzer counted 17 occurrences containing `Exception`, but the last such occurrence is at approximately `08:10:42`, during setup/dungeon generation, and is the already known loaforcsSoundAPI/HarmonyX `TypeLoadException` class.

Two `NullReferenceException` occurrences are SoftMaskKiller-handled SoftMask setup noise.

After setup, the gameplay period contains no new exception cascade. No crash/freeze occurred and the user reported clean gameplay.

Classification:

**known/pre-existing non-blocking stack noise; not an S1.42U regression.**

## Acceptance decision

**S1.42U = runtime accepted.**

This closes the BCMER reactivation gate and restores the full normal stack on the accepted S1.42 technical descendant.

S1.42U now becomes:

- the newest runtime-accepted technical descendant; and
- the newest fully accepted full-normal-stack gameplay baseline.

S1.41 remains historically important as the previous full-normal-stack baseline but no longer holds the active acceptance role.

## Configuration integrity checks after acceptance

The S1.42U `LethalLevelLoader.cfg` is byte-identical to the canonical S1.42C restore baseline. Therefore all stored per-moon enemy power-count overrides and spawn-pool settings in that file remain restored exactly.

The S1.42U SpawnCycleFixes config is also byte-identical to S1.42C and retains `Consistent Spawn Times = true`, preserving the intended first-wave timing behavior.

## Next tuning stage

Next sequential plan:

`BuildSpecs/S1.42V_PLAN.md`

Confirmed requested tuning items:

- ImmortalSnail spawn rarity/weight: `80 -> 40`;
- CodeRebirth Functional Microwave volume: `0.7 -> 0.5` proposed after 0.7 remained subjectively too loud;
- modest Jetpack acceleration increase requested, but exact implementation must not be guessed or achieved through a broad handling-mode change.

Do not arm S1.42V until its exact Jetpack implementation/scope is technically resolved.
