# S1.42AH Runtime Acceptance — Mouth Dog Pikmin Dual Prevention

**Date:** 2026-09-09  
**Status:** ACCEPTED FULL NORMAL STACK  
**Previous accepted baseline:** S1.42AF — Path-Length-Safe Microwave Packaging  
**Build:** S1.42AH — Mouth Dog Pikmin Dual Prevention

## Accepted identity

Profile: `Profiles/LC V1 S1.42AH Mouth Dog Fix.r2z`  
Gale profile name: `LC V1 S1.42AH Mouth Dog Fix`  
Profile SHA-256: `06e07fe6805e5e41786c16b5c1ea2132c4f65b385517c902f8aa566ccf49cd4e`  
Compatibility DLL SHA-256: `bf86f338dba1428327088f0aaa2af8d9816f647c3b3c12214a5fc52db8e34573`  
Build workflow run: `34141360051` — SUCCESS  
Automated build commit: `fdb6b94e34144f860f6ac6eb2fd5bdbdd7797ef5`  
Parent: exact accepted S1.42AF.

Candidate record: `Current/139_S1.42AH_BUILD_CANDIDATE_MOUTHDOG_DUAL_PREVENTION.md`  
Prior partial runtime decisions: `Current/140_S1.42AH_RUNTIME_PARTIAL_VALIDATION_MOUTHDOG_COLLISION_PLAYER.md`, `Current/141_S1.42AH_RUNTIME_PARTIAL_VALIDATION_REVERSE_DIRECTION_ADAPTER.md`  
Readable profile snapshot: `ProfileSources/S1.42AH/`

## Final decisive runtime evidence

Final uploaded evidence:

`RuntimeEvidence/S1.42AH/20260909T162513Z/`

Complete runtime log SHA-256:

`ae57fb71a38952936e9056150240e2eaa70be9d89253b15644b3f5d35dd09729`

The user's instruction for this decision is to ignore the first gameplay run in this log and use only the **last run** as the final neighbor-path evidence. This acceptance follows that constraint.

In the decisive last run the runtime log records:

- `16:20:55.175` — `Spawned enemy: MouthDog`;
- `16:20:55.199` — `Spawned enemy: Redwood Titan`;
- `16:20:55.206` — a second `Spawned enemy: MouthDog`;
- `16:23:09.749` — `Kill enemy called! destroy: False`;
- `16:23:09.751` — `Playing death sound for enemy: Redwood Titan`;
- `16:23:09.751` — `RedwoodTitan(Clone) from the exterior enemy list has died` and its exterior power is removed.

The user deliberately performed the final MouthDog/non-Pikmin-neighbor test and reported that a Mouth Dog appeared to kill the Redwood Titan. The runtime log independently confirms the relevant MouthDogs and Redwood Titan were present in that last run and that the Redwood Titan actually reached the normal enemy-death path during the exercised sequence. The log format does not emit an attacker identity on the Redwood Titan death line, so this record does **not** claim a stronger per-hit attacker log than exists. The final decision is based on the deliberate observed interaction together with the runtime death evidence and the exact source/patch contract below.

## Exact non-Pikmin pass-through contract

The S1.42AH `MouthDogAI.OnCollideWithEnemy(Collider, EnemyAI)` Harmony prefix is prevention-only. Its exact runtime type gate returns `true` when `collidedEnemy` is null or is **not** an instance of resolved `LethalMin.PikminAI`; only validated Pikmin instances return `false` and skip the Dog override.

The exact Vanilla V81 focused source evidence for `MouthDogAI.OnCollideWithEnemy` proves the native non-same-type collision path:

```text
if (!(collidedEnemy.enemyType == enemyType) && !(timeSinceHittingOtherEnemy < 1f))
{
    if (currentBehaviourStateIndex == 2 && !inLunge)
    {
        ...
        EnterLunge();
    }
    timeSinceHittingOtherEnemy = 0f;
    collidedEnemy.HitEnemy(2, null, playHitSFX: true);
}
```

Therefore the exact repository-native V81 neighbor contract is **generic MouthDog -> non-Pikmin `EnemyAI` `OnCollideWithEnemy` pass-through with native `HitEnemy(2)` behavior**.

Earlier lifecycle documents used `BiteKillEnemyAI` as shorthand for this neighbor gate. The focused V81 source authority does not expose that as the exact method name here; future current-state wording should prefer the exact `OnCollideWithEnemy` / `HitEnemy(2)` contract.

No `[MouthDogVanillaCollisionGuard]` block marker is emitted for the Redwood Titan sequence. That is consistent with the exact prefix design: the guard logs only when a validated `LethalMin.PikminAI` collision is skipped, while non-Pikmin `EnemyAI` passes through unchanged.

## Previously completed runtime gates

The accepted decision incorporates the two earlier targeted S1.42AH runtime records:

1. exact adapter and Vanilla collision patch installation;
2. repeated live MouthDog -> Pikmin collision prevention;
3. MouthDog -> player behavior preserved, including real mauling/death coverage;
4. explicitly thrown Pikmin -> MouthDog latch / `AttackEnemy` / repeated damage;
5. Pikmin combat can kill the MouthDog;
6. native death cleanup returns attackers to idle, removes attack task/leader and unlatches them;
7. dead MouthDog body transitions into native Pikmin corpse carrying;
8. adapter prevention executes before bite/grab mutation;
9. ordinary audible-noise response remains active;
10. known project regression markers remain clean in the targeted evidence.

The final last-run Redwood Titan sequence closes the only remaining closest-neighbor contract: non-Pikmin EnemyAI pass-through through the exact patched Vanilla collision surface.

## Patch-safety decision

S1.42AH satisfies the permanent project-local patch safety policy:

- exact smallest reviewed interception surfaces;
- prevention before mutation;
- no whole-component disable;
- no broad `EnemyAI` fallback scan;
- no manual Pikmin state reconstruction;
- Enemy -> Pikmin harmful paths blocked;
- Pikmin -> Enemy native lifecycle retained;
- MouthDog -> player retained;
- MouthDog -> non-Pikmin EnemyAI neighbor behavior retained.

## Acceptance decision

**S1.42AH is ACCEPTED as the new full-normal-stack gameplay baseline.**

S1.42AF remains preserved as the accepted predecessor and rollback/provenance point. S1.42AG remains runtime-rejected partial-fix evidence.

There is no remaining S1.42AH runtime gate and no active candidate after this decision.

The next independent gameplay/config scope is the already documented BCMER ShyGuy interior-only correction under `BuildSpecs/DEFERRED_BCMER_SHYGUY_INTERIOR_ONLY_PLAN.md`. Acceptance of S1.42AH makes that scope eligible for successor preparation, but this acceptance does not itself build or arm a successor.