# S1.42AK Build Candidate — LC Office Camera Enemy Balance

**Date:** 2026-09-18  
**Status:** BUILD PASS / STATIC DELTA VERIFIED / ACTIVE RUNTIME CANDIDATE / NOT ACCEPTED  
**Accepted gameplay baseline:** `S1.42AI`

## Candidate identity

- Build: `S1.42AK`
- Profile: `Profiles/LC V1 S1.42AK LC Office Camera Enemy Balance.r2z`
- Gale profile name: `LC V1 S1.42AK LC Office Camera Enemy Balance`
- SHA-256: `b39aa550a517ec727de6eb1ae825383933047d3c556cb6e8d4aa7611c9f89dee`
- Parent: exact `S1.42AJ` / `7c1441aeb0732208bb8e910d89348c2e0129ce202422103a025e8f8aea707dba`
- Successful build workflow run: `35366580975`
- Automated build commit: `395f8298230d343b523eeda3afbfc9253a281931`
- Build plan: `BuildSpecs/S1.42AK_PLAN.md`
- Static evidence: `BuildSpecs/S1.42AK_BUILD_EVIDENCE/STATIC_VERIFICATION.md`
- Readable snapshot: `ProfileSources/S1.42AK/`
- File index: `ProfileSources/S1.42AK/FILE_INDEX.json`

## Proven static boundary

S1.42AK was built directly from exact S1.42AJ, never from DIAG1/DIAG2. The verified archive delta is exactly:

1. `export.r2x`: Gale profile name plus `YaBoiDucki-men_stalker 3.1.2` enabled `true -> false`;
2. `BepInEx/config/me.biodiversity.aloe.cfg`: `[SpawnSettings] PowerLevel = 1 -> 0`;
3. added `BepInEx/config/Piggy.LCOffice.cfg` with `[General] Camera Frame Speed = 0`.

`BepInEx/config/wexop.random_enemies_size.cfg` is byte-identical to S1.42AJ. No LC Office scrap tuning and no diagnostic force-selection bytes are present.

## Full-normal runtime gate

This run is the balanced successor regression/acceptance gate. Use the normal profile only; do **not** use DIAG1/DIAG2 force-selection.

Required observations/evidence:

1. normal BepInEx startup, main menu and lobby/game start complete without a new project-critical failure or persistent error flood;
2. the active profile resolves as exact S1.42AK and the normal Gale import/materialization checks pass;
3. Men-stalker remains disabled: no package activation/load or Men-stalker runtime spawn attributable to the disabled package should appear;
4. ordinary enemy spawning and navigation remain healthy; Aloe may spawn normally and must not cause a new regression;
5. ordinary scrap/interior generation remains healthy; LC Office scrap quantity is **not** being tuned in this build;
6. inherited Interior Weight Normalization and other accepted gameplay contracts remain healthy;
7. play one ordinary unforced day on Offense, the same moon used for the LC Office evidence series, long enough to exercise normal interior/enemy/scrap behavior;
8. if LC Office is selected naturally, enter it, use the elevator repeatedly where practical, remain in/around the office for meaningful coverage and report whether the characteristic repeated DIAG1 stutter returns;
9. if another interior is selected, the run is still valid full-normal regression evidence. Do not rerun solely to force LC Office: exact DIAG2 already provides the targeted LC Office generation/elevator/camera A/B evidence. Whether any additional natural-office coverage is needed will be decided from the uploaded log plus the user's observations.

S1.42AI remains the sole accepted gameplay baseline until an explicit runtime decision promotes S1.42AK.

## Exact ready-to-test commands

Repository-driven Gale v2.4 replacement/import one-liner:

```powershell
$u='https://raw.githubusercontent.com/Tendas240/Lethal-Company-AI-Modding-Project/main/RuntimeTools/ReplaceActiveGaleProfileV24.ps1?cb='+[DateTime]::UtcNow.Ticks;iex (iwr -UseBasicParsing $u).Content
```

Exact S1.42AK autonomous runtime-log uploader:

```powershell
$src=Join-Path $env:APPDATA 'com.kesomannen.gale\lethal-company\profiles\LC V1 S1.42AK LC Office Camera Enemy Balance\BepInEx\LogOutput.log';if(!(Test-Path -LiteralPath $src)){throw "Log not found: $src"};$gh=(Get-Command gh -ErrorAction SilentlyContinue).Source;if(!$gh){$gh=@("$env:ProgramFiles\GitHub CLI\gh.exe","$env:LOCALAPPDATA\Programs\GitHub CLI\gh.exe","${env:ProgramFiles(x86)}\GitHub CLI\gh.exe")|Where-Object{$_ -and (Test-Path -LiteralPath $_)}|Select-Object -First 1};if(!$gh){winget install --id GitHub.cli -e --source winget --accept-package-agreements --accept-source-agreements;$gh=(Get-Command gh -ErrorAction SilentlyContinue).Source;if(!$gh){$gh=@("$env:ProgramFiles\GitHub CLI\gh.exe","$env:LOCALAPPDATA\Programs\GitHub CLI\gh.exe","${env:ProgramFiles(x86)}\GitHub CLI\gh.exe")|Where-Object{$_ -and (Test-Path -LiteralPath $_)}|Select-Object -First 1}};if(!$gh){throw 'GitHub CLI gh.exe could not be found after resolution/install attempt'};& $gh auth status --hostname github.com *> $null;if($LASTEXITCODE -ne 0){& $gh auth login --hostname github.com --git-protocol https --web;if($LASTEXITCODE -ne 0){throw 'GitHub authentication failed'}};$repo='Tendas240/Lethal-Company-AI-Modding-Project';$dst='RuntimeInbox/Current/LogOutput.log';$sha=(& $gh api "repos/$repo/contents/$dst" --jq '.sha' 2>$null);$p=@{message='Upload S1.42AK runtime log';content=[Convert]::ToBase64String([IO.File]::ReadAllBytes($src));branch='main'};if($sha){$p['sha']=$sha};($p|ConvertTo-Json -Compress)|& $gh api --method PUT "repos/$repo/contents/$dst" --input -;if($LASTEXITCODE -ne 0){throw 'Runtime log upload failed'}
```

Run the uploader **after** the gameplay run. If the gameplay run already happened and only the log is pending, use the uploader without replaying the run.
