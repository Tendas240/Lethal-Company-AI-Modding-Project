# 70 — Post-S1.42S Normal Enemy Restore Contract

**Date:** 2026-09-03  
**Status:** binding next-step contract  
**Current technical descendant:** S1.42S — Baboon Adapter Lifecycle Restore  
**Do not build an unrelated successor before this restore step.**

## Why this exists

The focused enemy-isolation regression chain is complete for the Baboon-Hawk/Pikmin lifecycle problem.

S1.42S passed the focused runtime gate:

- evidence: `RuntimeEvidence/S1.42S/20260903T205550Z/`
- log SHA-256: `9e0f771144ceb1679f340d5df7ff393df92a8541d7cfe27231a60bd514c6bfea`
- acceptance: `Current/69_S1.42S_RUNTIME_ACCEPTANCE_BABOON_PIKMIN_LIFECYCLE.md`

The project can now remove the temporary EnemyIsolation diagnostic.

## Canonical restore baseline

`Current/ENEMY_SPAWN_BASELINE_S1.42C.json`

Canonical baseline profile:

`Profiles/LC V1 S1.42C Pikmin Enemy Guard.r2z`

SHA-256:

`22901e5459be4e10d30bb9011bb25e80899bd8b9838a9f487d2a800559777eb3`

The baseline rule remains binding:

> remove only the temporary diagnostic enemy-isolation layer and restore/carry forward enemy-related configuration from S1.42C unless a later explicitly accepted gameplay change supersedes a value.

## Verified S1.42C -> S1.42S enemy-config comparison

The following stored enemy/spawn-owner configs are byte-identical between S1.42C and S1.42S:

- `BepInEx/config/LethalLevelLoader.cfg`
- `BepInEx/config/BrutalCompanyMinusExtraReborn/AllEnemies.cfg`
- `BepInEx/config/BrutalCompanyMinusExtraReborn/CoreProperties.cfg`
- `BepInEx/config/BrutalCompanyMinusExtraReborn/LevelProperties.cfg`
- `BepInEx/config/com.github.biodiversitylc.Biodiversity.cfg`
- `BepInEx/config/nomnomab.rollinggiant.cfg`
- `BepInEx/config/Scopophobia.cfg`
- `BepInEx/config/Ccode.SirenHead.cfg`
- `BepInEx/config/VectorV.TheCabinet.cfg`

Therefore the isolated spawn restriction is not hidden in those normal spawn-owner configs.

## Exact temporary diagnostic to remove

S1.42S contains:

`BepInEx/config/tendas.s139.compatibilityfixes.cfg`

with:

```ini
[Diagnostics]
Isolated Enemy Regression = true
```

The next controlled build must set this to:

`Isolated Enemy Regression = false`

or otherwise remove only that temporary test-only activation while keeping the compatibility plugin itself.

Do **not** remove the compatibility DLL.

## Explicitly accepted later LethalMin deltas to preserve

`BepInEx/config/NoteBoxz.LethalMin.cfg` differs from S1.42C in exactly two relevant accepted values:

### Preserve

`Thumper Bite Limit = 3`

S1.42C had 0. The later value 3 is an intentional accepted gameplay decision and must not be restored to 0.

### Preserve

`Crawler` is **not** in the Attack Blacklist.

S1.42C had:

`..., Leaf boy,Crawler`

S1.42S has:

`..., Leaf boy`

This later change is intentional so Pikmin -> Thumper/Crawler attack remains enabled.

Do not re-add `Crawler` during restore.

## BCMER state

Exact package in S1.42S:

`SoftDiamond-BrutalCompanyMinusExtraReborn 1.71.0`

Current state:

**disabled**

Do not upgrade to 2.0.0 or any other version.

For attribution safety, the preferred next stage is:

1. restore normal enemy population with EnemyIsolation off;
2. keep exact BCMER 1.71.0 disabled for that first restore validation;
3. if normal enemy restoration passes, re-enable exact BCMER 1.71.0 in a separate controlled stage using the accepted S1.41 BCMER configuration.

This keeps the two variables separate.

## Permanent S1.42S compatibility fixes to preserve

Compatibility plugin source:

`Patches/S139CompatibilityFixes/Plugin.cs`

Current version:

**1.3.14**

S1.42S embedded DLL SHA-256:

`3fd38c0e8ff76b55c5c335cd9eb867e254a422caea2287fb95d46447e2167960`

Do not regress:

- BaboonBirdPikminEnemy must remain enabled;
- block only the proven Hawk -> Pikmin entry points;
- preserve native PikminEnemy death/unlatch lifecycle;
- preserve Pikmin -> Baboon Hawk attack;
- preserve Thumper/Crawler counterattack capability;
- preserve Puffer -> Pikmin protection;
- preserve all other already accepted compatibility fixes.

Every new/changed patch must follow:

`Current/68_PROJECT_LOCAL_PATCH_SAFETY_AND_REGRESSION_POLICY.md`

## Recommended next build identity

Next sequential build ID:

**S1.42T**

Recommended name:

**Normal Enemy Restore**

Recommended profile:

`Profiles/LC V1 S1.42T Normal Enemy Restore.r2z`

This is a plan identity only. At handover time S1.42T has **not** been built.

## S1.42T first runtime gate

With EnemyIsolation off and BCMER still disabled:

- normal non-isolated enemy population must appear again;
- terminal EnemyScan should list normal active enemies;
- no startup crash/freeze;
- accepted Pikmin interaction fixes must remain intact;
- no Work/no-task loop;
- no Leader-null loop;
- no new compatibility exception flood;
- do not require another focused Baboon-Hawk stress test unless the restore build changes the relevant patch code.

## Do not do

- do not revert all configs wholesale to S1.42C;
- do not restore `Thumper Bite Limit` to 0;
- do not re-add Crawler to LethalMin Attack Blacklist;
- do not remove the v1.3.14 compatibility plugin;
- do not re-enable/upgrade BCMER in the same first restore gate unless there is new explicit reason;
- do not build a new custom patch merely for the monitor-only disconnect exception;
- do not perform unrelated repository migration or gameplay tuning in the same build.
