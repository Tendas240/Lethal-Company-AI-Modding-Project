# 69 — S1.42S Runtime Acceptance: Baboon/Pikmin Lifecycle Restored

**Date:** 2026-09-03  
**Build:** S1.42S — Baboon Adapter Lifecycle Restore  
**Verdict:** **PASS for the focused isolated Baboon-Hawk/Pikmin regression gate**

## Evidence

Runtime evidence:

`RuntimeEvidence/S1.42S/20260903T205550Z/`

Raw log:

`RuntimeEvidence/S1.42S/20260903T205550Z/raw/LogOutput.log`

Log SHA-256:

`9e0f771144ceb1679f340d5df7ff393df92a8541d7cfe27231a60bd514c6bfea`

Profile:

`Profiles/LC V1 S1.42S Baboon Adapter Lifecycle Restore.r2z`

Profile SHA-256:

`addc5f0cd2508bf821e4e8eda80aca0f94234c7f2823c9acc6e8655060790fee`

Compatibility plugin:

**v1.3.14**

Embedded DLL SHA-256:

`3fd38c0e8ff76b55c5c335cd9eb867e254a422caea2287fb95d46447e2167960`

## Startup / patch verification

The correct plugin loaded:

`Loading [S1.39 Compatibility Fixes 1.3.14]`

Native lifecycle ownership marker was present:

`[LethalMinNativeOwnership] Pikmin -> enemy combat, native PikminEnemy death detection, latch removal, TaskEnd/SetToIdle, enemy-body carry, and Onion delivery remain owned by LethalMin. Baboon Hawk compatibility blocks only Hawk -> Pikmin entry points; the BaboonBirdPikminEnemy component stays enabled.`

Exact S1.42S guard marker was present:

`[BaboonHawkPikminGuard] One-way Hawk -> Pikmin protection initialized; collisionPatched=True; bitePatched=True; BaboonBirdPikminEnemy remains ENABLED so native PikminEnemy.Update death/unlatch cleanup can run.`

Forbidden old marker was absent:

`[BaboonHawkPikminGuard] Disabled LethalMin.BaboonBirdPikminEnemy`

The removed S1.42R dead-target bridge marker was also absent.

## Focused reproduction

The three relevant attackers were:

- `Yellow Pikmin_IkVZWQe`
- `Yellow Pikmin_3wnH`
- `Yellow Pikmin_ruCpzY`

All three attacked the same Baboon Hawk.

The Hawk died at approximately:

`20:52:32.546`

Confirmed by:

- `Client #0 hit enemy BaboonHawkEnemy(Clone) with force of 1.`
- `Kill enemy called! destroy: False`
- `Playing death sound for enemy: Baboon hawk`
- Starlancer AI Fix death/power removal.

## Native post-death lifecycle

### Yellow Pikmin_IkVZWQe

At `20:52:32.552`, approximately 6 ms after Hawk death:

- `Setting to idle on local client`
- `Removing current task`
- `Unlatching Pikmin: Yellow Pikmin_IkVZWQe`
- collision mode restored to 1.

No further `Hitting enemy with: 0.03` occurred after Hawk death.

The Pikmin was later reclaimable and the log confirms:

`Assigned Leader: MasaeAhmya (0)`

### Yellow Pikmin_3wnH

At `20:52:32.555`, approximately 9 ms after Hawk death:

- `Setting to idle on local client`
- `Removing current task`
- `Unlatching Pikmin: Yellow Pikmin_3wnH`
- collision mode restored to 1.

No further attack hits occurred after Hawk death.

The Pikmin then naturally found the dead Hawk body, switched to `CarryItem`, grabbed it, helped route it to the Onion, completed the carry task, and was later reclaimable:

`Assigned Leader: MasaeAhmya (0)`

### Yellow Pikmin_ruCpzY

At `20:52:32.675`:

- `Task finished`
- `Setting to idle on local client`
- `Removing current task`

No further attack hits occurred after Hawk death.

It then naturally switched to corpse carrying, helped deliver the Hawk body, completed the carry task, and was later reclaimable:

`Assigned Leader: MasaeAhmya (0)`

## Strong anti-regression result

For all three test attackers:

**post-death stale attack-hit count = 0**

This directly contrasts with S1.42Q/R where affected Pikmin continued logging `Hitting enemy with: 0.03` against the dead target until teardown.

The user's visible result matches the log:
- none of the three Pikmin disappeared;
- all remained available;
- all could be reclaimed/follow the player again.

## Adjacent lifecycle validation

The S1.42S patch-safety gate also gained useful adjacent coverage.

### Hawk -> Pikmin protection

During the test:

`[BaboonHawkPikminGuard] Blocked LethalMin BaboonBirdPikminEnemy.OnColideWithPikmin. Adapter remains enabled for native death/latch cleanup.`

This proves the narrow collision prevention path was actually exercised while the adapter remained enabled.

No BitePikmin/GrabPikmin block was required in this run, so those fallback paths were not directly exercised here.

### Corpse carry / Onion delivery

The dead Hawk body was naturally recognized and routed by LethalMin.

Relevant sequence:

- Pikmin found `BaboonHawkBody(Clone)`;
- `CarryItem` tasks were assigned;
- body route to Onion was created;
- `BaboonHawkBody(Clone) has reached its route end`;
- `Sucking item BaboonHawkBody(Clone) into onion`;
- carriers completed their tasks;
- `42 sprouts of Yellow Pikmin added to onion`;
- body despawn completed.

Therefore S1.42S did **not** break the adjacent corpse-carry/Onion lifecycle.

## Regression counters

Across the log:

- `Work state with no task assigned!`: **0**
- `Leader is null when following`: **0**
- old complete-adapter-disable marker: **0**
- S1.42R `LethalMinLatchedDeathGuard` marker: **0**

## Non-blocking teardown observation

One new LethalMin exception occurred only during lobby disconnect, after ShipOnion had already saved Pikmin:

`ArgumentException: NetworkObjectReference can only be created from spawned NetworkObjects.`

Stack:

`LethalMin.PikminNoticeZone.OnTriggerStay (UnityEngine.Collider other)`

Timing:

- `ShipOnion: saved 35 pikmin to disk on despawn.`
- LobbyControl disconnect begins.
- Exception fires once.
- scene/object teardown continues.

This did not occur during the Hawk interaction and did not affect recovery, carry, Onion delivery, or visible gameplay in the test.

Classification:

**monitor-only / non-blocking teardown issue**

Do not add a project patch for it without a reproducible user-facing problem and a full Patch Safety Review.

## Acceptance boundary

S1.42S is accepted for the isolated Baboon-Hawk/Pikmin regression gate.

This does **not** yet promote S1.42S as the final full gameplay baseline because:

- EnemyIsolation is still enabled;
- BCMER 1.71.0 is still temporarily disabled;
- final restoration/integration work remains.

The last fully accepted normal gameplay baseline remains S1.41 until the temporary isolated state is removed and the restored normal stack passes its next gate.

## Next controlled step

Per `Current/ENEMY_SPAWN_BASELINE_S1.42C.json`:

1. remove/disable the temporary EnemyIsolation overlay;
2. restore/carry forward enemy-related configuration exactly from the canonical S1.42C restore baseline except for later explicitly accepted permanent fixes;
3. preserve all S1.42S accepted permanent compatibility fixes;
4. do not silently change package versions;
5. reintroduce BCMER exact 1.71.0 only according to the controlled roadmap, not as an unrelated upgrade;
6. validate the normal enemy population before final S1.42 acceptance.
