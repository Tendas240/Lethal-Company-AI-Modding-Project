# 54 — S1.42O Runtime — No Cleanup Executed / Exact Native Method Is FinishTask()

**Date:** 2026-09-03  
**Game:** Lethal Company V81  
**Evidence build:** S1.42O

## Primary evidence

Evidence:
`RuntimeEvidence/S1.42O/20260903T171220Z/`

Raw log:
`RuntimeEvidence/S1.42O/20260903T171220Z/raw/LogOutput.log`

Log SHA-256:
`1a1251f19a1d82e90b72e82ac8e4babd523c32e695fe3d7923d4590debb0be71`

## User-observed result

Eight Pikmin disappeared again after the Baboon Hawk died.

## Critical startup result

S1.42O attempted to resolve:
`LethalMin.PikminAI.TaskFinished()`

Runtime result:
`TaskFinished()` does not exist.

Exact startup diagnostic:
`AccessTools.Method: Could not find method for type LethalMin.PikminAI and name TaskFinished and parameters ()`

The compatibility plugin then correctly refused to use the known-bad low-level `RemoveCurrentTask()` fallback.

Therefore **S1.42O performed no Baboon-Hawk death cleanup at all**.

## Runtime-exposed exact candidate list

The loaded LethalMin.PikminAI declares:
- `FinishTask():Void`
- `FinishTaskClientRpc():Void`
- `FinishTaskServerRpc():Void`
- `HandleIdleStateOnOwnerClient():Void`
- `RemoveCurrentTask():Void`
- `SetToIdle(Boolean):Void`
- `SetToIdleClientRpc():Void`
- `SetToIdleServerRpc():Void`
- `TryUnlatchPikmin():Boolean`
- `UnlatchPikmin():Void`
- other latch/task helpers.

The exact native non-RPC high-level task completion method is therefore:
`PikminAI.FinishTask()`

## Death sequence

Baboon Hawk death:
`17:11:15.502`

After death, the eight attack participants continued logging `Hitting enemy with: 0.03`.

SellBodies Dead Baboon Hawk body creation starts around:
`17:11:19.507`

The attack-hit loop continues beyond that point.

The eight names that later receive LethalMin unlatch cleanup during session teardown are:
- Yellow Pikmin_pxom
- Yellow Pikmin_XYErHj
- Yellow Pikmin_khCd
- Yellow Pikmin_LuMKh
- Yellow Pikmin_apYy5
- Yellow Pikmin_Aq9v9
- Yellow Pikmin_IkVZWQe
- Yellow Pikmin_PerDu

This matches the user's observed eight missing Pikmin.

The later `Unlatching Pikmin` lines at ~17:11:35 occur during teardown/despawn and are not a successful Hawk-death recovery.

## Root cause

S1.42O used the wrong guessed method name.

The runtime itself identifies the correct high-level native method as:
`LethalMin.PikminAI.FinishTask()`

This also aligns with the healthy S1.42N control path whose log text was:
`Task finished -> Setting to idle -> Removing current task`

## S1.42P direction

Resolve and invoke exact declared:
`LethalMin.PikminAI.FinishTask()`

Requirements:
- exact declaring type;
- zero parameters;
- void return;
- implementation body present;
- no direct RemoveCurrentTask fallback;
- retain one-shot SpawnedEnemies resolver;
- retain 4.0 m death zone;
- retain corpse guard unchanged;
- log per-Pikmin native FinishTask success and aggregate count.

## Acceptance

S1.42P must prove:
1. exact FinishTask() resolves at startup;
2. real Hawk attackers are selected;
3. native FinishTask() runs for those attackers at Hawk death;
4. attack-hit output stops promptly;
5. no sustained Work-state-with-no-task loop;
6. affected Pikmin remain visible, responsive and recoverable;
7. following count can be restored;
8. corpse remains Onion-carryable;
9. living Hawks ignore corpse;
10. Hawk -> Pikmin ignore remains intact;
11. no leader-null loop.

EnemyIsolation remains enabled. BCMER 1.71.0 remains disabled.
