# Externalized Cold History — Archive

The frozen legacy payload formerly stored under `Archive/` was removed from the current working tree on 2026-09-08 after a positive deletion audit.

Exact pre-externalization Git tree SHA:

`0dd155347e3174d5bdef7b27922ff738a9a11f35`

Verified recovery repository:

`Tendas240/Lethal-Company-AI-Modding-Project-PreOverhaul-20260904`

Frozen recovery commit:

`5dbd0e637a480d8591773e422bbca4b0654cad20`

Recovery rule: `Archive/<relative-path>` maps to the identical `Archive/<relative-path>` at that frozen recovery commit.

Machine authority and registered historical-reference mapping:

`Current/COLD_HISTORY_STORAGE.json`

No Git history rewrite was performed. The exact historical bytes also remain reachable through this primary repository's earlier Git history.

This directory is historical recovery infrastructure only. Current project truth remains in `Current/CURRENT_STATE.json` and routes through `Current/PROJECT_KNOWLEDGE_MAP.md`.
