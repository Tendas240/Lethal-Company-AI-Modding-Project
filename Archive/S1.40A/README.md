# S1.40A archived handover helpers

These files were formerly in `Current/` when S1.40A was the handover target.

They were moved here during the 2026-09-03 S1.41 handover cleanup because they retain historical/diagnostic value but are no longer current.

Important:
- Do not use these files as the current project state.
- The current canonical state is documented under `Current/`.
- S1.40A ultimately failed runtime acceptance because DawnLib per-content `Allow Editing Config=false` still overrode the intended natural-spawn suppression.
- S1.40B fixed that editing gate and passed.
