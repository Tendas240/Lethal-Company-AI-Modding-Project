name: Capture C3F10 DunGenReferenceFixer evidence

on:
  pull_request:
    paths:
      - 'AnalysisTools/inspect_universal_interior_c3f10.py'
      - 'SourceEvidence/UniversalInteriorViability/PhaseC3F10/**'
      - 'SourceEvidence/UniversalInteriorViability/PhaseC3F9/FINDINGS.md'
      - 'ProfileSources/S1.42AK/export.r2x'
      - '.github/workflows/universal-interior-c3f10-reference-restoration-evidence.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --disable-pip-version-check dnfile==0.18.0 dncil==1.0.2 PyYAML==6.0.2
      - name: Compile and self-test C3F10 helper
        run: |
          python -m py_compile AnalysisTools/inspect_universal_interior_c3f10.py
          python AnalysisTools/inspect_universal_interior_c3f10.py --out c3f10-output --self-test

  provenance:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --disable-pip-version-check dnfile==0.18.0 dncil==1.0.2 PyYAML==6.0.2
      - name: Bind exact checked-out head
        env:
          EXPECTED_HEAD: ${{ github.event.pull_request.head.sha || github.sha }}
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD"
      - name: Record unreviewed exact-package byte binding
        id: capture
        continue-on-error: true
        run: python AnalysisTools/inspect_universal_interior_c3f10.py --out c3f10-output --record-provenance
      - name: Upload derived provenance JSON only
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: c3f10-dungenreferencefixer-provenance-${{ github.event.pull_request.head.sha || github.sha }}
          path: c3f10-output/*.json
          if-no-files-found: error
          retention-days: 14
      - name: Enforce fail-closed provenance capture
        if: steps.capture.outcome != 'success'
        run: exit 1
