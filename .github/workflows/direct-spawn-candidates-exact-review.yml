name: Exact review direct S1.42AI spawn candidates

on:
  push:
    branches:
      - analysis/s142ai-direct-spawn-candidates-exact
    paths:
      - 'AnalysisTools/inspect_direct_spawn_candidates_exact.py'
      - '.github/workflows/direct-spawn-candidates-exact-review.yml'

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Install pinned ILSpy
        run: dotnet tool install --global ilspycmd --version 11.0.0.9375
      - name: Capture exact full-source review evidence
        run: python AnalysisTools/inspect_direct_spawn_candidates_exact.py
      - name: Preserve exact review artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: s142ai-direct-spawn-candidates-exact
          path: direct-spawn-candidates-output/
          if-no-files-found: error
          retention-days: 90
