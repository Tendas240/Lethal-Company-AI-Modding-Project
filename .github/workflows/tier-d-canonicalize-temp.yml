name: Canonicalize Tier-D S1.42AI review

on:
  push:
    branches:
      - analysis/s142ai-tier-d-exact
    paths:
      - 'AnalysisTools/canonicalize_tier_d_plan_temp.py'
      - '.github/workflows/tier-d-canonicalize-temp.yml'

permissions:
  contents: write

jobs:
  canonicalize:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Update exact S1.42AI planning checkpoint
        run: python AnalysisTools/canonicalize_tier_d_plan_temp.py
      - name: Commit bounded canonical plan update
        shell: bash
        run: |
          git diff --check
          git diff -- BuildSpecs/S1.42AI_PLAN.md
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add BuildSpecs/S1.42AI_PLAN.md
          git commit -m 'Update S1.42AI plan after Tier-D exact review'
          git push
